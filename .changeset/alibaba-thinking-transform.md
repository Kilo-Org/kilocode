---
"@kilocode/cli": patch
---

Register `@ai-sdk/alibaba` in the provider transform so thinking variants (`enableThinking` / `thinkingBudget`) and provider option key remapping work correctly for direct Alibaba provider configurations.

---
"@kilocode/cli": minor
---

Add a `/worktree` slash command and a `session.new.worktree` command palette entry that open a small dialog for creating a new git worktree workspace, then activate it so the next prompt starts a session in that worktree.

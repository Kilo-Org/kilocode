---
"@kilocode/kilo-jetbrains": patch
---

Truncate list row titles with an ellipsis when they do not fit, instead of cutting them off mid-character. Applies to every list that uses the shared row renderer, including the Agent Manager worktree list and the session and history lists, matching how the line under the title has always behaved.

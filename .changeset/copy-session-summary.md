---
"@kilocode/cli": minor
---

feat(tui): add "Copy session summary" command palette entry (`/copy-summary`) that generates a non-destructive markdown summary focused on accomplishments and architectural decisions, copies it to the clipboard, and offers Export / Copy / Close actions.

---
"kilo-code": minor
"@kilocode/cli": minor
"@kilocode/kilo-gateway": minor
"@kilocode/sdk": minor
---

Restore provider routing selection for Kilo Gateway and OpenRouter models: pin a model to a specific upstream inference provider (e.g. a faster or higher-precision endpoint) from the chat model bar or Settings → Models, instead of relying on automatic gateway routing.

---
"@kilocode/kilo-jetbrains": patch
---

Explain why file checkpoints weren't restored after a revert. The rollback banner now shows a specific reason - snapshots disabled, no checkpoint available, or the folder isn't a Git repository - instead of a single generic notice.

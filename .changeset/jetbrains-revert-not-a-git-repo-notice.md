---
"@kilocode/kilo-jetbrains": minor
---

Explain why file checkpoints weren't restored after a revert and add Checkpoints settings. The rollback banner now shows the specific reason, links directly to settings when snapshots are disabled, and JetBrains users can enable or disable snapshots without editing config files.

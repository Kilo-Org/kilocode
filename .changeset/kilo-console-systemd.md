---
"@kilocode/cli": minor
---

Add `kilo console install`, `uninstall`, `enable`, `disable`, and `is-enabled` subcommands to manage the Kilo Console as a systemd user or system service. On non-Linux hosts or hosts without systemd, the subcommands print a friendly message and exit non-zero.

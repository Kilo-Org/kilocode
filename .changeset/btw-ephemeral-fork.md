---
"kilo-code": minor
---

Add /btw side-question command that forks the current conversation with cached context, answers in a temporary fork, then deletes it without polluting main history. Running /btw with no args shows the most recent side answer.

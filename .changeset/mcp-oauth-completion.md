---
"@kilocode/cli": patch
---

Complete `kilo mcp auth` when another Kilo process updates the same server's stored OAuth credentials while the browser tab is open, and report which step of the authorization failed.

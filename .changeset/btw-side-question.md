---
"@kilocode/cli": minor
---

Add `/btw` command: ask a side question in a temporary read-only fork of the current session. The answer is shown in the transcript but excluded from the conversation history, and the fork is deleted afterwards. Run `/btw` with no arguments to see the most recent side question.

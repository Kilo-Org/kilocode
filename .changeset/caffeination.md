---
"@kilocode/cli": minor
"@kilocode/sdk": minor
"kilo-code": minor
---

Add a default-off Keep Awake toggle in VS Code that preserves screen locking and display sleep. Track foreground and background work so it stops when no agent can continue automatically, and remember the safety notice after one acceptance.

---
"@kilocode/cli": minor
"kilo-code": minor
"@kilocode/kilo-jetbrains": minor
---

Support Claude Code skills and slash commands by default, add separate compatibility toggles for skills/commands and CLAUDE.md instructions, label Claude-sourced slash commands, and prompt per repo when Claude context is detected. Claude command frontmatter is parsed like Kilo commands, but Claude-only fields such as allowed-tools are not enforced.

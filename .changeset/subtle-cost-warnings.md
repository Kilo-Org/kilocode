---
"kilo-code": minor
---

Show a short, dismissible notice when one model request costs at least the new Request Cost Notice amount (default $1, 0 disables), and before the next request skips the prompt cache because of a model or reasoning change, or because the session was idle longer than the new Cache Idle Notice time (default 5 minutes, 0 disables).

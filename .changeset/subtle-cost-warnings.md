---
"kilo-code": minor
---

Show a short, dismissible notice when one model request costs at least the new Request Cost Notice amount (default $1, 0 disables), and before a model or reasoning change makes the next request skip the prompt cache.

---
"@kilocode/cli": patch
"kilo-code": patch
---

Keep sandbox choices applied across session scopes, fail closed when supported backends become unavailable, and package the sandbox file-write worker with VS Code.

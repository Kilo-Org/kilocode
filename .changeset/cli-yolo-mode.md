---
"@kilocode/cli": patch
---

Add a `--auto-approve` CLI flag and `/auto-approve` TUI command to skip tool permission prompts for the current session. Aliases (`--yolo`, `/yolo`) and a separate global auto-approve command are also available; replies cover the active session and its direct Task child sessions.

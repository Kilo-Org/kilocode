---
"@kilocode/cli": patch
---

Remove the KiloClaw setup prompt shown by `/kiloclaw` (`/claw`) when no instance was provisioned. The command now opens the KiloClaw chat view directly, which already surfaces instance status on its own.

---
"kilo-code": minor
---

Make the model selector open with groups collapsed by default, expanding only the group holding the currently selected model, and pin the active group header while scrolling so switching between providers no longer requires scrolling through large catalogs.

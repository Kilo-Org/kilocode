---
"@kilocode/cli": minor
---

Add `--create-pr` flag to prompt the agent to create a pull request when the task completes. This flag requires `--auto` mode and instructs the agent to commit any uncommitted changes, push the branch, and create a PR using the GitHub CLI.

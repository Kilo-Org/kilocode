---
"kilo-code": patch
---

Fix settings dropdowns (including the autocomplete model selector) being clipped off the right edge of the viewport when the selected label is long — the menu now shrinks and expands leftward from the trigger to stay inside the viewport.

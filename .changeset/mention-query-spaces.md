---
"kilo-code": patch
---

Keep the `@` mention list open while typing spaces, so names with spaces are searchable — files such as `@my report.txt`, entries like `@past chats` and `@git changes`, and past chats by title directly in the list.

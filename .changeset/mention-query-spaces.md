---
"kilo-code": patch
---

Keep the `@` mention list open while typing spaces, so names with spaces are searchable — files such as `@my report.txt`, menu entries like `@past chats`, `@git changes`, and `@browse files`, and past chats by title directly in the list.

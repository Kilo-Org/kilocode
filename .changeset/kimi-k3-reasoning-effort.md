---
"@kilocode/cli": patch
---

Kimi K3 now offers the reasoning effort levels its API actually supports (`low`/`high`/`max`, plus `medium`/`xhigh` on the Anthropic-compatible endpoint) instead of the generic `instant`/`thinking` toggle.

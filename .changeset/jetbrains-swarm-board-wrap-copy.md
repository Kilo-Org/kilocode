---
"@kilocode/kilo-jetbrains": minor
---

Wrap full message bodies in the Swarm board instead of clipping them to one line, keep the board open when opening an agent from it, and add a "Copy all" action that copies the entire board history as plain text.

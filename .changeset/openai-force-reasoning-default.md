---
"@kilocode/cli": patch
---

Force reasoning on by default for the OpenAI provider when routing through the Kilo gateway, unless reasoning is explicitly disabled (`reasoning.enabled: false` or `reasoning.effort: "none"`).

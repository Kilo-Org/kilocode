---
"kilo-code": patch
"@kilocode/cli": patch
---

Honor project and user comment conventions in the fallback, Trinity, and Ling prompts instead of banning all code comments, so repository instructions such as "document every export" are followed.

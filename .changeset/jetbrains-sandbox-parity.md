---
"@kilocode/kilo-jetbrains": minor
---

Add sandbox confinement to the JetBrains plugin with global policy settings, prompt and `/sandbox` controls, and Worktree Run selection. Requests to run a command outside the sandbox always require your explicit approval, even with auto-approve enabled.

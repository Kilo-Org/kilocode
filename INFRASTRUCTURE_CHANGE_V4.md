# Infrastructure Change Review V4 — upstream v1.18.13 merge (round 4)

Reviewed HEAD: `b793883de6` (worktree HEAD is `596bf49680`; the top 3 commits are report-only docs — verified `git diff b793883de6..HEAD --name-only` lists only `*_V2.md`/`*_V3.md` report files, so the checkout matches the reviewed head for all repo content). New PR base: `4f59fcb666` (ancestor of head; three-dot == two-dot). Round-4 delta: `git diff b6505b164b..b793883de6` (151 files: round-3-fix `6d331a726f`, annotation `3b2686af84`, and a base-branch merge `72c57d9765`). Full PR diff: `git diff 4f59fcb666...b793883de6` (422 files). Upstream tag: `v1.18.13` = `a105350812`. Prior reports: `INFRASTRUCTURE_CHANGE.md`, `_V2.md`, `_V3.md`.

## Scope & methodology

Three passes. (1) Re-verified every open round-3 item at the new head by reading current files and checking whether the delta touches the relevant paths (it touches zero files under `patches/`, `artifacts/`, `.opencode/`). (2) Scrutinized the tasked delta infra files, separating **PR-introduced** from **base-branch-side** changes: of the 151 delta files only 27 appear in the full PR diff (`4f59fcb666...b793883de6`); the other 124 arrived via the `origin/main` merge and are already on the PR's target branch. Notably, several task-listed "new infra" items are **base-side, not PR-introduced**: `check-architecture.ts`, `architecture-allowlist.json`, the `check-opencode-annotations.yml` step, the root `check:architecture` script (present in base `4f59fcb666:package.json`), `packages/kilo-vscode/esbuild.js`, `packages/sdk/js/script/build.ts`, `packages/opencode/script/test-runner.ts`, `packages/kilo-vscode/script/local-bin.ts`, `nix/hashes.json`, `packages/opencode/script/generate.ts`, `packages/client/src/generated/*`, `packages/sdk/js/src/v2/gen/sdk.gen.ts`. The architecture-guard commits (`73be1056c2`, `c49c8c9f9d`, `c5608d0b7b`) are all ancestors of the base (`git merge-base --is-ancestor` verified). They were still reviewed as tasked, with provenance noted. (3) Fresh sweep of the full 422-file PR diff for infra patterns; ran guards: `check-workflows.ts`, `check-test-ci.ts`, `check-architecture.ts`, `check-opencode-annotations.ts` (both modes), `bun test ./script`, `bun install --frozen-lockfile`, and the targeted `bun test ./test/kilocode/test-cli.test.ts`.

## Prior-findings verification

### V3-1 — `patches/ghostty-web@0.3.0.patch` stale orphan: **STILL OPEN (unchanged)**

- Delta touches no patch files. At head: root `package.json` declares 16 `patchedDependencies`; every declared patch file exists; `patches/ghostty-web@0.3.0.patch` still sits on disk **undeclared**, and `packages/kilo-console/package.json` still depends on `ghostty-web@0.4.0`, so the 0.3.0 patch would not apply even if declared. No-op artifact, pre-existing on main (not introduced by this PR). `bun install --frozen-lockfile` passes regardless (undeclared patch files are ignored). Human action still pending: delete the stale patch or rebase/re-declare it for 0.4.0.

### Finding 3 (r1–r3) — `artifacts/` skip-list policy: **STILL OPEN (unchanged)**

- `artifacts/` is still absent from `skipFiles` in `script/upstream/utils/config.ts` (grep-verified), so upstream demo/marketing content keeps syncing in, and `artifacts/glm52-rise-video/package.json` is still stamped with Kilo's repo version `7.4.20` (release tooling keeps bumping it). The LFS push itself was verified in round 3; the sync-vs-exclude decision remains a human call.

### Finding 4 (r1–r3) — `.opencode/command/translate.md` orphaned reference: **STILL OPEN (trivial, unchanged)**

- File unchanged at `b793883de6`: still `model: opencode/gpt-5.6-sol` (upstream provider namespace, unresolvable in our CLI, inert at runtime). Still not skip-listed, unlike the sibling `script/translate-app.*` tooling removed in round 2. `.opencode/glossary/` holds 17 files at both round-3 and round-4 heads (V3 said 18 — an off-by-one in the V3 report, not a change). Future merges keep syncing upstream model references into these files.

### Finding N3 (r2) / round-3 changeset naming: **FIXED stays FIXED; wording polished**

- Delta diff for `.changeset/opencode-v1-18-1-to-v1-18-13.md` is one line: "Adopt OpenCode v1.18.1 through v1.18.13 improvements" → "Adopt upstream improvements from v1.18.1 through v1.18.13" (drops the OpenCode brand mention from release-notes text). Filename remains correctly range-named; bump set unchanged (`@kilocode/cli` + `kilo-code`, `patch`). Round-3's minor polish note stands (not a finding): still a single generic sentence where its two siblings carry detailed categorized bullets; changeset text ships verbatim in release notes.

### Prior fixes — **HOLD**

- **setup-bun single Node setup:** exactly one `Setup Node` step (upstream's unconditional `setup-node@49933ea5…` v4.4.0); Kilo's node-gyp header/nodedir blocks retained. Delta does not touch the file. Full-PR `.github/` footprint is still exactly two files: the setup-bun action and `test.yml`.
- **translate-app skip-listing:** `script/translate-app.{ts,test.ts,md}` still in `skipFiles`, files absent from the tree (`ls` no-match), `translate:app` still absent from root scripts.
- **remove-kilo-web transform:** intact — `script/upstream/transforms/remove-kilo-web.ts` present and wired into both `merge.ts` (import `transformKiloWeb`) and the `translate()` pipeline in `utils/upstream.ts` (`removeKiloWeb`); the omission markers are present in `packages/opencode/src/index.ts` (lines 5, 25).

## New findings

### V4-1. Empty `catch {}` in `packages/opencode/script/kilocode/test-cli.ts` violates the repo's no-empty-catch rule (low, PR-introduced)

- The PR-side test-CLI caching rework added a bare `} catch {}` (around the `existsSync`/`readFileSync` cache-hit probe, line ~69). AGENTS.md bans empty catch blocks outright ("at minimum, log it"); the same file's two other new catches both `console.warn`, so this one is inconsistent with the file's own pattern as well.
- Functional impact is nil — any probe failure falls through to a rebuild, which is the safe path — so this is style/robustness only. Human verification: either add a `console.warn` (matching the sibling catches) or consciously accept the exception to the rule.
- Adjacent convention nit (cross-report, for the markers reviewer): the delta also added `// kilocode_change - new file` to the top of this file, but it lives under `script/kilocode/` where markers are explicitly unnecessary per AGENTS.md ("Markers are NOT needed in paths that contain kilocode in the name").

No other new infra defects found in the delta or the full PR diff.

## Notable non-findings

- **Provenance: only 27 of 151 delta files are PR-introduced.** The PR-side infra set is `.changeset/opencode-v1-18-1-to-v1-18-13.md`, `package.json`, `bun.lock`, `packages/opencode/script/build.ts`, `packages/opencode/script/kilocode/test-cli.ts`, `packages/sdk/openapi.json`, `packages/sdk/js/src/v2/gen/types.gen.ts`; the remaining PR-side files are product code (config/mcp/session/agent-manager/i18n). Everything else tasked for this round entered via the base-branch merge and is already on the target branch.
- **Workflow allowlist intact.** `bun run script/check-workflows.ts` → ok (29 workflows), exit 0. The delta's only `.github/` change is `check-opencode-annotations.yml` (base-side): one added step, `Check domain architecture boundaries and state ratchets` → `bun run script/check-architecture.ts`. The file was already allowlisted, so no allowlist drift; content edits to an accepted workflow remain invisible to the guard (known by-design gap, noted in V2). Full-PR `.github/` diff is unchanged since round 2 (setup-bun action + `test.yml` only).
- **`script/check-architecture.ts` + `architecture-allowlist.json` (base-side CI guard) is sound and passes at this head** (`check-architecture: ok (12 classified Kilo ratchet sites, 0 boundary violations)`, exit 0). Five rules: domain directionality for `packages/{core,llm,schema}/src` (all three dirs exist; glob on a missing dir is not a risk today), `InstanceState.make` ratchet, direct-SQLite-constructor ratchet, `process.env`-in-tools ratchet, and no-raw-OS-ops in Kilo HttpApi handlers. Maintainability is good by design: ratchets are exact-count with **stale-entry detection**, so the allowlist is forced to shrink as sites are refactored, and violation messages name the file to update. Wiring is complete: workflow step + root `check:architecture` script (both base-side). One friction note for future merges (observation, not a finding): rule 4's allowlist opts three **upstream-shared** tool files (`mcp-websearch.ts`, `warpgrep.ts`, `websearch.ts`) into exact-count matching, so a future upstream merge that adds/removes a `process.env` read in those files will trip CI until the allowlist count is updated — deliberate ratchet behavior, but it slightly tensions with the file header's "upstream-owned files are exempt to prevent merge conflicts" claim.
- **Build/compile optimization — PR-side pieces are coherent.** `packages/opencode/script/build.ts`: adds an mtime-based up-to-date check for the Kilo Console bundle (inputs: kilo-console `src`/`package.json`/`vite.config.ts`/`index.html`, `kilo-web-ui`/`kilo-indexing`/`kilo-ui`/`ui`/`sdk/js` srcs, and root `bun.lock` — all verified to exist) and parallelizes console/sandbox-worker/sandbox-network bundling via `Promise.all`; the `rm -rf dist` move and both new blocks are wrapped in `kilocode_change` markers by annotation commit `3b2686af84` (verified in-file). mtime-caching fails safe after checkouts (git bumps mtimes → rebuild); CI always builds fresh since `dist/` is absent. `test-cli.ts` fingerprint cache (`sha256` over bun.lock + per-package src/migration/package.json mtimes+sizes, ignoring `kilo-vscode`/`kilo-jetbrains`/`kilo-docs` which cannot affect the CLI bundle) caches to `packages/opencode/.artifacts/test-cli-cached/` — covered by the existing `packages/opencode/.artifacts/` gitignore entry; explicit-`targetDir` callers (the regression test) bypass the cache. Targeted test run: `bun test ./test/kilocode/test-cli.test.ts` → 1 pass / 0 fail.
- **Build/compile optimization — base-side pieces reviewed statically.** `packages/kilo-vscode/esbuild.js`: replaces `esbuild-plugin-solid` with a babel-based transform cached in memory + on disk (`node_modules/.cache/esbuild-solid`, key = path+mtime+size+build-script-hash); extracts `getExtensionConfig()`/`getWebviewsConfig()`; keeps `minifyIdentifiers: false` for the extension bundle with a comment documenting the `@aws-sdk/credential-providers` Symbol-mangling rationale. `packages/sdk/js/script/build.ts`: cosmetic (single prettier invocation for both gen dirs). `packages/opencode/script/test-runner.ts`: switches from per-run `mkdtemp` build dirs to the persistent cached bundle (block newly wrapped in `kilocode_change` markers by `3b2686af84`). `packages/kilo-vscode/script/local-bin.ts`: drops the pre-build `bunx <pinned> install --frozen-lockfile` and adds a fallback that reruns the build via the **active** bun runtime if the pinned `bunx` build fails — note this relaxes the pinned-bun determinism the removed comment explicitly warned about; base-side, so already main's decision, flagged here only for awareness. `nix/hashes.json`: all four platform hashes updated in the same shape as prior rounds; correctness only verifiable by a nix build (not run). `packages/opencode/script/generate.ts`: models.dev snapshot now cached 6h on disk with a 5s fetch timeout and cache-fallback on fetch failure; all catches log; cache lives under `node_modules/.cache/` (gitignored).
- **Lockfile/manifest consistency verified live.** `bun install --frozen-lockfile` → exit 0, "Checked 2032 installs across 2274 packages (no changes)". The PR-side `package.json` diff vs base is unchanged in substance since round 3 (upstream catalog intake + `test:script:ci`, both previously assessed); the delta's only package.json line (`check:architecture`) is base-side.
- **Generated SDK/client matches the server at this head (verified).** Delta SDK change is exactly the PTY `size: {rows, cols}` field across `openapi.json`, `sdk.gen.ts`, `types.gen.ts`, and `packages/client/src/generated/*`, matching the `kilocode_change`-marked server changes in `packages/schema/src/pty.ts` (`CreateInput.size`) and `packages/core/src/pty.ts` (spawn with cols/rows) — the code is base-side, the regen propagated consistently (base and head both carry it). The PR-side generated diff vs base (`reasoning_details`→`reasoning_text` enum relaxation, `subagent_depth`, `interleaved` boolean relaxation, `x-websocket: true` on PTY connect) predates round 4 (present at round-3 head, 5/5 occurrence match) and re-verified against server sources at this head: `provider/provider.ts:1012`, `plugin/modal/models.ts:11`, `core/src/v1/config/config.ts:185`, `protocol/src/groups/pty.ts:131`. The delta's only Kilo route-handler change (`config-console.ts`) is behavioral (emits the pre-existing `Event.ConfigUpdated`; SDK event union already includes config-updated events) — no route/schema surface change, no regen owed.
- **Annotation guards behave as in prior rounds.** `--worktree`: "No shared upstream source files changed — nothing to check" (exit 0). `--base 4f59fcb666`: self-skips on merge detection (exit 0). The shared-file edits observed in the delta (`build.ts`, `test-runner.ts`, pty files, config-console.ts) all carry proper `kilocode_change` markers; `3b2686af84` exists precisely to add the two script markers.
- **Fresh sweep of the full 422-file PR diff** found no previously unassessed infra except `packages/llm/script/recording-cost-report.ts` (one-line URL change `models.dev` → `models.opencode.ai`), which is **verbatim upstream** (confirmed identical at `a105350812`). No Docker, `turbo.json`, `.npmrc`/`bunfig.toml`, `.husky` content, publish/release script, issue template, CODEOWNERS, dependabot, or `.gitattributes` changes anywhere in the PR. `.opencode-version` remains `v1.18.13`. `patches/` churn in the PR diff is exclusively the round-1/2-assessed upstream patch set (mistral/mcp-sdk/virtual-core/solid-js rekeying) — zero round-4 patch changes. Per-package `package.json` diffs are version stamps (7.4.16→7.4.20), `test:ci` wiring, and upstream catalog intake, all previously assessed.
- **Root tooling tests pass.** `bun test ./script` → 62 pass / 0 fail across 11 files (same count as round 3 — no new test file for check-architecture, which is acceptable since `check-test-ci.ts` only mandates scheduling for existing test files; `check-test-ci.ts` itself → ok, 25 packages, 11 root script test files).

## Command outputs

| Command | Result |
|---|---|
| `git diff --name-only b6505b164b..b793883de6 \| wc -l` | 151 |
| `git merge-base 4f59fcb666 b793883de6` | `4f59fcb666` (base is ancestor) |
| `git merge-base --is-ancestor 73be1056c2/c49c8c9f9d/c5608d0b7b 4f59fcb666` | all three in base branch |
| `bun run script/check-workflows.ts` | `check-workflows: ok (29 workflows)`, exit 0 |
| `bun run script/check-architecture.ts` | `ok (12 classified Kilo ratchet sites, 0 boundary violations)`, exit 0 |
| `bun run script/check-test-ci.ts` | `ok (25 test-bearing package(s), 11 root script test file(s))`, exit 0 |
| `bun run script/check-opencode-annotations.ts --worktree` | "No shared upstream source files changed", exit 0 |
| `bun run script/check-opencode-annotations.ts --base 4f59fcb666` | "Skipping … upstream merge detected", exit 0 |
| `bun install --frozen-lockfile` | "Checked 2032 installs across 2274 packages (no changes)", exit 0 |
| `bun test ./script` (root) | 62 pass / 0 fail, 11 files |
| `bun test ./test/kilocode/test-cli.test.ts` (packages/opencode) | 1 pass / 0 fail |
| `grep -c "Setup Node" .github/actions/setup-bun/action.yml` | 1 (upstream v4.4.0 pin) |
| `grep '"artifacts' script/upstream/utils/config.ts` | no match (still not skip-listed) |
| `git show a105350812:packages/llm/script/recording-cost-report.ts` | identical `models.opencode.ai` URL (verbatim upstream) |

## Limitations

- No builds were executed (no `script/build.ts` end-to-end run, no extension `compile`/`package`, no Gradle, no nix build); the kilo-console/test-cli caching and esbuild rework were verified statically plus the one targeted test noted above. Nix hash correctness remains unverifiable without a nix build.
- The mtime-based caches (kilo-console, test-cli fingerprint, esbuild-solid disk cache) share the usual mtime caveat: safe after git operations (mtimes bump), but a hand-crafted tree with stale mtimes could serve a stale bundle; all three fail safe on cache-miss and none are used to gate CI correctness.
- The `bun.lock` delta was reviewed at the level of the frozen-install result and prior rounds' key-level analysis, not line-by-line.
- GitHub PR/CI state was not inspected; guard results above are local executions at the reviewed head.
- Delta product-code changes (agent-manager, sandbox policy, review command, i18n, UI) were infra-scoped out except where they intersect build/release/SDK tooling.
- The review ran at the PR branch head `596bf49680` after verifying the top 3 commits touch only report files; no repository files were modified other than this report, and nothing was committed.

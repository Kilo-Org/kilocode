# واجهة سطر الأوامر (CLI)

> **نظرة عامة:** نسخة سطر الأوامر المستقلة من Kilo Code
> **المسار:** `cli/` > **النقطة الرئيسية:** `cli/src/index.ts`

## 🏗️ نظرة عامة على البنية

واجهة سطر الأوامر (CLI) هي نسخة مستقلة من Kilo Code تعمل في الطرفية بدون الحاجة إلى VS Code. مبنية باستخدام Node.js و TypeScript.

### الهيكل الرئيسي

```
cli/src/
├── index.ts              # نقطة الدخول الرئيسية
├── cli.ts                # منطق CLI الأساسي
├── commands/             # الأوامر المتاحة
├── config/               # إدارة الإعدادات
├── services/             # الخدمات الأساسية
├── ui/                   # واجهة المستخدم الطرفية
├── auth/                 # المصادقة والحسابات
├── state/                # إدارة الحالة
└── utils/                # الأدوات المساعدة
```

## 🚀 التشغيل والاستخدام

### نقطة الدخول

```typescript
// cli/src/index.ts
#!/usr/bin/env node

import { Command } from "commander"
import { CLI } from "./cli.js"

const program = new Command()
program
    .name("kilocode")
    .description("Kilo Code Terminal User Interface - AI-powered coding assistant")
    .version(Package.version)
```

### خيارات التشغيل

```bash
# التشغيل الأساسي
kilocode

# تحديد وضع التشغيل
kilocode --mode code

# تحديد مساحة العمل
kilocode --workspace /path/to/project

# التشغيل التلقائي
kilocode --auto

# التشغيل الموازي
kilocode --parallel

# استعادة جلسة
kilocode --session <sessionId>
```

## 🎨 واجهة المستخدم الطرفية (TUI)

### 1. نظام الواجهة

مبنية باستخدام React مع Ink لعرض واجهة مستخدم تفاعلية في الطرفية.

```typescript
// ui/components/App.tsx
export function App() {
	// واجهة المستخدم الطرفية
	// عرض الرسائل
	// معالجة الإدخال
}
```

### 2. المكونات الرئيسية

- **ChatInterface** - واجهة الدردشة
- **MessageList** - قائمة الرسائل
- **InputArea** - منطقة الإدخال
- **StatusBar** - شريط الحالة
- **SidePanel** - اللوحة الجانبية

### 3. التفاعل

- التنقل بلوحة المفاتيح
- الاختصارات السريعة
- التمرير والبحث
- عرض الأكواد والملفات

## 🔧 الأوامر المتاحة

### 1. الأوامر الأساسية

```typescript
// commands/
├── chat.ts              # بدء الدردشة
├── auth.ts              # المصادقة
├── config.ts            # الإعدادات
├── modes.ts             # الأوضاع
└── workspace.ts         # مساحة العمل
```

### 2. أوامر متقدمة

- **parallel** - التشغيل الموازي
- **session** - إدارة الجلسات
- **debug** - التصحيح
- **logs** - عرض السجلات

## 🧠 الأوضاع (Modes)

### 1. الأوضاع الافتراضية

```typescript
// constants/modes/defaults.ts
export const DEFAULT_MODES = [
	{
		slug: "code",
		name: "Code",
		description: "General purpose coding assistant",
	},
	{
		slug: "plan",
		name: "Plan",
		description: "Architecture and planning",
	},
	{
		slug: "debug",
		name: "Debug",
		description: "Debugging and troubleshooting",
	},
]
```

### 2. الأوضاع المخصصة

يمكن تحميل أوضاع مخصصة من ملف الإعدادات.

```typescript
// config/customModes.ts
export function loadCustomModes() {
	// تحميل الأوضاع المخصصة
	// التحقق من الصحة
	// الدمج مع الأوضاع الافتراضية
}
```

## 🔐 المصادقة والأمان

### 1. نظام المصادقة

```typescript
// auth/index.ts
export async function authWizard() {
	// معالج المصادقة
	// حفظ التوكنات
	// التحقق من الصحة
}
```

### 2. إدارة التوكنات

- تخزين التوكنات بشكل آمن
- تحديث التوكنات تلقائياً
- دعم مقدمي الخدمة المتعددين

### 3. الأمان

- تشفير البيانات الحساسة
- صلاحيات محدودة
- التحقق من الهوية

## 📊 إدارة الحالة

### 1. حفظ الجلسات

```typescript
// state/session.ts
export class SessionManager {
	// حفظ الجلسات
	// استعادة الجلسات
	// مزامنة الحالة
}
```

### 2. السجل والتاريخ

- حفظ المحادثات
- البحث في السجل
- استعادة السياق

### 3. الإعدادات

```typescript
// config/persistence.ts
export function saveConfig(config: Config) {
	// حفظ الإعدادات
	// التحقق من الصحة
	// إعادة التحميل
}
```

## 🔄 التشغيل الموازي

### 1. الفروع الموازية

```typescript
// parallel/parallel.ts
export function getParallelModeParams() {
	// إنشاء فرع جديد
	// إدارة التغييرات
	// الدمج عند الانتهاء
}
```

### 2. المزامنة

- مزامنة التغييرات
- حل التعارضات
- الحفاظ على النسخ الاحتياطية

## 🌐 التكامل مع الخدمات

### 1. مقدمو الخدمة

```typescript
// services/providers/
├── kilocode.ts          # Kilo Code API
├── openai.ts            # OpenAI API
├── anthropic.ts         # Anthropic API
└── local.ts             # النماذج المحلية
```

### 2. الاتصال بالشبكة

- إدارة الاتصال
- معالجة الأخطاء
- إعادة المحاولة التلقائية

### 3. التخزين المؤقت

- تخزين الاستجابات
- تقليل الاستهلاك
- تحسين الأداء

## 🧪 الاختبار والتصحيح

### 1. أوضاع التصحيح

```typescript
// debug/index.ts
export const DEBUG_MODES = {
	verbose: true,
	network: true,
	timing: true,
}
```

### 2. السجلات

```typescript
// services/logs.ts
export const logs = {
	info: (message) => console.log(message),
	warn: (message) => console.warn(message),
	error: (message) => console.error(message),
}
```

### 3. الاختبار

- اختبارات الوحدات
- اختبارات التكامل
- اختبارات end-to-end

## 📈 الأداء والتحسين

### 1. تحسين التشغيل

- التحميل غير المتزامن
- التخزين المؤقت الذكي
- معالجة الإدخال الفعالة

### 2. إدارة الموارد

- التحكم في الذاكرة
- إدارة الاتصالات
- تنظيف الموارد

### 3. التوسع

- دعم المشاريع الكبيرة
- معالجة الملفات المتعددة
- التنفيذ الموازي

## 🚀 النشر والتوزيع

### 1. البناء

```bash
# بناء CLI
pnpm cli:build

# تجميع الحزمة
pnpm cli:bundle
```

### 2. التوزيع

- NPM packages
- Binary executables
- Docker images

### 3. التحديثات

- التحقق من التحديثات
- التنزيل التلقائي
- الترقية السلسة

---

**ملخص:** واجهة سطر الأوامر توفر تجربة Kilo Code كاملة في الطرفية مع دعم كامل لجميع الميزات الأساسية والمتقدمة.

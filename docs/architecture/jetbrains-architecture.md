# ملحق JetBrains (JetBrains Plugin)

> **نظرة عامة:** ملحق Kilo Code لبيئة تطوير JetBrains
> **المسار:** `jetbrains/` > **اللغة:** Kotlin + Node.js/TypeScript

## 🏗️ نظرة عامة على البنية

ملحق JetBrains يجلب Kilo Code إلى بيئة تطوير IntelliJ IDEA وغيرها من منتجات JetBrains. يتكون من جزأين رئيسيين:

1. **الملحق (Plugin)** - مكتوب بلغة Kotlin لـ IntelliJ Platform
2. **المضيف (Host)** - خادم Node.js/TypeScript لتشغيل منطق Kilo Code

### الهيكل الرئيسي

```
jetbrains/
├── plugin/              # ملحق IntelliJ Platform
│   ├── src/main/kotlin/ # كود Kotlin الرئيسي
│   ├── build.gradle.kts # إعدادات البناء
│   └── plugin.xml       # وصف الملحق
├── host/                # خادم Extension Host
│   ├── src/            # كود TypeScript
│   ├── bootstrap.ts    # تشغيل الخادم
│   └── typings/        # تعريفات TypeScript
└── scripts/            # سكربتات البناء والتحقق
```

## 🔧 المتطلبات الأساسية

### 1. Java Development Kit (JDK) 21

```bash
# تثبيت Java 21 باستخدام SDKMAN
curl -s "https://get.sdkman.io" | bash
sdk install java 21.0.5-tem
sdk use java 21.0.5-tem
```

### 2. VSCode Submodule

- **المسار:** `deps/vscode/`
- **الغرض:** توفير VSCode runtime dependencies
- **التهيئة:** يجب تهيئته قبل البناء

### 3. Node.js و pnpm

- **Node.js:** الإصدار 20.x
- **pnpm:** لإدارة مساحة العمل

## 🚀 عملية البناء

### 1. التحقق من الاعتماديات

```bash
# التحقق من الاعتماديات يدوياً
./jetbrains/scripts/check-dependencies.sh
```

### 2. بناء الملحق

```bash
# بناء ملحق JetBrains
pnpm jetbrains:build

# تشغيل البناء
pnpm jetbrains:run

# بناء الحزمة
pnpm jetbrains:bundle
```

## 🧩 بنية الملحق (Plugin)

### 1. الإعدادات الأساسية

```kotlin
// plugin/build.gradle.kts
plugins {
    id("java")
    id("org.jetbrains.kotlin.jvm") version "1.9.10"
    id("org.jetbrains.intellij") version "1.15.0"
}

intellij {
    version.set("2023.2")
    type.set("IC") // IntelliJ Community Edition
}
```

### 2. وصف الملحق

```xml
<!-- plugin/plugin.xml -->
<idea-plugin>
    <id>ai.kilocode.kilocode</id>
    <name>Kilo Code</name>
    <vendor email="support@kilo.ai" url="https://kilo.ai">Kilo Code</vendor>

    <description><![CDATA[
        AI-powered coding assistant for JetBrains IDEs
    ]]></description>
</idea-plugin>
```

### 3. المكونات الرئيسية

```
plugin/src/main/kotlin/ai/kilocode/
├── KiloCodePlugin.kt      # نقطة دخول الملحق
├── toolwindow/           # نوافذ الأدوات
├── editor/               # تكامل المحرر
├── actions/              # الإجراءات والأوامر
├── settings/             # الإعدادات
└── utils/                # الأدوات المساعدة
```

## 🏠 بنية المضيف (Host)

### 1. نقطة الدخول

```typescript
// host/bootstrap.ts
export async function bootstrap() {
	// تشغيل خادم Extension Host
	// إعداد الاتصال بالملحق
	// تهيئة الخدمات
}
```

### 2. الاتصال بالملحق

```typescript
// host/src/extension-host.ts
export class ExtensionHost {
	// معالجة الرسائل من الملحق
	// تنفيذ أوامر Kilo Code
	// إدارة الجلسات
}
```

### 3. الخدمات

```
host/src/
├── extension-host.ts     # المضيف الرئيسي
├── message-handler.ts    # معالجة الرسائل
├── session-manager.ts    # إدارة الجلسات
└── utils/                # الأدوات المساعدة
```

## 🔄 التكامل مع IntelliJ Platform

### 1. نوافذ الأدوات (Tool Windows)

```kotlin
// toolwindow/KiloCodeToolWindow.kt
class KiloCodeToolWindow {
    // عرض واجهة Kilo Code
    // معالجة تفاعل المستخدم
    // إدارة الحالة
}
```

### 2. تكامل المحرر (Editor Integration)

```kotlin
// editor/KiloCodeEditorManager.kt
class KiloCodeEditorManager {
    // الإكمال التلقائي
    // الإجراءات السياقية
    // التحقق من الكود
}
```

### 3. الإجراءات (Actions)

```kotlin
// actions/KiloCodeActions.kt
class KiloCodeActions {
    // أوامر القائمة
    // اختصارات لوحة المفاتيح
    // الأحداث السياقية
}
```

## 🎨 واجهة المستخدم

### 1. تصميم الواجهة

- استخدام Swing/JavaFX لواجهة المستخدم
- تكامل مع سمة IntelliJ
- دعم الوضع المظلم/الفاتح

### 2. عرض الويب

```kotlin
// webview/KiloCodeWebView.kt
class KiloCodeWebView {
    // عرض واجهة الويب
    // معالجة الأحداث
    // التواصل مع JavaScript
}
```

### 3. التخصيص

- إعدادات الملحق
- تخصيص السلوك
- إدارة مفاتيح API

## 🔐 الأمان والإعدادات

### 1. إدارة المفاتيح

```kotlin
// settings/KiloCodeSettings.kt
class KiloCodeSettings {
    // تخزين مفاتيح API
    // إعدادات الخصوصية
    // التحقق من الصحة
}
```

### 2. الصلاحيات

- التحكم في الوصول للملفات
- صلاحيات الشبكة
- إعدادات الأمان

### 3. التشفير

- تشفير البيانات الحساسة
- تخزين آمن للتوكنات
- اتصالات آمنة

## 🧪 الاختبار والتصحيح

### 1. بيئة التطوير

```bash
# تشغيل بيئة التطوير
pnpm jetbrains:run

# البناء للتصحيح
pnpm jetbrains:build
```

### 2. الاختبار

```kotlin
// test/KiloCodePluginTest.kt
class KiloCodePluginTest {
    // اختبار الوظائف الأساسية
    // اختبار التكامل
    // اختبار الأداء
}
```

### 3. التصحيح

- استخدام IntelliJ Debugger
- سجلات الملحق
- تصحيح المضيف

## 📦 النشر والتوزيع

### 1. بناء الحزمة

```bash
# بناء حزمة الإصدار
pnpm jetbrains:bundle

# إنشاء ملف التثبيت
gradle buildPlugin
```

### 2. النشر

- JetBrains Plugin Repository
- التوزيع المباشر
- التحديثات التلقائية

### 3. التوافق

- IntelliJ IDEA
- PyCharm
- WebStorm
- PhpStorm
- وغيرها...

## 🔄 التوافق مع VS Code Extension

### 1. مشاركة الكود

- استخدام نفس الخدمات الأساسية
- مشاركة منطق الأعمال
- توافق الواجهات

### 2. التكامل

```typescript
// host/src/vscode-compat.ts
export class VSCodeCompatibility {
	// محاكاة VSCode API
	// تحويل الأحداث
	// توافق البيانات
}
```

### 3. المزامنة

- مزامنة الإعدادات
- مشاركة الجلسات
- توافق البيانات

## 🚀 التحسينات المستقبلية

### 1. الميزات المخططة

- دعم المزيد من منتجات JetBrains
- تكامل أعمق مع المحرر
- تحسينات الأداء

### 2. التحسينات

- تقليل استهلاك الموارد
- تحسين سرعة الاستجابة
- تحسين واجهة المستخدم

### 3. التوسع

- دعم اللغات الإضافية
- تكامل مع أدوات أخرى
- مكونات إضافية قابلة للتخصيص

---

**ملخص:** ملحق JetBrains يجلب تجربة Kilo Code الكاملة إلى بيئة تطوير JetBrains مع الحفاظ على التوافق مع امتداد VS Code.

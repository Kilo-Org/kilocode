Search the codebase using an AI-powered code search agent. It performs intelligent multi-step searches across the repository using parallel tool calls, returning only the most relevant code spans. Use this for complex semantic queries where simple grep patterns would be insufficient.
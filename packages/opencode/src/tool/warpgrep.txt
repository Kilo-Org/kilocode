Search the codebase using WarpGrep, an AI-powered code search agent. WarpGrep performs intelligent multi-step searches across the repository using parallel tool calls, returning only the most relevant code spans. Use this for complex semantic queries where simple grep patterns would be insufficient. Requires MORPH_API_KEY or WARPGREP_API_KEY.
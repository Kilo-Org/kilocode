/**
 * Error code for a failed revert or redo. The extension host has no translations, so it tags
 * the error with this code and the webview supplies the toast wording.
 */
export const REVERT_ERROR_CODE = "revert.error.body"

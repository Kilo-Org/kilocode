Add the following section to the existing `packages/kilo-docs/code-with-ai/agents/model-selection.md` file, after the existing model selection content and before any troubleshooting sections:

```markdown
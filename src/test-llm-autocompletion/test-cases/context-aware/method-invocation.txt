#### Description: Should autocomplete method invocation from class instance
#### Filename: user-service.ts
interface User {
  id: string;
  email: string;
  name: string;
}

class UserService {
  async findById(id: string): Promise<User | null> {
    return null;
  }

  async findByEmail(email: string): Promise<User | null> {
    return null;
  }

  async updateUser(id: string, data: Partial<User>): Promise<User> {
    return {} as User;
  }
}

// Usage example
const userService = new UserService();
const userId = "user-123";
const user = await userService.<<<AUTOCOMPLETE_HERE>>>
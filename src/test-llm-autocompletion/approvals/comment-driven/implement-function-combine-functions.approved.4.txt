
function addNumbers(a, b) {
    return a + b
}

function multiplyBy5(a) {
    return a * 5
}

// Implement function to combine above functions
// addNumbers and multiplyBy5
// such that the result of addNumbers is multiplied by 5

function combine(a, b) {
    return multiplyBy5(addNumbers(a, b))
}
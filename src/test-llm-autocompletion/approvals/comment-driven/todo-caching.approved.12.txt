class DataService {
  // TODO: Implement caching mechanism to reduce API calls
  constructor() {
    this.cache = new Map();
  }

  async fetchData(id) {
    return await api.get(`/data/${id}`);
  }
}
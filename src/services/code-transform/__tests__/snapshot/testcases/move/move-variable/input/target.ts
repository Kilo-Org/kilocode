/**
 * Target file for moving a variable
 */

export const existingVariable = "This is an existing variable in the target file"

export function targetFunction(): string {
	return `Target function with ${existingVariable}`
}

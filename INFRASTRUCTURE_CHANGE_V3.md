# Infrastructure Change Review V3 — upstream v1.18.13 merge (round 3)

Reviewed HEAD: `b6505b164b` (worktree HEAD is `1b05d7343f`, which only adds round-1/2 report docs on top; reviewed at detached `b6505b164b`). New PR base: `6fce4e2564` (branch merged latest `origin/main`). Round-3 delta: `git diff 37a5cbf5db..b6505b164b` (433 files). Full PR diff: `git diff 6fce4e2564...b6505b164b` (419 files). Upstream tag: `v1.18.13` = `a105350812`. Prior reports: `INFRASTRUCTURE_CHANGE.md`, `INFRASTRUCTURE_CHANGE_V2.md`.

## Scope & methodology

Three passes. (1) Re-verified every open round-2 item at the new head by reading files and, where possible, exercising live checks: the LFS question was settled by querying the fork's LFS batch API directly (network was available this round). (2) Reviewed the delta's infra footprint, separating **PR-introduced** changes from **main-side** changes inherited via the `origin/main` merge: `git diff 6fce4e2564...b6505b164b` shows the full PR now contains only two `.changeset/` files, zero `nix/`, zero `plans/`, zero `packages/kilo-vscode/script/`, zero `packages/kilo-jetbrains/` build files, and zero `.kilo/` files — all of those entered the delta from `origin/main` and are already on main (base `6fce4e2564` contains them). They were still sanity-checked as tasked, with provenance noted. (3) Fresh sweep of the full 419-file PR diff for infra patterns; ran guards: `bun run script/check-workflows.ts` (ok, 29 workflows), `bun run script/check-test-ci.ts` (ok, 25 packages, 11 root script test files), `bun test ./script` (62 pass / 0 fail, 11 files), `bun install --frozen-lockfile` (ok — "Checked 2057 installs across 2308 packages (no changes)").

## Prior-findings verification

### Finding 3 (r1/r2) — `artifacts/glm52-rise-video/` LFS push: **RESOLVED (push verified); skip-list policy STILL OPEN**

- **LFS push verified.** Queried `https://github.com/Kilo-Org/kilocode.git/info/lfs/objects/batch` for all five mp4 OIDs with their real sizes: every object returns a `download` action, no errors (`e732068df8f5` 333945 B, `dd7dfb21da6e` 1591636 B, `44d0db412b4b` 349608 B, `4711b5fc12c0` 2569837 B, `fec0900bb125` 2788751 B). The objects exist in the fork's LFS storage; fresh clones with `lfs: true` will smudge correctly. The blocker from rounds 1–2 is closed.
- **Policy sub-issues unchanged:** `artifacts/` is still not in `skipFiles` (verified in `script/upstream/utils/config.ts`), so upstream demo/marketing content keeps syncing in, and `artifacts/glm52-rise-video/package.json` is still stamped with Kilo's repo version `7.4.20` (release tooling will keep bumping it). The sync-vs-exclude decision remains a human call.

### Finding 4 (r1/r2) — `.opencode/command/translate.md` orphaned reference: **STILL OPEN (trivial)**

- File unchanged at `b6505b164b`: still `model: opencode/gpt-5.6-sol` — upstream's provider namespace, unresolvable in our CLI; inert at runtime (Kilo scans `.kilo`/`.kilocode`). Neither `.opencode/command/translate.md` nor `.opencode/glossary/` (18 files, all present) was skip-listed, while the sibling `script/translate-app.*` tooling was removed and skip-listed in round 2. Future merges keep syncing upstream model references into these files. Recommend skip-listing both paths or explicitly accepting the drift.

### Finding N3 (r2) — changeset naming: **FIXED**

- The delta deleted `.changeset/opencode-v1-18-0.md` and replaced it with `.changeset/opencode-v1-17-13-to-v1-18-0.md` — range-named per the established convention, with a detailed categorized bullet list matching the style of `opencode-v1-17-9-to-v1-17-13.md`.
- New `.changeset/opencode-v1-18-1-to-v1-18-13.md` covers the remaining upstream range, correctly range-named, `patch` for `@kilocode/cli` + `kilo-code`. Minor polish note (not a finding): it is a single generic sentence where its two siblings carry detailed bullets; changeset text ships verbatim in release notes.

### Round-2 confirmed fixes — **HOLD**

- **setup-bun single Node setup:** `.github/actions/setup-bun/action.yml` still has exactly one `Setup Node` step (upstream's unconditional v4.4.0); Kilo's node-gyp header/nodedir block retained. The delta does not touch the file. Full-PR `.github/` footprint is unchanged since round 2 (setup-bun action + `test.yml` only); `check-workflows.ts` passes (no add/remove; both files already allowlisted).
- **translate-app skip-listing:** intact after the new transforms landed — `script/translate-app.{ts,test.ts,md}` still in `skipFiles`, `translate:app` still in `DELETE_UPSTREAM_SCRIPTS`, files absent from the tree, `skip-files.test.ts` coverage still passing.

## New findings

### V3-1. `patches/ghostty-web@0.3.0.patch` is a stale orphan (low, pre-existing on main — human verification)

- At `b6505b164b`, root `package.json` declares 16 `patchedDependencies`; every declared patch file exists, but `patches/ghostty-web@0.3.0.patch` sits on disk **undeclared**. Meanwhile `packages/kilo-console/package.json` depends on `ghostty-web@0.4.0`, so even if declared, the 0.3.0 patch would not apply. The file is a no-op artifact.
- Provenance: the orphan state exists identically at base `6fce4e2564` and at round-2 head `37a5cbf5db` — **not introduced by this PR** (the file is untouched by the full PR diff). Round 1's "Kilo-only patches … untouched" was accurate but did not notice the declaration had already been dropped on main. Flagged per the in-doubt rule: either delete the stale patch or re-declare/rebase it for 0.4.0 if the patch is still needed. `bun install --frozen-lockfile` passes regardless, since undeclared patch files are ignored.

No other new infra defects found in the delta or the full PR diff.

## Notable non-findings

- **All task-listed delta infra files are main-side, not PR-introduced.** `nix/hashes.json`, `packages/kilo-vscode/script/{dev-snapshot,ffmpeg-helper,local-bin}.ts`, `packages/kilo-vscode/eslint.config.mjs`, the `packages/kilo-jetbrains/` build files, `.kilo/plans/`, `plans/`, `.kilo/skills/jetbrains-cli-pin/`, and 34 of the 36 delta changesets are absent from the full PR diff (`6fce4e2564...b6505b164b`); they arrived via the `origin/main` merge and are already reviewed/shipped on main. The PR itself adds exactly two changesets (the two opencode merge changesets above) and no plans files.
- **`package.json` / `bun.lock` delta is internally consistent and now transform-backed.** `opentui-spinner` was removed from the catalog and from `packages/opencode` + `packages/tui` deps, and the removal is encoded in the merge tooling (`DELETE_UPSTREAM_CATALOG` + new `DELETE_UPSTREAM_DEPENDENCIES` in `transform-package-json.ts`, with test coverage), so future merges won't reintroduce it. `cli-spinners` top-level downgrade 3.4.0 → 2.9.2 is the correct consequence (only `opentui-spinner` needed 3.x). No remaining `opentui-spinner` reference in any workspace `package.json`. Frozen lockfile install passes.
- **Root `dev` script flip is main-side; AGENTS.md line is stale on main too.** `"dev": "KILO_CLIENT=cli bun run --cwd packages/opencode --conditions=node src/index.ts"` came from `3917ed1f9b` (ancestor of `origin/main`). Root `dev` now uses `--conditions=node` while `packages/opencode/package.json` `dev`/`dev:temporary`, root `dev-setup`, and `AGENTS.md` line 12 still say `--conditions=browser`. Inherited inconsistency, not a PR artifact — noted for the main-branch backlog.
- **`nix/hashes.json`** updated for all four platforms in the same delta as the lockfile change — consistent shape; hash correctness itself can only be verified by a nix build (not run).
- **SDK regen matches the server at this head (verified).** The delta's `packages/sdk/openapi.json` (+222), `sdk.gen.ts` (+83), `types.gen.ts` (+82) cover exactly the server changes: new Kilo routes `/kilocode/command/files` and `/kilocode/command/remove` (implemented in `packages/opencode/src/kilocode/server/httpapi/groups/kilocode.ts:55-56`) and the session-diff `full`/`file` query params (implemented with proper `kilocode_change` markers in `server/routes/instance/httpapi/{groups,handlers}/session.ts`; `full` is `enum: ["true","false"]`, `file` is `type: string` in the spec). No stale or missing SDK surface detected; no regen flag needed.
- **`script/upstream/*` delta is coherent Kilo-owned merge tooling.** New `transforms/remove-kilo-web.ts` replaces the `WebCommand` import and registration in `packages/opencode/src/index.ts` with `kilocode_change` comments (output verified present at head, lines 25 and 109), flags unfamiliar shapes for review instead of rewriting them, and is wired into both `merge.ts` and the `translate()` conflict pipeline in `utils/upstream.ts`; `web.ts` itself is skip-listed. `transform-i18n.ts` now gates marker emission behind a `markers` param so the generic `translate()` path no longer appends stray markers. Tests cover all of it (`bun test ./script`: 62 pass / 0 fail). Cosmetic nit only: `removeKiloWeb`'s `removals` is a boolean-as-count (at most 1 even when both import and registration are replaced) — affects log text only.
- **ffmpeg acquisition (main-side, checked as tasked).** `packages/kilo-vscode/script/ffmpeg-helper.ts` fetches per-platform prebuilt binaries via `npm pack @ffmpeg-installer/<platform>@<pinned-version>`, untars, and copies `ffmpeg`/`ffmpeg.exe` into the extension `bin/` dir, with a `.kilo-ffmpeg-target` marker (the delta's only change to this file) so cross-platform rebuilds don't silently reuse a wrong-arch binary. Versions are pinned in the spec strings; integrity relies on the npm registry (no separate checksum). Build-time only; runtime falls back to system FFmpeg on win32-arm64. The whole feature (PR #12983) is in the new base, not in this PR's diff.
- **JetBrains build changes (main-side)** make the bun path configurable via `.gradle/kilo-cli-pin.properties` (consumed by `GenerateOpenApiSpecTask` and `buildRepoCli`, defaulting to `bun`), matching the new `.kilo/skills/jetbrains-cli-pin` skill whose clean script only deletes gitignored build artifacts. `gradle.properties` bump 7.0.12 → 7.0.14 is the normal main release flow.
- **Planning docs (`.kilo/plans/`, `plans/`)** — `.kilo/plans/` already held 10+ committed files at base `6fce4e2564`; the delta adds six more, all from main. "Should planning docs ship?" is therefore a main-branch policy question, not something this PR introduces or can fix.
- **Fresh sweep of the full 419-file PR diff:** infra-path hits are limited to the already-assessed set — the two `.github/` files (delta-untouched since round 2), the two opencode changesets, `.opencode-version` (still `v1.18.13`), `.opencode/command/translate.md` (finding), `artifacts/glm52-rise-video/` (finding), root `package.json`/`bun.lock`, `patches/` churn (all covered in rounds 1–2: mistral/mcp-sdk/virtual-core/solid-js patch set; the delta changed no patch files), and `script/{check-model-tool-network,check-test-ci}.ts` + `script/upstream/**` (covered). No Docker, `turbo.json`, `.npmrc`, `bunfig.toml`, `.husky`, publish/release script, issue template, CODEOWNERS, or dependabot changes anywhere in the PR.
- **Annotation guards:** `check-opencode-annotations.ts --worktree` reports "No shared upstream source files changed"; `--base 6fce4e2564` self-skips on merge detection, as in prior rounds. The new shared-file edits observed (session diff params, `index.ts` web omission) carry proper `kilocode_change` markers.

## Limitations

- Network was available this round (unlike rounds 1–2) and used only for read-only verification: LFS batch API query and `git ls-remote`. No GitHub PR/CI state was inspected.
- No builds were executed (no `script/build.ts`, no Gradle build, no nix build); nix hash correctness and the JetBrains bun-pin flow were reviewed statically.
- Delta product-code changes (kilo-jetbrains Kotlin sources, kilo-vscode contribution points, agent-manager/i18n content) were infra-scoped out, except where they intersect build/release tooling.
- The `bun.lock` delta was reviewed at the level of changed keys plus a successful frozen install, not line-by-line.
- The review was performed at detached `b6505b164b` for accurate file state; the worktree was switched back to the PR branch afterward. No files were modified other than this report; nothing was committed.

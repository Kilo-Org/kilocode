---
description: capture durable learnings to AGENTS.md with local recall
---

Review the current work and, when useful, use `kilo_local_recall` to search or read related local sessions from this project family.

Extract only durable, non-obvious learnings and add them to the closest relevant `AGENTS.md` files.

Rules:

- Prefer the narrowest applicable `AGENTS.md`.
- Keep each learning to 1-3 lines.
- Skip session-specific notes, temporary workarounds, and obvious facts from the code.
- Preserve existing style in each `AGENTS.md`.

After updating, summarize which `AGENTS.md` files changed and what kind of learning was captured.

$ARGUMENTS

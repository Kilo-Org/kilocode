# Plan: Fix PR #10302 Review Issues

## Context
PR #10302 changes the "Copy session transcript" and "Export session transcript" handlers in `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` to fetch all messages from the server instead of the truncated sync store.

The bot review found 4 issues (2 warnings, 2 suggestions) all in the same file.

## Issues to Fix

### 1. Non-null assertion on `allMessages.data!` (WARNING) — Lines 984 and 1035
**Problem:** `allMessages.data!` uses a non-null assertion. If the API returns no data, this throws at runtime.
**Fix:** Replace `allMessages.data!` with `(allMessages.data ?? [])` in both handlers.

### 2. `kilocode_change end` marker placement (SUGGESTION) — Lines 991 and 1043
**Problem:** The `// kilocode_change end` marker sits between arguments of `formatTranscript()`, which is visually misleading.
**Fix:** Move the end marker to just after the `sessionMessages` variable declaration in both handlers.

## Implementation Steps

1. **Copy handler** (around lines 982-991):
   - Change `allMessages.data!` to `(allMessages.data ?? [])`
   - Move `// kilocode_change end` to after line 987 (after `sessionMessages` declaration, before `formatTranscript`)

2. **Export handler** (around lines 1033-1043):
   - Change `allMessages.data!` to `(allMessages.data ?? [])`
   - Move `// kilocode_change end` to after line 1038 (after `sessionMessages` declaration, before the blank line and `formatTranscript`)

## Files Changed
- `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` — 4 small edits in 2 locations

# Broken Pipeline Chains — v3 Verification

**Verdict: no broken end-to-end chains found across the full round-3 range `cbbbd7217f..3003a302bc` (22 files in two fix commits). Both v2 failure chains (F1 `isKimiFamily` crash, sdk-next macOS timeout) remain fixed and verified. The root tooling test scheduling chain and the new spinner vendoring, package.json strip-transform, and i18n marker-gating chains are all closed end-to-end, locally and in CI.**

Round-3 reviewed head: `3003a302bc65a4ce0df7c544303c0898db5406e3`. The review worktree HEAD `6f676b6dbb` sits two docs commits on top of it; `git diff 3003a302bc HEAD --name-only` shows only the 14 v1/v2 report `.md` files, so the source tree is identical to the reviewed head.

## Part 1 — delta `cbbbd7217f..af6d1ded6d` (8 files, 34+/10-)

### Scope and method

Reviewed the 8-file delta at head `af6d1ded6d0c42f31b2cea2b84e478f6ac10445a` (part-1 head of round 3). Method: full dereference trace of the Kilo block in `transform.ts`, production-schema vs test-mock guarantee check, targeted suite runs, the exact new CI command run locally, guard failure-path proof via an isolated git fixture in the approved scratch dir (no source edits), timeout survey across all 21 `test:ci` scripts, and log-level inspection of the fresh CI run.

### v2-related chain status

#### F1: `isKimiFamily` null-guard — FIXED (verified)

- Both dereferences in the Kilo block are guarded at `transform.ts:1314-1323`: `id?.toLowerCase() ?? ""` for `[model.providerID, model.api.id]`, and `model.api.url?.toLowerCase() ?? ""` for the host fallback. These are the only derefs in the block. The block's remaining assumption — `model.api` being an object — is already required by upstream's own first line of `anthropicEffort` (`model.api.id.includes(...)`, opus-4-5 check, unmarked upstream code), so the Kilo block adds no assumption upstream didn't already make.
- Production guarantees vs mock reality: `Provider.Model.providerID` is required (`provider.ts:1071`, `ProviderV2.ID`); `ProviderApiInfo` (`provider.ts:995-999`) requires `id`/`url`/`npm` as `Schema.String`. The sole production caller of the chain is `provider.ts:1295` (`reasoningVariants(model, base)`) with schema-decoded models, so production never hits the guard. Only `as any` mocks do — and upstream's own mock helper (`transform.test.ts:3130-3131`, `({ id, api: { id, npm, url: "" }, ... }) as any`, no `providerID`) is unchanged, so the shared suite itself now exercises the guarded path instead of crashing on it.
- `bun test ./test/provider/transform.test.ts` -> **385 pass, 0 fail** (v2 head: 382 pass, 3 fail). The three v2 crashers (`converts effort for @ai-sdk/anthropic`, `converts effort for @ai-sdk/google-vertex/anthropic`, `leaves legacy Anthropic effort options to budget fallback`) pass.
- The new test `handles partial metadata from generic Anthropic providers` enters through the real public export `ProviderTransform.reasoningVariants` (`transform.ts:1165`) -> `effortVariants` (:1183) -> `reasoningEffort` -> `@ai-sdk/anthropic` case -> `anthropicEffort` (:1297) -> `isKimiFamily`, with `providerID: undefined` and `api.url: undefined` — the exact mock shape that crashed v2 (same entry point and call path as the three v2 CI failures). It asserts fall-through to upstream's generic adaptive path: `{ thinking: { type: "adaptive" }, effort: "high" }` ("claude-sonnet-4-6" matches `anthropicAdaptiveEfforts`'s `sonnet-4-6` arm; `anthropicOmitsThinking` false). Not a shallow helper call — `isKimiFamily` is not exported.
- No semantic change for populated models: on defined strings `id?.toLowerCase() ?? ""` is identity, and `"".includes("kimi"|"moonshot"|host)` is always false, so the `?? ""` fallback can never manufacture a Kimi match. The two v2 tests still pass — ID-based (`moonshotai`/`kimi-k3` -> adaptive+summarized for low/high/max) and host-based (`https://api.moonshot.ai/anthropic`) — confirming detection still fires for both id and host cases. `bun test ./test/kilocode/provider/kimi-adaptive-effort.test.ts` -> **3 pass, 0 fail**.
- CI: `unit (linux, 1/2)` and `unit (windows, 1/4)` — the two legs red at v2 with this crash — are green on run 31055346775.

#### sdk-next timeout — FIXED (verified)

- `packages/sdk-next/package.json` `test:ci` timeout is now `--timeout 30000`, matching the heavyweight-suite convention (codemode, core, effect-drizzle-sqlite, http-recorder, kilo-indexing, kilo-memory, kilo-sandbox, llm, tui all 30000).
- Propagation verified: workflow step "Run non-CLI unit tests" runs `bun turbo test:ci ... --filter='!@kilocode/cli' --filter='!@kilocode/kilo-jetbrains'`; `@opencode-ai/sdk-next` is not filtered and appears in the macOS leg's "Packages in scope" log line. `turbo.json`'s `test:ci` task declares only `outputs` + `passThroughEnv` — turbo cannot and does not override the `--timeout` flag inside the package script. JUnit lands at `packages/sdk-next/.artifacts/unit/junit.xml`, matched by `packages/*/.artifacts/unit/junit.xml` in both the report and upload steps.
- The heavyweight suite is `embedded.test.ts` (boots the real Effect app via `OpenCode.create()` with a tempdir SQLite DB) — exactly the class of first-boot cost that killed the 10s budget on macos-15. Local run of the package's full `test:ci`: **5 pass, 0 fail in 2.92s**; 30s gives ~10x headroom over warm local.
- CI: `unit (macos)` — the leg that died at 1m53s on the v2 head — is green on run 31055346775.
- Survey of all 21 `test:ci` scripts for the same latent mismatch (suites with servers/listeners/boot cost under a <30s budget): only `packages/client` (10s) merits a human-verification flag — its single import-boundaries test spawns two full `bun build` bundles via `Bun.spawn`. No listeners; green in both v2 and v3 CI, so evidence of risk is thin. All other sub-30s suites are clean: `httpapi-codegen` (5s) has zero spawn/serve/fetch in its tests (pure codegen); the default-5s suites (kilo-gateway, kilo-telemetry, kilo-console, session-ui, ui) boot no servers — the only spawn among them is ui's markdown-bidi `bun -e` child, a sub-second render check. `kilo-jetbrains` and `opencode` use custom runners; vitest packages manage their own timeouts.

### Part 1 findings

None at P-level.

### Notable verified chains (guard extension and remaining part-1 delta)

- **Root tooling tests scheduled end-to-end.** Workflow step "Run root tooling unit tests" (`test.yml:149-151`) runs `bun run test:script:ci`, defined in root `package.json:20` as `mkdir -p .artifacts/unit && bun test ./script --timeout 30000 --reporter=junit --reporter-outfile=.artifacts/unit/junit.xml`. Run locally from the repo root at the part-1 head: **55 pass, 0 fail, 139 expects, across exactly 10 files** — precisely the 10 git-tracked `script/**/*.test.ts` files (`script/kilocode/release-notes.test.ts`, `script/upstream/opencode-changesets.test.ts`, 5 under `script/upstream/transforms/`, 3 under `script/upstream/utils/`). The root `bunfig.toml` `[test] root = "./do-not-run-tests-from-root"` blocks bare root `bun test` but not an explicit path argument (empirically bypassed). The root junit file exists, is well-formed (`tests="55"`), and matches the newly added `.artifacts/unit/junit.xml` line in both `report_paths` (mikepenz/action-junit-report) and the artifact upload `path`. (At the final round-3 head this run is 58 pass across the same 10 files — see part 2.)
- **No checker/runner drift.** Guard enumerates `git ls-files packages script` filtered by `/^script\/.*\.test\.tsx?$/` (10 files); runner executed 10 files. No `*.spec.*`/`_test` files exist under `script/` that only one side would see; `script/check-test-ci.ts` itself is not a bun test-file pattern match. In CI everything is tracked at checkout, so the git-based enumeration cannot miss files the runner sees.
- **Guard fails when scheduling is missing (fixture-proven).** In an isolated scratch git repo with the real script copied in and one `script/foo.test.ts` tracked: (A) no `test:script:ci` -> exit 1, `Test suites missing CI scheduling:\npackage.json#test:script:ci`; (B) `test:script:ci` containing `bun test ./script` -> exit 0, `1 root script test file(s)`; (C) `test:script:ci` present but running `vitest run` -> exit 1. The guard catches both absent scheduling and a decoy script that wouldn't actually run the tests. At the real head it prints `check-test-ci: ok (25 test-bearing package(s), 10 root script test file(s))` (exit 0); the `25` arithmetic is exact because exempted `packages/kilo-vscode` does have 327 tracked test files and thus sits in `dirs`.
- **Matrix gating is deliberate, not restrictive.** The new step's `if: matrix.settings.run && matrix.settings.packages && matrix.settings.os == 'linux'` is identical to the pre-existing "Verify package test scheduling" guard step and fires on exactly one of seven legs (linux 1/2, `packages:true`). Root script tests are platform-independent merge/release tooling, so single-leg execution is sufficient and consistent with the guard's placement; the guard runs immediately before the runner on the same leg. The report/upload steps run on every `run` leg with `if: always()`; adding the root junit path is purely additive (the `packages/*/` glob still matches on CLI-only legs via the CLI's own junit, as it did at v2).
- **Consumers loop is closed.** `test:script:ci` is referenced by exactly: the workflow (runner), `check-test-ci.ts` (guard), and `transform-package-json.ts` PRESERVE_SCRIPTS + its test (merge survival). No turbo task named `test:script:ci` exists, so the root junit cannot be turbo-cache-revived on legs that never ran it.
- **Preserve list is complete.** Kilo-only root scripts at head vs pristine upstream `32696c425f`: `test:script:ci`, `extension`, `extension:isolated`, `extension:isolated:clean`, `dev-setup`, `dev:local` — all six are in `PRESERVE_SCRIPTS["package.json"]`. The updated `transform-package-json.test.ts` (asserts `test:script:ci` survives `fixScripts`) passed within the root run.
- **CI confirms the runner step.** linux 1/2 log on run 31055346775: guard prints the ok line at 23:10:40Z, immediately followed by `bun run test:script:ci` -> `Ran 55 tests across 10 files` — matching local exactly.

### Part 1 commands and results

- Provenance (part 1, recorded at the pre-rebase checkout): `git rev-parse HEAD` -> `6d6c4eb730` (that checkout's tree was `af6d1ded6d` + report docs; after the rebase the equivalent local checkout is `6f676b6dbb`, whose tree is the round-3 reviewed head `3003a302bc` + the same report docs); `git diff --name-only af6d1ded6d HEAD` -> 14 report `.md` files only; `git diff --name-status cbbbd7217f af6d1ded6d` -> the 8 advertised files, `34 insertions(+), 10 deletions(-)`.
- Chain 1: `bun test ./test/provider/transform.test.ts` (from `packages/opencode/`) -> 385 pass, 0 fail, 692 expects. `bun test ./test/kilocode/provider/kimi-adaptive-effort.test.ts` -> 3 pass, 0 fail, 3 expects.
- Chain 2: `bun run test:ci` (from `packages/sdk-next/`) -> 5 pass, 0 fail, 19 expects, 2 files, 2.92s. Timeout survey via node over all `packages/*/package.json` -> table per above (sdk-next now 30000).
- Chain 3: `bun run test:script:ci` (from root) -> 55 pass, 0 fail, 10 files, 1.90s; `.artifacts/unit/junit.xml` created with `tests="55"`. `bun run script/check-test-ci.ts` -> `ok (25 test-bearing package(s), 10 root script test file(s))`. Fixture (scratch dir, git-init, copied script): cases A/B/C -> exit 1 / exit 0 / exit 1 as described. `git ls-files script | grep -E '\.test\.tsx?$'` -> 10 files.
- Chain 4: `git grep -rn "test:script:ci" af6d1ded6d` -> 6 hits (workflow, root package.json, guard, transform, transform test, and no others). Kilo-only root script keys diffed against upstream `32696c425f` -> 6 keys, all preserved.
- CI: `gh run view 31055346775 --json headSha,conclusion,jobs` -> sha `af6d1ded6d0c42f31b2cea2b84e478f6ac10445a`, conclusion `success`; linux 1/2 and macos job logs grepped for the step/guard/sdk-next evidence above.
- Cleanup: local `.artifacts/` (root and sdk-next) and the scratch fixture removed; `git status --porcelain` shows only other agents' V3 reports.

### Part 1 limitations

- The negative control for F1 (reverting the guard to re-crash the new test) was not run because source edits are out of scope for this review; the negative is established by v2's CI run 31053081178 and local repro, where the identical mock shape (no `providerID`) crashed through the identical entry (`reasoningVariants` -> `anthropicEffort` -> `isKimiFamily`).
- The `packages/client` 10s timeout flag is a human-verification item with thin evidence: bundling twice in one test is the closest analogue to sdk-next's boot cost, but the package was green in both v2 and v3 CI, including macOS.
- CI verification of the new step and the sdk-next fix is log-level for the linux 1/2 and macos legs; I did not download every leg's full log.
- No live Kimi/Moonshot endpoint was contacted; Kimi-detection semantics were verified at the unit level only (same as v1/v2).

### Part 1 CI state

Run **31055346775** on the part-1 head `af6d1ded6d0c42f31b2cea2b84e478f6ac10445a`: **success** (12m14s). All 14 jobs green, including every previously failing leg: `unit (linux, 1/2)`, `unit (windows, 1/4)`, and `unit (macos)`. All sibling workflows on this head (typecheck, annotations, source links, forbidden strings, markdown table padding, test-vscode, CodeQL, visual regression) also succeeded. The two v2 CI failure modes are both closed.

## Part 2 — delta `af6d1ded6d..3003a302bc` (14 files, 236+/94-)

### Scope and method

Reviewed the 14-file delta of commit `3003a302bc` ("fix: address pull request review comments") at the round-3 reviewed head. The delta does three things: (1) removes the external `opentui-spinner` dependency (root `package.json`, `packages/opencode/package.json`, `packages/tui/package.json`, `bun.lock`) and replaces it with a vendored `SpinnerRenderable` in `packages/tui/src/component/register-spinner.ts` (+139) plus a local `ColorGenerator` type in `packages/tui/src/ui/spinner.ts`, covered by new test `packages/tui/test/kilocode/spinner-runtime.test.ts`; (2) teaches the merge tooling to strip `opentui-spinner` on future upstream merges (`transform-package-json.ts`: catalog strip list + new `DELETE_UPSTREAM_DEPENDENCIES` consumed by now-exported `transformDependencies`, +2 tests); (3) gates i18n marker injection behind a new `markers` parameter on `transformI18nContent`, with `translate()` in `script/upstream/utils/upstream.ts` passing `isI18nFile(file)` (+3 tests), plus human translation updates to Kilo-only locales `packages/ui/src/i18n/{it,nl}.ts`. Method: repo-wide grep for residual dependency consumers, registration-site trace, API-surface comparison against the original `opentui-spinner@0.0.7` dist (still present in the local stale bun store) and against the installed `@opentui/core@0.4.3` type declarations, transform caller/order trace, idempotency runs of the i18n transform on checked-in locales, `translate()` output comparison against true upstream v1.18.0 (`32696c425f`) for upstream-owned files, targeted + full suite runs, `packages/tui` typecheck, and CI log inspection on the new head.

### Part 2 findings

None at P-level. Two hygiene notes flagged for human verification (no chain break; safe to ignore if deemed intentional):

- **H1 (hygiene): `bunfig.toml` still lists `opentui-spinner` in `minimumReleaseAgeExcludes`** (the only remaining non-tooling reference outside `script/upstream/` strip logic and `script/upgrade-opentui.ts` cleanup code). Harmless: the list only gates install-time release-age checks if the package were ever re-added, and the new `DELETE_UPSTREAM_DEPENDENCIES` strip makes re-addition via merge impossible. Not a broken chain — an exclusion entry for an absent package is inert.
- **H2 (hygiene): `script/upgrade-opentui.ts` retains `opentui-spinner/@opentui/`-specific lockfile cleanup** (`fixKnownLockfileIssues`, `findStaleLockfileEntriesInText`). With the dependency gone from `bun.lock`, the scan matches nothing and the function returns `[]` without writing (verified by construction: it only rewrites when *all* stale entries are `opentui-spinner/@opentui/*` peer lines, and none exist). Dead-ish but defensive code; removal optional.

### Notable verified chains (part 2)

- **SPINNER chain closed — dependency removal, vendored renderable, registration, consumers, types, tests.**
  - Removal is total: `rg "opentui-spinner"` over the tree (excluding `bun.lock`/`node_modules`/`*.md`) finds zero source imports — only the `bunfig.toml` exclusion entry (H1), the intentional strip lists in `transform-package-json.ts` + its test, and `script/upgrade-opentui.ts` cleanup code (H2). `bun.lock` and all three `package.json` files are clean; the lockfile delta also correctly drops the nested `@opentui/core@0.3.4`/`@opentui/solid@0.3.4` peer copies and downgrades `cli-spinners` 3.4.0 -> 2.9.2, whose sole remaining consumer is `ora@8.2.0` (`^2.9.2`). No dangling lockfile reference.
  - Registration: `registerOpencodeSpinner()` is called at module top-level in exactly 5 files — `packages/tui/src/app.tsx:96` (TUI startup), `packages/tui/src/component/spinner.tsx:8`, `packages/tui/src/component/prompt/index.tsx:71`, `packages/opencode/src/cli/cmd/run/footer.view.tsx:60`, `footer.subagent.tsx:11` — so every module that renders `<spinner>` registers on import, before first render. The guard `if (!getComponentCatalogue().spinner) extend({ spinner: SpinnerRenderable })` makes repeat calls idempotent. `packages/tui/package.json` exports `./component/register-spinner`, `./component/spinner`, and `./ui/spinner`; `packages/opencode/tsconfig.json` maps `@tui/* -> ../tui/src/*` for the Kilo plugin/claw consumers.
  - Consumers: all 4 `<spinner>` JSX sites (`spinner.tsx:19`, `prompt/index.tsx:1716`, `footer.view.tsx:865`, `footer.subagent.tsx:131`) pass only `frames`/`interval`/`color` — exactly the vendored `SpinnerOptions`. The original 0.0.7 package's extra options (`name`, `autoplay`, `backgroundColor`) and its `createPulse`/`createWave` exports are unused anywhere in the tree, so dropping them breaks nothing.
  - Type chain: local `ColorGenerator` in `ui/spinner.ts` is signature-identical to the original (`(frame, char, frames, chars) => ColorInput` vs upstream's `(frameIndex, charIndex, totalFrames, totalChars)`); `createColors`/`createFrames` still exported from the same module consumers already import; `register-spinner.ts` imports the type type-only from `../ui/spinner` (no cycle). The `declare module "@opentui/solid" { interface OpenTUIComponents { spinner: typeof SpinnerRenderable } }` augmentation feeds `ExtendedIntrinsicElements<OpenTUIComponents>` -> `IntrinsicElements` in the installed `@opentui/solid` jsx-runtime, deriving JSX props from the constructor's `SpinnerOptions` via `ExtractRenderableOptions` — the same vendoring pattern as the pre-existing `bg-pulse.tsx`.
  - Runtime FFI chain: every call the vendored class makes exists in the installed `@opentui/core@0.4.3` with matching signatures — `resolveRenderLib(): RenderLib` (re-exported via `export * from "./zig.js"`), `RenderLib.encodeUnicode(text, widthMethod)` returning `{ptr, data: Array<{width, char}>} | null` (vendored code null-checks and reduces `data` exactly as typed), `freeUnicode(encoded)`, `OptimizedBuffer.drawChar(char: number, x, y, fg: RGBA, bg: RGBA)`, `parseColor(color: ColorInput): RGBA`, `ctx.widthMethod`. The original 0.0.7 dist made the identical 2-arg `encodeUnicode` call, confirming faithful vendoring rather than drift. (Motivation confirmed: the external package peer-depended `@opentui/core@^0.3.4` and dragged a nested 0.3.4 runtime into `bun.lock`; Kilo's active runtime is 0.4.3.)
  - Tests: `bun test ./test/kilocode/spinner-runtime.test.ts` (from `packages/tui/`) -> **1 pass** (registers, asserts catalogue `spinner` is defined and its prototype extends `Renderable` from the *active* `@opentui/core` — the exact runtime-identity property the vendoring exists to guarantee). Full `packages/tui` suite: **211 pass, 1 skip, 0 fail, 49 files**. `bun run typecheck` (tsgo) in `packages/tui`: clean, proving the vendored class compiles against 0.4.3 including the JSX augmentation and all consumer props. The test file is git-tracked, so the CI guard's `git ls-files` enumeration and the turbo `test:ci` runner both see it.
  - Behavior parity notes (verified non-issues for current consumers): vendored drops the original's interval range validation (both consumer intervals, 80ms and 40ms, are inside the original's 16-1000ms valid range anyway); vendored `set frames([])` is ignored vs original's fallback-to-default (no consumer sets frames dynamically); vendored timer keeps ticking while hidden but skips advancing (original descheduled) — cosmetically different, functionally equivalent for always-visible-while-mounted consumers.
- **TRANSFORM-PACKAGE-JSON chain closed — strip logic matches reality and runs after merge on every path.**
  - Ordering: in `transformPackageJson` (and the parallel `transformConflictedPackageJson` ~:850 and the third path ~:1056), dependencies/devDependencies/peerDependencies are first union-merged from ours+theirs via `mergeWithNewestVersions` (step 3) and only then passed through `transformDependencies` (step 7), which now drops any `opentui-spinner` entry via `DELETE_UPSTREAM_DEPENDENCIES`. An upstream re-introduction therefore cannot survive any of the three transform paths regardless of whether "ours" still has the dep. `KILO_DEPENDENCIES` (step 8, after the strip) injects only `@kilocode/kilo-gateway`/`kilo-telemetry` — it never re-adds the spinner.
  - Catalog: `DELETE_UPSTREAM_CATALOG["package.json"]` now includes `opentui-spinner`, applied by `fixCatalog` (called with `relativePath === "package.json"` on the root file) after the catalog merge — so the root `workspaces.catalog` entry upstream may carry is stripped too. Current tree matches the transform's expectations: grep confirms zero `opentui-spinner` in every `package.json` and in `bun.lock`.
  - Tests: `bun test ./script/upstream/transforms/transform-package-json.test.ts` -> all pass, including the new `transformDependencies removes the incompatible spinner runtime` (asserts `{ "opentui-spinner": "catalog:" }` is stripped with the exact change message) and the extended `fixCatalog removes unsupported upstream entries` (catalog strip, 3 changes). Full root tooling run (`bun test ./script --timeout 30000` from root): **58 pass, 0 fail across 10 files** — up from 55 at the part-1 head, i.e. the guard's file enumeration automatically picked up the new tests with no scheduling change needed. CI linux 1/2 log on run 31094000345 shows `(pass) transformDependencies removes the incompatible spinner runtime` in the root tooling step.
- **TRANSFORM-I18N + upstream.ts chain closed — marker gating is per-file correct, checked-in files are transform-stable, and the pre-existing prompt-text corruption path is fixed.**
  - New behavior: `transformI18nContent(content, verbose, markers = false)` only appends `// kilocode_change` when `markers` is true. `transformI18nFile` (the merge-time path used by `transformAllI18n` in `merge.ts:487` and `transformConflictedI18n` in `merge.ts:645`) passes `true`, preserving the previous marking convention for locale files. `translate()` (`upstream.ts:194`) — the generic per-file translation used by tooling — passes `isI18nFile(file)`, where `isI18nFile` matches `packages/*/src/i18n/*.ts` (`defaultConfig.i18nPatterns`, config.ts:241).
  - The bug this fixes: before the delta, `transformI18nContent` *always* appended `// kilocode_change` to any line it rewrote, so `translate()` on a non-locale file with transformable branding (e.g. `packages/opencode/src/session/prompt/meta.txt`, a plain-text prompt) produced comment-marker garbage. Verified fixed at head: `translate("packages/opencode/src/session/prompt/meta.txt", upstream v1.18.0 text)` is byte-identical to the checked-in file (fully Kilo-branded, zero `kilocode_change` markers in it) — the `identical` drift bucket now actually reachable for this file. New tests lock both directions: non-locale content gets no markers, and the prompt-file case is marker-free through the real `translate()`.
  - `translate()` callers (all three traced): `reset.ts:61` (`resetFile` — writes `translate()` output when resetting to upstream; per-file marker gating now correct for both locale and non-locale files), `reset.ts:117` (`classifyDrift` — same comparison semantics), `fix-kilocode-markers.ts:72` (uses `translate()` output as diff base but passes it through `clean()` before diffing, so markers in the base are stripped regardless — unaffected by the parameter).
  - Checked-in locale files are stable under the merge-time transform: running `transformI18nContent(content, false, true)` on the current `it.ts`, `nl.ts`, `de.ts`, `fr.ts`, `en.ts` is a **no-op** for all five (0 replacements; 6/10/7/7/7 marked lines preserved) — every line the transform would rewrite is already `// kilocode_change`-marked and therefore preserved, so the next merge's `transformAllI18n` will not rewrite them again. `isI18nFile` returns true for all of them.
  - `it.ts`/`nl.ts` provenance: both are **absent in true upstream v1.18.0** (`32696c425f` ships ar/br/bs/da/de/en/es/fr/ja/ko/... but not it/nl) — they are Kilo-only files, so the `// kilocode_change - new file` header (line 1 of both) is accurate, no upstream conflict path can touch them, and the 40-line-per-file translation updates in this commit (sessionReviewV2 keys, `lineComment.cancel`, `common.showMore`, `diffs.changed.one/other`) are pure content. Sampled translated keys all exist in `en.ts` (en=it=nl=1 for each of 5 spot-checked keys).
  - Consumption of the translated dicts: `packages/ui/src/i18n/{it,nl}.ts` (package `@opencode-ai/ui`, export map `./i18n/* -> ./src/i18n/*.ts`) -> re-exported by `packages/kilo-ui/src/i18n/{it,nl}.ts` and `packages/kilo-web-ui/src/i18n/{it,nl}.ts` (`export * from "@opencode-ai/ui/i18n/..."`) -> imported as `@kilocode/kilo-ui/i18n/{it,nl}` in `packages/kilo-vscode/webview-ui/src/context/language.tsx` (`uiIt`/`uiNl`). Every link present; nothing populated-but-unread.
  - Marker-source asymmetry (verified pre-existing, not introduced by this delta): for upstream-owned locales like `de.ts`, `translate()` produces *no* marker on the transformed branding line because `applyBrandingTransforms` rewrites "OpenCode Go" -> "Kilo Go" before the i18n stage sees it (i18n replacements=0), whereas merge-time `transformI18nFile` marks it via its own replacement list — and the checked-in `de.ts:81` does carry the marker. `classifyDrift` handles this correctly by design (`clean()` strips markers; extra Kilo-only keys classify the file as small-diff, the normal fork state). This delta neither worsens nor claims to fix that asymmetry; the new `isI18nFile` gate is about *non*-locale files and is verified above.

### Part 2 commands and results

- Provenance: `git log --oneline` -> HEAD `6f676b6dbb` = `3003a302bc` + 2 docs commits; `git diff --name-only 3003a302bc HEAD` -> 14 report `.md` files only; `git show 3003a302bc --stat` -> the 14 advertised files, `236 insertions(+), 94 deletions(-)`.
- Chain SPINNER: `rg "opentui-spinner"` (tree-wide, excl. lock/node_modules/md) -> no source imports. `rg "registerOpencodeSpinner|register-spinner"` -> 5 module-level call sites + exports map + test. `rg "<spinner"` -> 4 JSX sites, props frames/interval/color only. Installed-type greps: `@opentui/solid@0.4.3` `IntrinsicElements extends ExtendedIntrinsicElements<OpenTUIComponents>`; `@opentui/core@0.4.3` `zig.d.ts` `resolveRenderLib(): RenderLib`, `encodeUnicode(text, widthMethod)` 2-arg returning `{ptr,data}|null`, `freeUnicode`, `drawChar`, `parseColor`. Original `opentui-spinner@0.0.7` dist (stale `node_modules/.bun` copy) compared: identical `ColorGenerator` signature, identical 2-arg `encodeUnicode` call, `registerSpinner` = `extend({spinner})` auto-run. `bun test ./test/kilocode/spinner-runtime.test.ts` (packages/tui) -> 1 pass. `bun test --timeout 30000` (packages/tui) -> 211 pass, 1 skip, 0 fail, 49 files. `bun run typecheck` (packages/tui, tsgo) -> clean.
- Chain TRANSFORM-PACKAGE-JSON: `rg "transformDependencies"` -> 3 call sites in the transform (+test), all after the merge steps; `KILO_DEPENDENCIES` inspected (gateway/telemetry only). `bun test ./script/upstream/transforms/transform-package-json.test.ts ./script/upstream/transforms/transform-i18n.test.ts` (root) -> 25 pass, 0 fail. `bun test ./script --timeout 30000` (root) -> 58 pass, 0 fail, 10 files (CI-runner equivalent; new tests auto-enumerated). `rg "opentui-spinner" bun.lock package.json packages/*/package.json` -> clean; `cli-spinners` 2.9.2 retained only for `ora@8.2.0`.
- Chain TRANSFORM-I18N/upstream.ts: `rg "translate\("` -> 3 callers (reset.ts x2, fix-kilocode-markers.ts x1), semantics verified per caller (above). `rg "transformI18nContent"` -> only `transformI18nFile` (markers=true), `translate` (markers=isI18nFile), tests. `defaultConfig.i18nPatterns` = `["packages/*/src/i18n/*.ts"]`. Idempotency `bun -e` run: transform(markers=true) is a no-op on checked-in it/nl/de/fr/en. `translate()` vs `git show 32696c425f:<file>`: `meta.txt` byte-identical to local with zero markers; de/fr/en differ only by intended Kilo deltas (markers + Kilo-only keys), with the branding line verified to be rewritten by the earlier branding stage. `git cat-file -e 32696c425f:packages/ui/src/i18n/{it,nl}.ts` -> absent (Kilo-only). Key spot-check: 5 sampled translated keys present in en/it/nl. Consumption greps: `export * from "@opencode-ai/ui/i18n/{it,nl}"` in kilo-ui and kilo-web-ui; `@kilocode/kilo-ui/i18n/{it,nl}` imported in `webview-ui/src/context/language.tsx`; `packages/ui/package.json` exports `./i18n/*`.
- CI: `gh run list -R Kilo-Org/kilocode --branch johnnyeric/kilo-opencode-v1.18.0` -> run **31094000345** (`test`) on `3003a302bc65a4ce0df7c544303c0898db5406e3`, conclusion `success`; all 14 jobs green (`unit` linux 1/2+2/2, windows 1-4/4, macos, `test (linux)`, `unit (linux)`, jetbrains x3, HttpApi exerciser, detect). Sibling workflows on the same head all `success`: typecheck (31094000157), Check shared upstream annotations (31093999938), Check Source Links (31093999941), Check forbidden strings (31093999952), Check markdown table padding (31094000002), test-vscode (31094000283), CodeQL (31094000166), Visual Regression (31093999921). linux 1/2 log: `@opencode-ai/tui` in "Packages in scope"; root tooling step shows `(pass) transformDependencies removes the incompatible spinner runtime`.
- Cleanup: temp CI log file removed; `git status --porcelain` shows only V3 report `.md` files.

### Part 2 limitations

- CI evidence for the tui spinner test is leg-level only: the non-CLI unit step runs turbo with `--output-logs=errors-only`, so successful per-test lines (including `spinner-runtime.test.ts`) are suppressed in the log; the green leg plus the local 211-test run are the evidence. The root tooling step logs tests individually, which is why the transform test is CI-visible but the spinner test is not.
- The vendored `SpinnerRenderable`'s frame animation (`setInterval` + `renderSelf` drawing) is verified by type/FFI-signature/API comparison and by the registration test, but no visual/runtime TUI session was driven in this review; a stuck-frame or invisible-spinner defect would not be caught by these checks. Judged low-risk given the code is a near-verbatim vendoring of a package that worked in production, with consumer props unchanged.
- `packages/opencode` full typecheck was not run (nothing in its `src` changed in this delta; the `package.json` dep removal is proven safe by the zero-importer grep). `packages/tui` typecheck, which covers the vendored code and all JSX consumers via the shared augmentation, is clean.
- Idempotency was checked on 5 of the locale files (it, nl, de, fr, en), not all shipped locales; the five include both Kilo-only and upstream-owned files and cover every line shape present in the delta.
- The `node_modules/.bun` store in the review worktree still contains `opentui-spinner@0.0.7` (stale local install predating the lockfile change). This is local-only state, not a repo defect — `bun.lock` at head has no reference, so a fresh install drops it. The stale copy was used deliberately as the comparison baseline for the vendoring check.

## Final CI state

- Part-1 head `af6d1ded6d0c42f31b2cea2b84e478f6ac10445a`: run **31055346775** — success, all 14 jobs + all sibling workflows green (details in part 1).
- Round-3 reviewed head `3003a302bc65a4ce0df7c544303c0898db5406e3`: run **31094000345** (`test`) — success, all 14 jobs green; sibling workflows (typecheck, annotations, source links, forbidden strings, markdown table padding, test-vscode, CodeQL, visual regression) all success. The two v2 CI failure modes remain closed, and the round-3 deltas introduce no new broken chain.

Only `BROKEN_PIPELINE_CHAINS_V3.md` was authored; no source files, v1/v2 reports, or other agents' V3 files were modified.

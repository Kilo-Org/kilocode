---
phase: 3
plan: 02
wave: 2
depends_on: [03-01]
refine_cycle: 1
title: "devil-ui RenderTarget + Adapters + Hooks"
primary_agents: [frontend-developer, senior-developer]
reviewer: backend-architect
must_haves:
  - "Root catalog gains @opentui/solid + @opentui/core + @devilcode/kilo-ui entries (currently missing — blocks singleton mitigation)"
  - "opencode package.json flipped to catalog: for @opentui/solid + @opentui/core; gains @devilcode/kilo-ui: workspace:* + @devilcode/keybind: workspace:* (latter was in plan, former was missing)"
  - "devil-ui ships a minimal Bun test runner setup (per-package bunfig.toml + no vitest) since devil-ui currently has NO test runner configured"
  - "RenderTarget context + provider + useRenderTarget hook in devil-ui"
  - "Terminal adapter using @opentui/solid (peer-dep via catalog) + DOM adapter using native elements"
  - "use-command-registry hook subscribes to CommandRegistry.subscribe() (NEW in 03-01 refine) to track entries reactively"
  - "use-prompt-history hook with pluggable persistence + Up/Down navigation"
  - "devil-ui hooks/index.ts + context/index.ts MUST preserve existing upstream re-exports (@opencode-ai/ui) and add new exports explicitly, not via export *"
  - "devil-ui package.json exports map updated (context + adapters + hooks + primitives stubs)"
  - "bun pm ls @opentui/solid reports a single resolved version (0.1.87) via catalog dedup"
  - "bun turbo typecheck green monorepo-wide"
deliverables: [D11, D12, D13, D14, D15, D16, D27, D28, D29, D31]
new_in_refine_cycle1: [catalog entries for opentui, @devilcode/kilo-ui add to opencode, devil-ui bunfig + test setup, explicit named re-exports]
estimated_loc: 555
---

<objective>
Add the RenderTarget abstraction, terminal + DOM adapters, and the two primitive-backing hooks to `packages/devil-ui/`. Wire the `@devilcode/keybind` workspace dep into both devil-ui and opencode. Leave primitives (Plan 03-03) as empty stubs — this plan ships everything they consume, nothing they ARE.
</objective>

<context>
Reference: `.planning/specs/03-tui-scaffolding-spec.md` §3 (D11–D16, D27–D29, D31), §4 (RenderTarget.Context schema), §5 (package structure), §6 (adapter contract), §9 (deps).

Prior plan (03-01 — complete per this plan's dependency chain):
- `@devilcode/keybind` exports `CommandScope`, `Command`, `Keybind`, `createCommandRegistry`, `createKeybindRegistry`, `searchCommands`, `createLeaderChain`.
- devil-keybind has zero opencode / devil-ui / SolidJS / OpenTUI deps.
- ≥ 90% test coverage on registry / matcher / leader.

Existing assets in `packages/devil-ui/`:
- Already a SolidJS component library (see `.planning/CODEBASE.md`).
- Built on `@kobalte/core` primitives (40+ components).
- Storybook harness at `.storybook/`.
- Existing exports map in package.json — read before modifying to understand current pattern.
- `context/` directory may or may not exist — verify. If present, follow its pattern for `render-target.tsx`.
- `hooks/` directory may or may not exist — verify and adapt.

OpenTUI versions in opencode (verified via Phase 2 typecheck): `@opentui/solid@0.1.87` + `@opentui/core@0.1.87`. Plan 03-02 pins these as peer deps in devil-ui at the same exact version. If a workspace catalog exists, pin via catalog; otherwise exact-version in devil-ui's peerDependencies.

Codebase conventions:
- Fork hygiene: devil-ui is entirely Kilo — no `devilcode_change` markers.
- Format: Prettier 120-char, no semicolons. Run `bun run format` from devil-vscode before commits.
- CI gates: `bun turbo typecheck`, `bun run knip` (devil-vscode), `bun run format:check`, `bun run check-kilocode-change`.
- Windows-safe spawning: not applicable here.

Risks (spec §12):
- **Renderer-singleton collision**: terminal adapter must NOT import `@opentui/solid` from a path that could resolve to a different installed copy than opencode's. Peer-dep + catalog-pin is the mitigation. Task 1 ends with a `bun pm ls @opentui/solid` verification step.
- Duplicate keybind parser: devil-ui's adapters do NOT parse keybinds. All parsing lives in devil-keybind. Plan 03-02 does not introduce new duplication.
</context>

<tasks>

<task id="1" title="Catalog-pin opentui + RenderTarget context + terminal adapter (refined cycle 1)">
<action>
**REFINEMENT NOTE — cycle 1 critique surfaced**:
- Root catalog has `fuzzysort: 3.1.0`, `zod: 4.1.8`, but NO `@opentui/*` entries. opencode lists `@opentui/solid` + `@opentui/core` at 0.1.87 as REGULAR deps (not catalog). The singleton mitigation (`bun pm ls @opentui/solid` returning one version) requires catalog dedup — must ADD catalog entries first.
- opencode does NOT currently list `@devilcode/kilo-ui` — Plan 03-03 imports from it; will fail resolve without add.
- devil-ui `context/index.ts` is `export * from "@opencode-ai/ui/context"` and `hooks/index.ts` re-exports upstream too; new primitives must be added via EXPLICIT named re-exports, not additional `export *` (conflict risk).

**STEP 1 — Catalog + opencode dep refactor + multi-version dedup (prerequisite)**:

**Cycle-2 refine critical findings applied**:
- `@devilcode/kilo-ui: "workspace:*"` inside `workspaces.catalog` is MALFORMED — catalog is a version-alias registry, `workspace:*` is a dep-level protocol. Catalog only hosts registry packages.
- `node_modules/.bun` already contains TWO `@opentui/core` versions (0.1.75 + 0.1.87) via transitive deps. Catalog alone does NOT dedupe transitives — a root `overrides` entry is required.

1a. Edit root `package.json` `workspaces.catalog` (add ONLY registry packages; preserve existing ordering):
    - `"@opentui/solid": "0.1.87"`
    - `"@opentui/core": "0.1.87"`
    - DO NOT add `@devilcode/kilo-ui` to the catalog — it is a workspace package; consumers write `"@devilcode/kilo-ui": "workspace:*"` directly in their package.json.

1b. Edit `packages/opencode/package.json`:
    - Flip `"@opentui/solid": "0.1.87"` → `"@opentui/solid": "catalog:"`
    - Flip `"@opentui/core": "0.1.87"` → `"@opentui/core": "catalog:"`
    - ADD `"@devilcode/kilo-ui": "workspace:*"` to `dependencies` (alphabetical w/ existing `@devilcode/*` entries).

1c. Identify transitive OpenTUI consumers BEFORE install: `bun pm why @opentui/core` + `bun pm why @opentui/solid`. If any result shows a version other than 0.1.87, ADD a root `package.json` `overrides` entry (merge with existing overrides block):
    ```json
    "overrides": {
      "@opentui/core": "0.1.87",
      "@opentui/solid": "0.1.87",
      ...existing entries
    }
    ```
    Then proceed. If no transitive mismatch found, skip the override add.

1d. Run `bun install` from repo root.

1e. Verify: `bun pm ls @opentui/solid` AND `bun pm ls @opentui/core` report EXACTLY one resolved version each (`0.1.87`). Also verify `find node_modules -path "*@opentui*" -name package.json` returns only 0.1.87 tree entries. If any 0.1.75 remnant appears, delete `node_modules` + `bun.lock`, repeat from step 1c. Singleton is a hard gate — do not proceed past Task 1 without this green.

2. Read `packages/devil-ui/package.json` + existing context / hooks / adapters directories to understand current export patterns before writing new code. Note specifically:
   - `src/context/index.ts` currently does `export * from "@opencode-ai/ui/context"` (upstream re-export).
   - `src/hooks/index.ts` currently does `export { useFilteredList } from "@opencode-ai/ui/hooks"` + `export * from "./create-auto-scroll"` (mixed upstream + local).
   - NEW exports must be added as additional EXPLICIT `export { Name } from "./local"` lines; DO NOT add `export *` blocks that might shadow upstream symbols.

3. `packages/devil-ui/src/context/render-target.tsx` (D11, ~110 LOC, cycle-2 refined for declarative focus):
   - Import from `solid-js`: `createContext`, `useContext`, `createSignal`, `Show`, `JSX`, `Accessor`.
   - Export types:
     - `RenderTargetKind = "terminal" | "dom"`
     - `RenderTargetAdapter` interface per spec §4 (cycle-2): `kind`, `measure(text)`, `focusedNodeId: Accessor<string | null>`, `setFocusedNodeId(id)`, `supportsRichHighlight`. NO imperative `focus(nodeId)` method — see spec §6 critical contract note.
   - `RenderTargetContext` — SolidJS context, default `null`.
   - `RenderTargetProvider(props: { adapter: RenderTargetAdapter, children })` — wraps context.
   - `useRenderTarget(): RenderTargetAdapter` — throws outside provider.
   - Helper factory `createFocusSignal(): { focusedNodeId, setFocusedNodeId }` — adapters call this in their factories so both share the signal shape.
   - Export `RenderSurface` helper component (renamed from `Surface` per cycle-2 critique CRITICAL-4 — prevents collision with any identically-named symbol upstream-re-exported via `export * from "@opencode-ai/ui/context"`). Props: `{ kind: RenderTargetKind, children, ...forwardProps }`. DOM branch renders `<div>` directly. Terminal branch does NOT render OpenTUI elements in this file — instead it returns `children` and expects the consumer's terminal branch to wrap in `<box>` itself; this keeps `@opentui/*` imports out of `render-target.tsx` entirely (applies CRITICAL-5 fix). The adapter's responsibility is the signals + measurement; visual wrapping lives in primitive files + workflow-tui.

4. `packages/devil-ui/src/adapters/terminal.ts` (D12, ~110 LOC, cycle-2 refined — dynamic-import guard + declarative focus):
   - **NO top-of-file static import of `@opentui/solid` or `@opentui/core`** (CRITICAL-5 fix). Cycle-2 critique: if any DOM bundler transitively pulls this file, a static import pollutes the browser bundle. Solution: async factory with dynamic-import inside the body.
   - Export `async function createTerminalAdapter(): Promise<RenderTargetAdapter>`:
     - Dynamic-import `@opentui/core` inside the body: `const core = await import("@opentui/core")`. Vite + ESBuild tree-shake dynamic-imports out of DOM bundles that never call the factory.
     - Build `{ focusedNodeId, setFocusedNodeId }` via the `createFocusSignal()` helper.
     - `kind: "terminal"`.
     - `measure(text)`: use `core.measureText(text)` (confirm exact export name when implementing; fallback to `{ width: text.length, height: text.split("\n").length }` if `@opentui/core` API differs).
     - `focusedNodeId` + `setFocusedNodeId` — from the focus signal. Primitives call `setFocusedNodeId("palette-input")` on mount; JSX `<input focused={focusedNodeId() === "palette-input"} />` responds reactively. This replaces the unimplementable imperative `focus(nodeId)` (CRITICAL-1 fix).
     - `supportsRichHighlight: false` — terminal uses plain-text inverse-color highlighting via fuzzysort's marker pattern.
   - **Peer-dep + dynamic-import boundary**: this file is the ONLY devil-ui location that references `@opentui/*`, and it references them ONLY inside a dynamic `import()` expression. DOM consumers calling nothing from here remain bundler-safe.
   - Plan 03-03 Task 3 consumer pattern updated: workflow-tui awaits the factory at module top level via top-level `await` (Bun + Node 22 support it under ESM). The resulting adapter is a module-scoped constant.

5. Update `packages/devil-ui/package.json` (partial D27, for this task):
   - Add to `peerDependencies`: `"@opentui/solid": "catalog:"`, `"@opentui/core": "catalog:"` (resolves to 0.1.87 via Step 1a catalog entry).
   - Add to `peerDependenciesMeta`: both marked as `{ optional: true }` — DOM-only consumers (Phase 9 webview) should not be forced to install.
   - Extend the `exports` map: add `"./context/render-target"` → `"./src/context/render-target.tsx"` and `"./adapters/terminal"` → `"./src/adapters/terminal.ts"`.

6. Run `bun install` again (after Step 1c + 1d + peer-dep adds).

7. **Singleton verification** (re-run, definitive): `bun pm ls @opentui/solid` AND `bun pm ls @opentui/core` MUST report single version `0.1.87`. Combined with Step 1c overrides, this closes the singleton mitigation (spec §12 Risk 1).

8. **Symbol-collision grep (CRITICAL-4 fix)** — before committing, run:
   ```bash
   grep -rn "export.*RenderSurface\|export.*RenderTargetProvider\|export.*useRenderTarget" node_modules/@opencode-ai/ui/dist 2>/dev/null || true
   ```
   If any match appears, upstream ships a colliding symbol — rename the devil-ui export to avoid shadow. Expected: zero matches.

**CODEBASE GUIDANCE**:
- Mirror existing context patterns if `packages/devil-ui/src/context/` has siblings.
- No `devilcode_change` markers.
- Test by importing from `@devilcode/kilo-ui/context/render-target` in a throwaway script from repo root.

**RISK AREA** — The renderer-singleton collision risk is the highest in this plan. If the pm ls check fails, do not proceed to Task 2 or 3 until resolved.
</action>
<verify>
- `ls packages/devil-ui/src/context/render-target.tsx` exists.
- `ls packages/devil-ui/src/adapters/terminal.ts` exists.
- `bun install` completes without errors.
- `bun pm ls @opentui/solid` shows exactly one version (0.1.87).
- `bun turbo typecheck` → PASS.
- Import smoke test: `bun -e 'import { createTerminalAdapter } from "@devilcode/kilo-ui/adapters/terminal"; const a = createTerminalAdapter(); console.log(a.kind, a.supportsRichHighlight)'` prints `terminal false`.
</verify>
<done>
RenderTarget context, provider, hook, Surface helper, and terminal adapter land in devil-ui. `@opentui/*` pinned as optional peer deps. Single resolved version verified. Plan 03-03 primitives can import the terminal adapter when needed.
</done>
</task>

<task id="2" title="DOM adapter + adapters barrel + finish exports">
<action>
Add the DOM adapter (Phase 9 target) and finalize the devil-ui exports map.

1. `packages/devil-ui/src/adapters/dom.ts` (D13, ~110 LOC):
   - Zero imports from `@opentui/*`. This is the browser-target adapter.
   - Export `createDomAdapter(): RenderTargetAdapter`:
     - `kind: "dom"`
     - `measure(text)`: use a singleton hidden `<canvas>` + `CanvasRenderingContext2D.measureText` for width; estimate height via line-height metric. Cache the context instance. Gracefully handle the non-browser case (return `{ width: text.length * 8, height: 16 }` fallback) so Storybook under Node doesn't crash.
     - `focus(nodeId)`: `document.getElementById(nodeId)?.focus()`. No-op if document is undefined.
     - `supportsRichHighlight: true` (DOM supports `<b>` / `<mark>` highlighting from fuzzysort).

2. `packages/devil-ui/src/adapters/index.ts` (D14, ~10 LOC) — barrel:
   - Re-export `createTerminalAdapter` from `./terminal` AND `createDomAdapter` from `./dom`.
   - Do NOT re-export `RenderTargetAdapter` here — that type lives in `context/render-target.tsx`. Consumers import types from the context module, constructors from here.
   - NOTE: re-exporting `createTerminalAdapter` from the barrel WILL trigger the `@opentui/*` peer import in DOM consumers that import the barrel eagerly. To preserve DOM-only safety, split the barrel: `./adapters/index.ts` re-exports only DOM-safe constructors; terminal adapter is imported from the deep path `@devilcode/kilo-ui/adapters/terminal` explicitly. Document this rule with a top-of-file JSDoc comment.

3. Finish `packages/devil-ui/package.json` (D27):
   - `exports` additions:
     - `"./adapters"` → `"./src/adapters/index.ts"` (DOM-safe)
     - `"./adapters/terminal"` → `"./src/adapters/terminal.ts"` (terminal consumers only)
     - `"./adapters/dom"` → `"./src/adapters/dom.ts"` (DOM consumers only)
     - `"./hooks/use-command-registry"` → `"./src/hooks/use-command-registry.ts"` (stub file created in Task 3)
     - `"./hooks/use-prompt-history"` → `"./src/hooks/use-prompt-history.ts"` (stub file created in Task 3)
     - `"./primitives"` → `"./src/primitives/index.ts"` (stub, Plan 03-03 populates)
   - `dependencies` additions: `"@devilcode/keybind": "workspace:*"`, `"fuzzysort": "catalog:"` (NOT hard-pinned — catalog dedup).
   - Read existing `exports` map first; preserve ordering convention.

4. `packages/devil-ui/src/context/index.ts` (D29, refined cycle-2):
   - The existing file is `export * from "@opencode-ai/ui/context"`.
   - APPEND two new explicit named-export lines BELOW the existing star-re-export, using `RenderSurface` (renamed from `Surface` per cycle-2 CRITICAL-4):
     ```ts
     export { RenderTargetProvider, useRenderTarget, RenderSurface } from "./render-target"
     export type { RenderTargetKind, RenderTargetAdapter } from "./render-target"
     ```
   - DO NOT remove or reorder the existing upstream re-export. Named exports take precedence over star re-exports; `RenderSurface` + `RenderTargetProvider` + `useRenderTarget` + type names are all specific enough that collision is vanishingly unlikely, but run the Task 1 Step 8 grep to confirm.

5. `packages/devil-ui/src/primitives/index.ts` — create as an empty barrel (`export {}`), to reserve the path for Plan 03-03.

6. `packages/opencode/package.json` — verify `@devilcode/kilo-ui: workspace:*` + `@devilcode/keybind: workspace:*` are present (both added in Task 1 Step 1b + this step). If either missing, add now.

7. Run `bun install` again. Then `bun turbo typecheck`.

**CODEBASE GUIDANCE**:
- `bun run knip` is enforced in devil-vscode, NOT devil-ui — but dead exports still cause maintenance pain. Only export what Plan 03-03 will consume; if knip later flags something, trim.
- Check `packages/devil-vscode/` for any unintended imports from devil-ui introduced by the export-map expansion (shouldn't be, but verify).

**RISK AREA** — the `adapters/index.ts` barrel-vs-deep-path split is subtle. If Plan 03-03 accidentally imports `@devilcode/kilo-ui/adapters` from a DOM-only context and the barrel re-exports terminal, DOM consumers break at load time. Document the rule + enforce with a knip-style rule or a README note in `packages/devil-ui/src/adapters/README.md` (create a 10-line note here).
</action>
<verify>
- `ls packages/devil-ui/src/adapters/` shows `terminal.ts`, `dom.ts`, `index.ts`, `README.md`.
- `ls packages/devil-ui/src/primitives/index.ts` exists (empty barrel).
- `ls packages/devil-ui/src/context/index.ts` re-exports RenderTarget pieces.
- `bun install` clean.
- `bun turbo typecheck` → PASS.
- `cd packages/devil-vscode && bun run knip` → no new warnings tied to devil-ui (existing baseline respected).
- Verify `opencode/package.json` lists `@devilcode/keybind: workspace:*`.
- Import smoke test: `bun -e 'import { createDomAdapter } from "@devilcode/kilo-ui/adapters/dom"; const a = createDomAdapter(); console.log(a.kind)'` prints `dom`.
</verify>
<done>
DOM adapter + adapters barrel + finalized exports map land. opencode gains `@devilcode/keybind` workspace dep. Terminal + DOM entry points separated to prevent DOM consumers from loading `@opentui/*`. typecheck green, knip green.
</done>
</task>

<task id="3" title="use-command-registry + use-prompt-history hooks + devil-ui test setup (refined cycle 1)">
<action>
**REFINEMENT NOTE — cycle 1 critique surfaced**: devil-ui has NO test runner configured (no `test` script, no vitest, no `@solidjs/testing-library`). Plan cycle-0 verify step `bun test src/hooks/__tests__` would silently pass w/ zero tests OR crash on Solid JSX. Fix: ship a minimal Bun-native test setup.

**STEP 0 — devil-ui test infrastructure (prerequisite)**:

0a. Create `packages/devil-ui/bunfig.toml`:
    ```toml
    [test]
    root = "./src"
    ```
    Overrides the repo-root `./do-not-run-tests-from-root` sentinel.

0b. Update `packages/devil-ui/package.json` `scripts`:
    - ADD `"test": "bun test"` (does not conflict with existing `test:visual` Playwright script).
    - Add devDependency `@types/bun: "catalog:"` if not already present.

0c. For SolidJS hook testing without `@solidjs/testing-library` (avoid new heavy dep), use SolidJS's native `createRoot`. Cycle-2 CAUTION-1 note: `onMount` does NOT fire under a bare `createRoot` (no render cycle). Hooks must perform their subscription setup in the SYNC body + `onCleanup`, NOT inside `onMount`. Apply this constraint to `use-command-registry.ts` in Task 1 step 1 below.
    ```ts
    import { createRoot, onCleanup } from "solid-js"
    const dispose = createRoot((disposeFn) => {
      // mount hook; signals work; onCleanup fires on disposeFn()
      return disposeFn
    })
    dispose()
    ```
    Document this pattern in a one-file helper at `packages/devil-ui/src/hooks/__tests__/test-harness.ts` (~30 LOC). Helper exports `withRoot<T>(body: (cleanup: () => void) => T): T` for ergonomic test setups.

1. `packages/devil-ui/src/hooks/use-command-registry.ts` (D15, ~70 LOC):
   - Imports from `@devilcode/keybind`: `Command`, `CommandScope`, `CommandRegistry`, `createCommandRegistry`, `KeybindRegistry`, `createKeybindRegistry`.
   - Create a SolidJS context `CommandRegistryContext` holding `{ commands: CommandRegistry, keybinds: KeybindRegistry }`.
   - Export `CommandRegistryProvider` component (accepts optional `registry` prop; defaults to a freshly-created one via `createCommandRegistry` + `createKeybindRegistry`).
   - Export `useCommandRegistry()` hook returning `{ register, unregister, get, search, entries: Accessor<Command[]>, matchEvent }`:
     - **`entries` reactivity via CommandRegistry.subscribe()** — subscription setup happens **synchronously in the hook body** (NOT inside `onMount` — cycle-2 CAUTION-1): `const [entries, setEntries] = createSignal<Command[]>(commands.getAllByScope("global"))` followed by `const unsub = commands.subscribe(() => setEntries(refresh()))` and `onCleanup(unsub)`. Fires correctly under the `createRoot`-based test harness.
     - `register(cmd)` delegates to `commands.register(cmd)` (subscriber callback refreshes `entries()` automatically).
     - `search(query, scope?)` delegates to `commands.search(query, scope)`.
     - `matchEvent` delegates to `keybinds.matchEvent`.
   - Throws if called outside a provider.

2. `packages/devil-ui/src/hooks/use-prompt-history.ts` (D16, ~80 LOC):
   - Export `PromptHistoryStore` interface: `{ save(entry: string): void | Promise<void>, list(): string[] | Promise<string[]> }`.
   - Export `createMemoryStore(capacity = 50): PromptHistoryStore` — in-memory ring buffer.
   - Export `usePromptHistory(store: PromptHistoryStore)` hook returning:
     - `current: Accessor<string>` — empty string when index is -1.
     - `up()` — move index toward older entry (capped at history length - 1).
     - `down()` — move index toward newer (floor at -1, which shows empty).
     - `add(entry: string)` — call `store.save`, refresh signal from `store.list`. Reset index to -1.
   - Handle async store methods via `createResource` or plain `await` + effect. Default in-memory store is synchronous; webview localStorage adapter (Phase 9) is synchronous too; Phase 10 live-editing might want async — design the hook so async returns don't block the UI.
   - Initialize: call `store.list()` on mount; seed signal with result.

3. `packages/devil-ui/src/hooks/index.ts` (D28, refined):
   - Existing file: `export { useFilteredList } from "@opencode-ai/ui/hooks"` + `export * from "./create-auto-scroll"`.
   - APPEND (preserve existing lines):
     ```ts
     export { CommandRegistryProvider, useCommandRegistry } from "./use-command-registry"
     export { usePromptHistory, createMemoryStore } from "./use-prompt-history"
     export type { PromptHistoryStore } from "./use-prompt-history"
     ```
   - Do NOT use `export *` for the new files — prevents accidental upstream shadow.

4. **Hook-level unit tests** using the Step 0c harness:
   - `packages/devil-ui/src/hooks/__tests__/use-command-registry.test.ts`:
     - mount provider via `createRoot`, register 3 commands; read `entries()` accessor; assert length 3.
     - call `search("pla")`; assert result ordering.
     - call unregister fn returned from register; assert entries shrink to 2 AND `entries()` reactivity fires (signal re-evaluated).
     - **subscribe-path test** — register a command; assert the hook's internal subscriber received the notification (verify via side-effect: an auxiliary signal that mirrors registration count).
   - `packages/devil-ui/src/hooks/__tests__/use-prompt-history.test.ts`:
     - create memory store with capacity 3; add 4 entries; assert oldest drops.
     - call `up()` 3 times; assert current cycles through entries.
     - call `down()` past start; assert current is empty string.

**CODEBASE GUIDANCE**:
- SolidJS convention: hooks use `createSignal` + `createEffect`; do NOT pull `solid-js/store` unless needed (simpler signals preferred).
- If devil-ui lacks a test runner config, create a minimal `bunfig.toml` or use the existing one (read repo root `bunfig.toml` first — CLAUDE.md notes it blocks root-level testing).
- Test files must live inside `packages/devil-ui/` (not repo root) to satisfy bunfig.

**RISK AREA** — Phase 2 review surfaced a blocker around canDelegate dedup assertion. Plan 03-02 is unrelated to team schema, but the lesson applies: assert the `register returns unregister fn` happy-path AND the `duplicate id throws` error path (already covered in Plan 03-01, but re-verify at the hook layer here).
</action>
<verify>
- `ls packages/devil-ui/src/hooks/` shows `use-command-registry.ts`, `use-prompt-history.ts`, `index.ts`, `__tests__/*.test.ts`.
- `cd packages/devil-ui && bun test src/hooks/__tests__` — all hook tests pass.
- `bun turbo typecheck` → PASS.
- `bun run knip` (in devil-vscode) still green — no orphan exports.
- Consumer smoke: `import { usePromptHistory, createMemoryStore } from "@devilcode/kilo-ui/hooks/use-prompt-history"` resolves.
</verify>
<done>
Both hooks implemented + tested. Subscribable CommandRegistry + memory-backed prompt history exposed via devil-ui barrel. Plan 03-03 primitives can mount `<CommandRegistryProvider>` + `<RenderTargetProvider>` at the top of each story / demo.
</done>
</task>

</tasks>

<verification>
```bash
bun install
bun turbo typecheck
bun pm ls @opentui/solid   # expect single version 0.1.87
cd packages/devil-keybind && bun test
cd ../../ && cd packages/devil-ui && bun test src/hooks/__tests__
cd ../../ && cd packages/devil-vscode && bun run knip && bun run format:check
cd ../../ && bun run check-kilocode-change
```

All must PASS.
</verification>

<success_criteria>
- [ ] Root `package.json` `workspaces.catalog` gains `@opentui/solid: 0.1.87` + `@opentui/core: 0.1.87` + `@devilcode/kilo-ui: workspace:*`.
- [ ] `packages/opencode/package.json` `@opentui/*` flipped to `catalog:`; gains `@devilcode/kilo-ui: workspace:*` + `@devilcode/keybind: workspace:*`.
- [ ] `packages/devil-ui/package.json` lists `@devilcode/keybind: workspace:*` + `fuzzysort: "catalog:"` + peer `@opentui/solid: "catalog:"` + peer `@opentui/core: "catalog:"` (both `optional: true`). Has `"test": "bun test"` script.
- [ ] `packages/devil-ui/bunfig.toml` present with `[test] root = "./src"`.
- [ ] `packages/devil-ui/src/context/render-target.tsx` exports `RenderTargetProvider`, `useRenderTarget`, `Surface`, types `RenderTargetKind` + `RenderTargetAdapter`.
- [ ] `packages/devil-ui/src/adapters/{terminal.ts,dom.ts,index.ts,README.md}` exist; barrel is DOM-safe; README documents the deep-path rule for terminal.
- [ ] `packages/devil-ui/src/hooks/{use-command-registry.ts,use-prompt-history.ts,__tests__/test-harness.ts,__tests__/*.test.ts}` exist.
- [ ] `packages/devil-ui/src/context/index.ts` + `hooks/index.ts` use EXPLICIT named exports for new symbols (no `export *` collision w/ upstream `@opencode-ai/ui`).
- [ ] `cd packages/devil-ui && bun test src/hooks/__tests__` — all pass via createRoot harness (no `@solidjs/testing-library` added).
- [ ] `bun pm ls @opentui/solid` returns a single resolved version (0.1.87) via catalog dedup.
- [ ] `bun turbo typecheck` green.
- [ ] `bun run knip` green in devil-vscode.
- [ ] `bun run check-kilocode-change` green.
- [ ] `packages/devil-ui/src/primitives/index.ts` exists as an empty barrel.
- [ ] use-command-registry reactivity driven by `CommandRegistry.subscribe()` (no monkey-patch; no polling).
</success_criteria>

<output>
Append to `.planning/STATE.md` under `## Phase 3 Wave 2 Results`:

```md
## Phase 3 Wave 2 Results
- Plan 03-02 (Wave 2): devil-ui RenderTarget + Adapters + Hooks — Complete.
  - RenderTarget context + provider + Surface helper shipped
  - Terminal adapter (OpenTUI peer-dep) + DOM adapter (no OpenTUI) shipped
  - use-command-registry + use-prompt-history hooks + tests passing
  - @opentui/solid single version (0.1.87) verified via bun pm ls
  - opencode gains @devilcode/keybind workspace dep
  - typecheck / knip / check-kilocode-change all green
  - Plan 03-03 primitives can be assembled on top
```
</output>

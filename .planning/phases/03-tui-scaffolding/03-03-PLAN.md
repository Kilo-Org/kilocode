---
phase: 3
plan: 03
wave: 3
depends_on: [03-02]
refine_cycle: 1
title: "Primitives + Storybook + Integration"
primary_agents: [frontend-developer, ui-designer]
reviewer: qa-verification-specialist
must_haves:
  - "CommandPalette, HelpOverlay, FooterBar primitives implemented w/ terminal + DOM branches via <Surface>"
  - "PasteModal implemented DOM-first; terminal branch uses existing workflow-tui multiline input wrapper (OpenTUI has no <textarea> primitive — deferred full terminal paste modal to Phase 5)"
  - "Storybook terminal-harness feasibility spike time-boxed to 1h; expected outcome is INFEASIBLE (@opentui/solid targets terminal, not browser DOM; Storybook runs under Vite w/ browser conditions) — pre-authorized fallback is DOM-only Storybook + text-golden unit tests for terminal layout"
  - "4 Storybook story files: DOM variants mandatory; terminal variants only if spike confirms feasibility, otherwise text-golden snapshot files"
  - "Storybook story files placed flat at packages/devil-ui/src/stories/*.stories.tsx to match existing convention (not nested under primitives/)"
  - "Playwright visual regression spec added to existing tests/ dir (existing playwright.config.ts PRESERVED — do not overwrite); baseline snapshots generated in Linux/CI env only"
  - "packages/opencode/src/devilcode/workflow-tui/index.tsx: wrap goes INSIDE WorkflowProvider; WorkflowViewInner takes NO props (no spread operator); existing command.register() stays in place and coexists with new registry"
  - "Existing workflow-tui tree renders unchanged in smoke test; existing useKeybind + useCommandDialog + command.register consumers unaffected"
  - "bunx playwright install chromium documented as prerequisite (browsers not bundled)"
  - "bun turbo typecheck + bun run knip + bun run format:check + bun run check-kilocode-change all green"
deliverables: [D17, D18, D19, D20, D21, D22, D23, D24, D25, D26, D30]
new_in_refine_cycle1: [paste-modal terminal deferred, stories flat convention, playwright config preserved, WorkflowProvider-inside wrap, no props on WorkflowViewInner, browser install documented]
estimated_loc: 455
---

<objective>
Build the four interaction primitives — CommandPalette, HelpOverlay, FooterBar, PasteModal — as SolidJS components using the RenderTarget abstraction from Plan 03-02. Ship Storybook stories for each (terminal + DOM variants where feasible) and a Playwright visual regression for DOM. Integrate by wrapping `WorkflowViewInner` in providers inside the existing `workflow-tui/index.tsx` — no other workflow-tui files touched.
</objective>

<context>
Reference: `.planning/specs/03-tui-scaffolding-spec.md` §3 (D17–D26, D30), §5 (package structure), §6 (adapter contract), §8 (testing strategy).

Prior plans (both complete per dependency chain):
- **03-01**: `@devilcode/keybind` exposes Command / CommandRegistry / KeybindRegistry / searchCommands / createLeaderChain.
- **03-02**: devil-ui exposes RenderTargetProvider / useRenderTarget / Surface / terminal+DOM adapters / useCommandRegistry / usePromptHistory.

Existing workflow-tui (from CODEBASE.md + STATE.md Phase 2 results):
- 8 files at `packages/opencode/src/devilcode/workflow-tui/`.
- `index.tsx` is the top-level SolidJS root; currently wraps `WorkflowViewInner` in some providers (read the file before editing).
- `packages/opencode/src/cli/cmd/tui/context/keybind.tsx` owns `useKeybind` — do NOT touch.
- `dialog-command.tsx` owns the old command palette — leave in place (Phase 5 removes it).
- `detail-panel.tsx:113-115` has a rendering bug (spec) — NOT fixed in Phase 3; scheduled for Phase 5.

Storybook context:
- devil-ui has `.storybook/` already configured.
- OpenTUI renders to terminal — running it inside Storybook's browser sandbox is the open feasibility question from spec §12 risk 2.
- Fallback plan already approved by spec: DOM-only Storybook + text-golden unit tests for terminal layout.

Testing:
- Playwright visual regression lives at `packages/devil-ui/tests/primitives-visual.spec.ts` (D26).
- Baseline snapshots committed to repo (per CI-will-catch-regressions pattern).
- Terminal snapshots deferred (Phase 5 decision per spec §8).

Fork hygiene:
- No `devilcode_change` markers needed in devil-ui additions.
- Workflow-tui `index.tsx` lives under `src/devilcode/` — markers exempt.

Risks (spec §12):
- **Terminal Storybook harness unknown** — Task 1 spike; Task 2 adapts based on spike outcome.
- **Renderer-singleton collision** — already mitigated in Plan 03-02 via peer-dep + catalog-pin + `bun pm ls` check. Plan 03-03 primitives must NOT import `@opentui/solid` directly; they import the terminal adapter from `@devilcode/kilo-ui/adapters/terminal` which is the sole boundary.
- **Existing TUI regression** — Task 3 smoke-tests the workflow-tui tree to confirm the added provider wrappers do not break existing render.
</context>

<tasks>

<task id="1" title="CommandPalette + HelpOverlay + Storybook feasibility spike (refined cycle 1)">
<action>
Build the first two primitives + resolve the Storybook terminal-harness question before committing the full story strategy.

**REFINEMENT NOTE (cycle 1)**: `@opentui/solid` is a Solid RENDERER for terminal UI — it produces OpenTUI Renderable objects, not browser DOM. Storybook runs under Vite w/ `conditions: ["browser", ...]` so `@opentui/solid` imports will likely fail to resolve browser-compatible exports OR render nothing visible. Expected spike outcome: INFEASIBLE. Pre-authorize the fallback; don't budget weeks investigating.

**PART A — Storybook terminal feasibility spike (time-box: 1 HOUR — lowered from 2h per cycle-1 critique)**:

1. In `packages/devil-ui/.storybook/`, create a throwaway `spike-terminal.stories.tsx` that attempts a minimal `@opentui/solid` `<box>` import + render inside a Storybook DOM story.
   - If import fails at build time → confirmed INFEASIBLE.
   - If import succeeds but render is blank/broken → confirmed INFEASIBLE.
   - If render produces viewable output (unlikely) → feasible; document exactly what worked.
2. Document the outcome at `packages/devil-ui/src/primitives/TERMINAL-STORYBOOK-DECISION.md`:
   - Expected result: INFEASIBLE → fall back to DOM-only Storybook + text-golden unit tests (assert terminal-branch output string matches committed `__terminal-snapshots__/*.txt` files).
   - If feasible: proceed with terminal + DOM story variants. (Unlikely.)
3. Delete the spike file before finishing the task (keep the decision doc).

**PART B — CommandPalette primitive (D17, ~180 LOC)**:

4. `packages/devil-ui/src/primitives/command-palette/index.tsx`:
   - SolidJS component `<CommandPalette>` with props:
     - `scope: CommandScope` (from `@devilcode/keybind`)
     - `open: boolean` (controlled) OR `onClose: () => void`
     - `onSelect?: (cmd: Command) => void` (fires before close)
     - `placeholder?: string` (default: "Type a command...")
   - Internal state: `query: Signal<string>`, derived `results: Accessor<Command[]>` via `useCommandRegistry().search(query(), scope)`.
   - Keybind: palette responds to Escape via the adapter — but keybind routing is the consumer's job (palette exposes `onClose`; upstream registers Ctrl+K to toggle).
   - Rendering via `<Surface kind={adapter.kind}>`:
     - Terminal branch: `<box>` with border + inline `<input>` (OpenTUI's input primitive — confirm API before coding) + vertical list of results using `<text>`; highlighted result uses inverse color.
     - DOM branch: `<div class="command-palette">` with `<input>` + `<ul>`; highlighted result via `aria-selected`; fuzzysort highlights rendered via `<b>` if `adapter.supportsRichHighlight`.
   - Keyboard navigation inside palette: ArrowUp/Down moves selection; Enter fires `onSelect(results()[index])`; Escape fires `onClose()`.
   - Initial focus: on mount, call `adapter.focus(<palette-input-id>)`.

**PART C — HelpOverlay primitive (D18, ~140 LOC)**:

5. `packages/devil-ui/src/primitives/help-overlay/index.tsx`:
   - Props: `scope: CommandScope`, `open: boolean`, `onClose: () => void`.
   - Reads `useCommandRegistry().entries()` filtered by `scope + "global"`.
   - Groups entries by scope heading (Global / Workflow / Team-Builder / Review).
   - Each entry renders as a row: `[keybind.binding || "—"]  <title>  <description || "">`.
   - `<Surface>`-based terminal vs DOM branch; terminal uses a bordered `<box>` with `<text>` rows; DOM uses `<table>` with scoped classes for styling.
   - Escape closes.

**PART D — Storybook stories**:

6. `packages/devil-ui/src/stories/command-palette.stories.tsx` (D22, ~90 LOC) — flat path matches existing devil-ui stories convention (critique C7):
   - Meta config + `decorators` that wrap in `RenderTargetProvider` (DOM) + `CommandRegistryProvider`.
   - A `setup()` function that registers ~12 sample commands across scopes to exercise ranking + grouping.
   - Stories:
     - `DOM.Default`
     - `DOM.WithQuery` (seed query "pla")
     - `Terminal.Default` — only if spike confirmed feasible in Part A; otherwise omit.
   - If terminal variant not feasible (expected): add a text-golden assertion file + unit test at `packages/devil-ui/src/hooks/__tests__/command-palette-terminal.test.ts` that renders CommandPalette via the Terminal `RenderTargetProvider`, serializes the result tree to a plain-text representation, and asserts it matches `packages/devil-ui/src/stories/__terminal-snapshots__/command-palette.txt`.

7. `packages/devil-ui/src/stories/help-overlay.stories.tsx` (D23, ~70 LOC) — same flat-path convention; same DOM-first + text-golden fallback pattern.

**CODEBASE GUIDANCE**:
- Prefer SolidJS idioms (`<Show>`, `<For>`, `<Switch>`) over ternaries.
- No classes; no default exports; Prettier 120-char.
- Read existing devil-ui stories at `packages/devil-ui/src/stories/` for meta-config + decorator conventions.
- If `packages/devil-ui/.storybook/main.ts` doesn't include `"./src/stories/**/*.stories.tsx"`, update the stories glob (read first, edit minimally).

**RISK AREA**:
- Spike outcome gates the rest of the plan. Do not start Task 2 until Part A resolves.
- If spike fails, update Plan 03-03 tasks 2+3 inline to reflect DOM-only + text-golden strategy BEFORE continuing.
</action>
<verify>
- `packages/devil-ui/src/primitives/TERMINAL-STORYBOOK-DECISION.md` exists + states feasible/infeasible + rationale.
- Spike file removed.
- `ls packages/devil-ui/src/primitives/command-palette/index.tsx` + `help-overlay/index.tsx` exist.
- `ls packages/devil-ui/src/stories/command-palette.stories.tsx` + `help-overlay.stories.tsx` exist.
- `cd packages/devil-ui && bun run build-storybook` builds without errors. Inspect output for the two new story paths.
- `bun turbo typecheck` → PASS.
- If text-golden fallback taken: `ls packages/devil-ui/src/stories/__terminal-snapshots__/*.txt` exist + tests assert content matches.
</verify>
<done>
Storybook terminal strategy decided + documented. CommandPalette + HelpOverlay primitives implemented with RenderTarget-branched rendering. Two Storybook story files land. Type-check + Storybook build green.
</done>
</task>

<task id="2" title="FooterBar + PasteModal + remaining stories">
<action>
Build the final two primitives and complete the Storybook coverage.

1. `packages/devil-ui/src/primitives/footer-bar/index.tsx` (D19, ~110 LOC):
   - Props: `scope: CommandScope`, `max?: number` (default 5).
   - Reads `useCommandRegistry().entries()` filtered by scope + `keybind != null` + `hidden === false`; sorts by a priority field — since Command schema doesn't yet have `priority`, accept rendering order = registration order and slice to `max`.
   - Terminal branch: horizontal `<box>` with inline `<text>` rows like `[Ctrl+K] Commands | [?] Help | [/paste] Paste`.
   - DOM branch: `<footer>` with `<span>` tiles + scoped styling.
   - Clicking / pressing a listed shortcut does NOT re-dispatch the command — FooterBar is a hint display, not a router. Keybind routing stays upstream.

2. `packages/devil-ui/src/primitives/paste-modal/index.tsx` (D20, ~100 LOC) — **terminal branch scope reduced per cycle-1 critique CA3: OpenTUI has NO `<textarea>` JSX primitive**:
   - Props: `open: boolean`, `onClose: () => void`, `onSubmit: (text: string) => void`.
   - DOM branch (primary for Phase 3): `<dialog>` with native `<textarea>` + two `<button>` actions.
   - Terminal branch (Phase 3): bordered `<box>` with a **single-line `<input>` placeholder** labeled "Paste (multiline editor: Phase 5)" plus `[Ctrl+Enter] Submit  [Esc] Cancel` hints. The actual multiline paste widget for terminal is NOT implemented in Phase 3; Phase 5 rebuilds it on top of the `WorkflowCommandInput` component's existing multiline handling (read `packages/opencode/src/devilcode/workflow-tui/command-input.tsx` to see the existing approach). Document this limitation in a `<!-- PHASE-5-TODO -->` HTML comment at the top of the file.
   - Ctrl+Enter submits; Esc cancels. Use `adapter.focus()` on mount to focus the single-line input (terminal) or textarea (DOM).
   - Controlled: caller owns `open` state.
   - This scope reduction keeps Phase 3 strictly primitive-scaffolding; the paste modal is shipped usable in DOM and stubbed in terminal. Phase 5 (runtime cockpit redesign) is already budgeted for workflow-tui replacement, so the full terminal-paste widget fits there.

3. `packages/devil-ui/src/primitives/index.ts` (D21, replace empty barrel):
   - `export { CommandPalette } from "./command-palette"`
   - `export { HelpOverlay } from "./help-overlay"`
   - `export { FooterBar } from "./footer-bar"`
   - `export { PasteModal } from "./paste-modal"`

4. `packages/devil-ui/src/stories/footer-bar.stories.tsx` (D24, ~70 LOC, flat path) — DOM.Default + (Terminal.Default OR text-golden).
5. `packages/devil-ui/src/stories/paste-modal.stories.tsx` (D25, ~70 LOC, flat path) — DOM.OpenDefault + DOM.OpenWithText + (Terminal.Open OR text-golden). Note the terminal story is necessarily stub-level per Task 2 step 2 refinement.

6. `packages/devil-ui/tests/primitives-visual.spec.ts` (D26, ~80 LOC):
   - **DO NOT create a new `playwright.config.ts`**. It already exists at `packages/devil-ui/playwright.config.ts` with full webServer config (Storybook build + http-server at localhost:6006, testDir `./tests`, baseURL set). Plan 03-03 only writes the spec inside the existing testDir.
   - Spec loads each primitive's DOM story via the Storybook iframe URL pattern (`http://localhost:6006/iframe.html?id=<story-id>&viewMode=story`); use the meta `title` from each story file to derive ids.
   - One baseline snapshot per primitive at `packages/devil-ui/tests/__screenshots__/primitives-visual-<primitive>.png` using Playwright's `toHaveScreenshot()` API (follows Playwright conventions for per-platform baselines).
   - **Document `bunx playwright install chromium` as a first-run prerequisite** (Playwright browsers not bundled; ~200MB download). Add to `packages/devil-ui/tests/README.md` (new, 10 lines).
   - **Cross-platform baselines** (critique CA4): user is on Windows; Playwright pixel-exact snapshots differ per-OS. Generate baselines ONLY in Linux (CI). Local Windows runs use `bunx playwright test --update-snapshots=none` and compare against committed Linux baselines (will show pixel diff; acceptable for local iteration, not a merge gate).

**CODEBASE GUIDANCE**:
- FooterBar and PasteModal must be independently testable (importable + renderable without a full WorkflowView context — only RenderTargetProvider + CommandRegistryProvider needed).
- Visual regression baselines commit to repo; keep them under 100KB each to avoid git bloat.
- Cross-platform: Playwright baselines are OS-dependent; document that CI runs on Linux (the typical Kilo CI env) so Windows devs should not regenerate locally without flagging.

**RISK AREA**:
- Playwright screenshots can flake on font rendering differences between platforms. Use `--update-snapshots` only from CI or a Linux dev env. Document this in `packages/devil-ui/tests/README.md` (5-line note).
</action>
<verify>
- `ls packages/devil-ui/src/primitives/{footer-bar,paste-modal}/index.tsx` exist.
- `cat packages/devil-ui/src/primitives/index.ts` exports all four primitives.
- `ls packages/devil-ui/src/stories/{footer-bar,paste-modal}.stories.tsx` exist (flat path per cycle-1 convention fix).
- `ls packages/devil-ui/tests/primitives-visual.spec.ts` exists; `ls packages/devil-ui/tests/__screenshots__/*.png` has 4 baselines (one per primitive).
- `cd packages/devil-ui && bun run build-storybook && bunx playwright test` — all DOM visual tests pass.
- `bun turbo typecheck` → PASS.
- `bun run knip` (in devil-vscode) → still green.
</verify>
<done>
All four primitives shipped + four Storybook story files + Playwright visual regression baselines committed. Primitives barrel exports everything Plan 03-03 Task 3 needs for integration.
</done>
</task>

<task id="3" title="opencode integration + smoke test">
<action>
Wire the new registry + RenderTarget provider into the existing `workflow-tui/index.tsx` as a minimal wrap — no restructuring, no replacing existing `useKeybind` / `useCommandDialog`.

1. Read `packages/opencode/src/devilcode/workflow-tui/index.tsx` in full. **Verified structure (critique C5 ground-truth)**:
   - `WorkflowViewInner()` takes **NO props**.
   - Existing `command.register([...])` call is INSIDE `WorkflowViewInner` (lines 25–34).
   - `WorkflowView()` wraps `<WorkflowProvider directory={sdk.directory!}>` around `<WorkflowViewInner />`.
   - Existing import: `useKeyboard` from `@opentui/solid`, `useCommandDialog` from `@tui/component/dialog-command`.

2. Edit `packages/opencode/src/devilcode/workflow-tui/index.tsx` (D30, ~12 LOC net, cycle-2 refined — async factory + top-level await):
   - Add imports at the top (alphabetized within the import block):
     ```ts
     import { createTerminalAdapter } from "@devilcode/kilo-ui/adapters/terminal"
     import { RenderTargetProvider } from "@devilcode/kilo-ui/context/render-target"
     import { CommandRegistryProvider } from "@devilcode/kilo-ui/hooks/use-command-registry"
     ```
   - Create a module-level adapter via top-level `await` (Plan 03-02 Task 1 step 4 made `createTerminalAdapter` an async factory to keep `@opentui/*` out of DOM bundles):
     ```ts
     // HMR-safe: adapter is a plain POJO; no global side effects at factory time.
     // Dynamic import of @opentui/core happens inside createTerminalAdapter.
     const terminalAdapter = await createTerminalAdapter()
     ```
     Verify Bun + the opencode module system handle top-level await in this file. `packages/opencode/` is ESM (`"type": "module"` in its package.json is inherited or explicit — confirm). If top-level await breaks tsgo typecheck or Bun startup, fall back to a `Promise<RenderTargetAdapter>` exported from this module and have `WorkflowView` await it via a SolidJS `createResource` — document the chosen pattern clearly.
   - Modify `WorkflowView` to scope the new providers **INSIDE `WorkflowProvider`**, AROUND `<WorkflowViewInner />`:
     ```tsx
     export function WorkflowView() {
       const sdk = useSDK()
       return (
         <WorkflowProvider directory={sdk.directory!}>
           <RenderTargetProvider adapter={terminalAdapter}>
             <CommandRegistryProvider>
               <WorkflowViewInner />
             </CommandRegistryProvider>
           </RenderTargetProvider>
         </WorkflowProvider>
       )
     }
     ```
   - `<WorkflowViewInner />` takes no props — do NOT use `{...existing}` spread (none exists).
   - The existing `command.register([...])` call inside `WorkflowViewInner` stays UNTOUCHED. It uses `useCommandDialog` (old palette); the new `CommandRegistryProvider` is a DIFFERENT registry, empty in Phase 3. Coexistence is explicit: Phase 3 runs both registries; Phase 5 migrates consumers off `useCommandDialog` and deletes it.
   - Do NOT register any commands against the new `CommandRegistryProvider` in this file — registry exists, empty, reserved for Phase 5 population.

3. **Existing-behavior smoke test**:
   - Run the TUI manually: `bun run dev .` from repo root.
   - Open the workflow view and confirm it renders without visual regression (header, detail panel, command input all look identical to Phase 2 baseline).
   - Confirm existing keybinds still work: Escape, slash commands, Ctrl+C.
   - Confirm `useCommandDialog` (old palette) still opens — Phase 3 does not remove it.
   - If smoke reveals regression, debug (likely culprit: renderer-singleton collision resurfacing). Run `bun pm ls @opentui/solid` to confirm single version; if multiple, fix catalog pin.

4. **Integration test (automated)**:
   - If `packages/opencode/test/devilcode/workflow-tui/` exists with integration specs, verify they still pass. If none exist, add `packages/opencode/test/devilcode/workflow-tui/index.smoke.test.ts` that:
     - Imports `WorkflowView` and renders it with a minimal mock context (reuse existing test harness if present).
     - Asserts the render does not throw.
     - Asserts `useCommandRegistry` is accessible from a child component (mount a probe component that calls the hook).
   - Bun native runner.

5. **CI gates** (run all from repo root):
   - `bun turbo typecheck`
   - `cd packages/devil-vscode && bun run knip && bun run format:check`
   - `bun run check-kilocode-change`

**CODEBASE GUIDANCE**:
- Workflow-tui `index.tsx` is under `src/devilcode/` — no `devilcode_change` markers.
- The 8 workflow-tui files are scheduled for Phase 5 replacement; Plan 03-03 must NOT pre-empt that work. Only touch `index.tsx`.

**RISK AREA**:
- The renderer-singleton risk re-emerges here because opencode loads @opentui/solid at runtime. If Plan 03-02's peer-dep strategy didn't fully dedupe, this is the integration point where it would show up. The `bun pm ls` check is repeated here as a belt-and-braces verification.
- `packages/opencode/src/cli/cmd/tui/context/keybind.tsx` already handles leader keys — the new `createLeaderChain` in devil-keybind is unused by existing code. Confirm no accidental double-registration of Ctrl+X handlers.
</action>
<verify>
- `packages/opencode/src/devilcode/workflow-tui/index.tsx` diff shows ~8 added lines (two providers + three imports + module-level adapter const); no other files changed.
- `bun pm ls @opentui/solid` — single version.
- `bun run dev .` — TUI boots, workflow view renders, no console errors, no visual regression observed.
- `bun turbo typecheck` → PASS.
- `cd packages/devil-vscode && bun run knip` → PASS.
- `cd packages/devil-vscode && bun run format:check` → PASS.
- `bun run check-kilocode-change` → PASS.
- Integration smoke test passes: `cd packages/opencode && bun test test/devilcode/workflow-tui/index.smoke.test.ts` (or existing integration suite).
- Existing `useCommandDialog` (old palette) still opens; Escape / slash / Ctrl+C still work.
</verify>
<done>
workflow-tui/index.tsx wraps with RenderTargetProvider + CommandRegistryProvider; existing TUI behavior preserved; all CI gates green; integration smoke test passes.
</done>
</task>

</tasks>

<verification>
```bash
bun install
bun turbo typecheck
bun pm ls @opentui/solid  # single version
cd packages/devil-keybind && bun test
cd ../../ && cd packages/devil-ui && bun test && bun run build-storybook && bunx playwright test
cd ../../ && cd packages/opencode && bun test test/devilcode/workflow-tui/
cd ../../ && cd packages/devil-vscode && bun run knip && bun run format:check
cd ../../ && bun run check-kilocode-change
bun run dev .  # manual smoke: workflow view renders, no regression
```

All must PASS.
</verification>

<success_criteria>
- [ ] 3 primitives fully live with terminal + DOM branches via `<RenderSurface>` (renamed from `<Surface>` per cycle-2 CRITICAL-4): command-palette, help-overlay, footer-bar.
- [ ] Primitives use declarative focus (`focused={adapter.focusedNodeId() === "id"}` JSX prop) — NO imperative `adapter.focus(nodeId)` calls (cycle-2 CRITICAL-1).
- [ ] paste-modal DOM branch fully live; terminal branch stubbed (single-line input + Phase-5-TODO comment documenting the full multiline terminal widget is deferred to Phase 5).
- [ ] Storybook terminal-harness decision documented at `packages/devil-ui/src/primitives/TERMINAL-STORYBOOK-DECISION.md` (expected INFEASIBLE → text-golden fallback).
- [ ] 4 Storybook story files at flat path `packages/devil-ui/src/stories/*.stories.tsx` — each covers DOM variant; terminal variant present only if spike confirmed feasible, else text-golden unit tests assert terminal-branch serialized output.
- [ ] Existing `packages/devil-ui/playwright.config.ts` PRESERVED (no overwrite).
- [ ] Playwright visual regression spec at `packages/devil-ui/tests/primitives-visual.spec.ts`; Linux/CI-generated baselines committed (Windows devs do not regenerate locally).
- [ ] `packages/devil-ui/tests/README.md` documents `bunx playwright install chromium` prerequisite.
- [ ] `packages/opencode/src/devilcode/workflow-tui/index.tsx` — new providers scoped INSIDE `<WorkflowProvider>`; `<WorkflowViewInner />` has no props (no spread); existing `command.register(...)` call inside WorkflowViewInner UNTOUCHED.
- [ ] Module-scoped `const terminalAdapter = await createTerminalAdapter()` (top-level await + async factory per cycle-2 CRITICAL-5); fallback documented if top-level await breaks Bun startup.
- [ ] `bun pm ls @opentui/solid` reports single resolved version 0.1.87 (via catalog dedup from Plan 03-02).
- [ ] Existing TUI smoke test clean: workflow view renders, no visual regression, existing keybinds + `useCommandDialog` + `command.register` still functional.
- [ ] `bun turbo typecheck` green.
- [ ] `bun run knip` green in devil-vscode.
- [ ] `bun run format:check` green in devil-vscode.
- [ ] `bun run check-kilocode-change` green.
- [ ] Integration smoke test passes.
</success_criteria>

<output>
Append to `.planning/STATE.md` under `## Phase 3 Wave 3 Results`:

```md
## Phase 3 Wave 3 Results
- Plan 03-03 (Wave 3): Primitives + Storybook + Integration — Complete.
  - CommandPalette + HelpOverlay + FooterBar + PasteModal shipped w/ <Surface>-branched rendering
  - Storybook terminal-harness: {feasible | infeasible, fallback to text-golden} — decision committed
  - 4 story files + Playwright visual regression baselines committed
  - workflow-tui/index.tsx wraps providers (~8 LOC); no other workflow-tui file touched
  - Existing TUI smoke test clean; useCommandDialog + useKeybind unaffected
  - All CI gates green (typecheck, knip, format, check-kilocode-change)
  - Phase 3 COMPLETE — ready for Phase 4 (Team Builder Views)
```

Then update the Next Action in `.planning/STATE.md` to: `Run /legion:review to review Phase 3 before Phase 4 planning.`
</output>

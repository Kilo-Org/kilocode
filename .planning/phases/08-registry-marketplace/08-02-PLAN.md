---
phase: 8
plan: 2
wave: 2
depends_on: ["08-01"]
must_haves:
  - publishManifest orchestration function
  - installManifest orchestration function
  - /team publish, /team install, /team trust, /team untrust commands
  - TUI integration (command-input.tsx, index.tsx)
agents:
  primary: Senior Developer
  secondary: Frontend Developer
  reviewer: QA Verification Specialist
refine_cycle: 0
---

<objective>
Ship the I/O orchestration layer and TUI command integration. `publishManifest()` creates signed manifest files. `installManifest()` fetches, verifies, and installs teams. Commands wired into workflow-tui following Phase 6 pattern.
</objective>

<context>
## Prior Plan Outputs (08-01)
- `team/registry/manifest.ts` — TeamRegistryManifest schema
- `team/registry/signing.ts` — signManifest, verifyManifestSignature
- `team/registry/http-client.ts` — fetchManifest
- `team/registry/trust-store.ts` — getTrustedPublisher
- `team/registry/errors.ts` — TeamSignatureError, TeamPublisherNotTrusted, etc.

## Phase 6 Pattern Reference
- `workflow-tui/commands/team-io.ts` — command handler pattern with `TeamIOCommandHandlers` DI type
- `command-input.tsx` — `cmd.startsWith("team export ")` branch pattern
- `index.tsx` — `registerTeamIOCommands()` registration pattern

## Key Constraints
- Install requires signature verification against trust store (unless skipTrustCheck)
- Unsigned manifests show warning but can still be installed
- `/team trust` reads a PEM file and adds to trust store
- `/team untrust` removes by publisher ID
</context>

<tasks>

<task id="1">
<title>Registry I/O module</title>
<action>
1. Create `team/registry/io.ts`:
   ```ts
   export interface PublishOptions {
     name: string
     author: string
     publisherId: string
     version: string
     license?: string
     description?: string
     tags?: string[]
     homepage?: string
     repository?: string
     privateKey?: string  // If provided, sign the manifest
   }
   
   export async function publishManifest(
     config: CanonicalTeamConfig,
     outputPath: string,
     options: PublishOptions,
   ): Promise<TeamRegistryManifest>
   // Creates envelope inline (not via exportTeamToFile — no extra file)
   // Computes checksum via computeTeamChecksum
   // Signs if privateKey provided
   // Writes manifest JSON to outputPath
   
   export interface InstallOptions {
     requireSignature?: boolean
     skipTrustCheck?: boolean
   }
   
   export async function installManifest(
     source: string,  // URL or file path
     options?: InstallOptions,
   ): Promise<{ config: CanonicalTeamConfig; manifest: TeamRegistryManifest; warnings: string[] }>
   // Detects URL vs file path
   // Fetches or reads
   // Validates schema
   // Verifies signature if present (unless skipTrustCheck)
   // Returns config + manifest + warnings
   ```
2. Create `test/devilcode/team/registry/io.test.ts`:
   - publishManifest creates valid file
   - publishManifest with privateKey signs manifest
   - installManifest from local file
   - installManifest verifies signature
   - installManifest warns on unsigned
   - installManifest rejects untrusted publisher (mock trust store)
3. EDIT `team/index.ts` — add registry re-exports:
   ```ts
   export * from "./registry"
   // Or explicit: export { TeamRegistryManifest, publishManifest, installManifest, ... } from "./registry"
   ```
</action>
<verify>
```bash
bun test test/devilcode/team/registry/io.test.ts && bun turbo typecheck
```
Expected: All io tests pass. Typecheck clean. `rg "publishManifest" packages/opencode/src/devilcode/team/index.ts` matches.
</verify>
<done>io.ts exports publishManifest and installManifest; 6+ io.test.ts cases pass; team/index.ts re-exports registry module</done>
</task>

<task id="2">
<title>Command handlers</title>
<action>
1. Create `workflow-tui/commands/team-registry.ts`:
   ```ts
   import type { Command } from "@devilcode/keybind"
   import { publishManifest, installManifest, addTrustedPublisher, removeTrustedPublisher, listTrustedPublishers } from "../../team/registry"
   import { TeamSignatureError, TeamPublisherNotTrusted, TeamManifestFetchFailed, TeamManifestInvalid } from "../../team/registry/errors"
   
   type RegisterFn = (cmd: Command) => () => void
   
   export type TeamRegistryCommandHandlers = {
     getActiveTeam: () => CanonicalTeamConfig | undefined
     onInstalled: (config: CanonicalTeamConfig) => Promise<void>
     promptPath: (message: string) => Promise<string | undefined>
     promptConfirm: (message: string) => Promise<boolean>
     toast: { success: (msg: string) => void; error: (msg: string) => void; warning: (msg: string) => void }
   }
   
   export async function publishCommand(args: { path: string; name: string; author: string; version: string; sign?: string }, handlers: TeamRegistryCommandHandlers): Promise<void>
   // Validates args
   // Reads privateKey if sign provided
   // Generates publisherId (UUID)
   // Calls publishManifest
   
   export async function installCommand(args: { source: string }, handlers: TeamRegistryCommandHandlers): Promise<void>
   // Calls installManifest
   // Shows warnings
   // Prompts for untrusted publisher confirmation
   // Calls onInstalled
   
   export async function trustCommand(args: { keyFile: string; publisherId: string }, handlers: TeamRegistryCommandHandlers): Promise<void>
   // Reads PEM file
   // Adds to trust store
   
   export async function untrustCommand(args: { publisherId: string }, handlers: TeamRegistryCommandHandlers): Promise<void>
   // Removes from trust store
   
   export function registerTeamRegistryCommands(register: RegisterFn, handlers: TeamRegistryCommandHandlers): () => void
   // Registers: workflow.team.publish, workflow.team.install, workflow.team.trust, workflow.team.untrust
   ```
2. Create `test/devilcode/workflow-tui/team-registry.commands.test.ts`:
   - Mock handlers
   - Each error type maps to correct toast variant
   - Untrusted publisher prompts confirmation
   - Trust/untrust modify store
</action>
<verify>
```bash
bun test test/devilcode/workflow-tui/team-registry.commands.test.ts && bun turbo typecheck
```
Expected: Command tests pass. Typecheck clean.
</verify>
<done>team-registry.ts exports all command handlers and registerTeamRegistryCommands; tests verify error→toast mapping and confirmation flow</done>
</task>

<task id="3">
<title>TUI integration</title>
<action>
1. EDIT `workflow-tui/command-input.tsx`:
   - Add imports from `./commands/team-registry`
   - Add branches BEFORE `WorkflowStage.safeParse(cmd)` fallback (per Phase 6 R2-10 pattern):
     ```ts
     if (cmd.startsWith("team publish ")) {
       // Parse: team publish <path> --name=<n> --author=<a> --version=<v> [--sign=<keyfile>]
       // Call publishCommand(args, handlers)
       return
     }
     if (cmd.startsWith("team install ")) {
       const source = cmd.slice("team install ".length).trim()
       if (!source) { toast.show({ message: "Usage: team install <url-or-path>", variant: "warning", duration: 3000 }); return }
       await installCommand({ source }, handlers)
       return
     }
     if (cmd.startsWith("team trust ")) {
       // Parse: team trust <keyfile> <publisher-id>
       // Call trustCommand
       return
     }
     if (cmd.startsWith("team untrust ")) {
       const publisherId = cmd.slice("team untrust ".length).trim()
       if (!publisherId) { toast.show({ message: "Usage: team untrust <publisher-id>", variant: "warning", duration: 3000 }); return }
       await untrustCommand({ publisherId }, handlers)
       return
     }
     ```
2. Build handlers factory (closure pattern from Phase 6):
   ```ts
   const registryHandlers = (): TeamRegistryCommandHandlers => ({
     getActiveTeam: () => team(),
     onInstalled: async (config) => {
       const current = await Config.get()
       await Config.update({ ...current, team: config })
     },
     promptPath: async (msg) => undefined,  // v1: toast hint
     promptConfirm: async (msg) => true,    // v1: auto-confirm (user can ctrl+c)
     toast: {
       success: (msg) => toast.show({ message: msg, variant: "info", duration: 3000 }),
       error: (msg) => toast.show({ message: msg, variant: "error", duration: 6000 }),
       warning: (msg) => toast.show({ message: msg, variant: "warning", duration: 4000 }),
     },
   })
   ```
3. EDIT `workflow-tui/index.tsx`:
   - Import `registerTeamRegistryCommands`
   - Inside `WorkflowViewInner`, add:
     ```ts
     const cleanupRegistryCmds = registerTeamRegistryCommands(registry.register.bind(registry), registryHandlers())
     onCleanup(cleanupRegistryCmds)
     ```
4. Create structural test:
   - `test/devilcode/workflow-tui/team-registry.integration.test.ts`:
     - Grep command-input.tsx for all 4 startsWith branches
     - Grep index.tsx for registerTeamRegistryCommands
</action>
<verify>
```bash
# Phase 7 regression gate
bun test test/devilcode/ && bun turbo typecheck
```
Expected: 318+ Phase 7 tests + new Phase 8 tests all pass. Typecheck clean.
</verify>
<done>command-input.tsx has 4 new team registry branches; index.tsx registers commands; all tests pass including Phase 7 regression</done>
</task>

</tasks>

<verification>
## Wave 2 Exit Gate
1. `bun test test/devilcode/team/registry/io.test.ts` — passes
2. `bun test test/devilcode/workflow-tui/team-registry*.test.ts` — passes
3. `bun test test/devilcode/` — all pass (Phase 7 regression + Phase 8)
4. `bun turbo typecheck` — clean
5. `rg "team publish " packages/opencode/src/devilcode/workflow-tui/command-input.tsx` — matches
6. `rg "registerTeamRegistryCommands" packages/opencode/src/devilcode/workflow-tui/index.tsx` — matches
</verification>

<success_criteria>
- publishManifest creates valid signed manifest files
- installManifest fetches, verifies signature, returns config
- /team publish, /team install, /team trust, /team untrust commands work
- Error handling shows correct toast messages
- Untrusted publisher shows warning (v1: auto-proceeds)
- All Phase 7 tests still pass (318+)
- Typecheck clean
</success_criteria>

<output>
## Wave 2 Complete
- NEW: `team/registry/io.ts` — publishManifest, installManifest
- NEW: `workflow-tui/commands/team-registry.ts` — 4 command handlers
- EDIT: `command-input.tsx` — 4 new team registry branches
- EDIT: `index.tsx` — registry command registration
- EDIT: `team/index.ts` — registry re-exports
- Tests: 3+ new files, ~200 LOC
- Ready for Plan 08-03
</output>

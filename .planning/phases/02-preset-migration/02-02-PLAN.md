---
phase: 2
plan: 2
wave: 2
slug: clean-break-removal-consumer-flip-team-init-docs
depends_on: [02-01]
must_haves:
  - PH2-R4
  - PH2-R5
  - PH2-R6
agents:
  primary: senior-developer
  reviewer: backend-architect
estimated_complexity: high
refine_cycle: 1
refine_notes: |
  Cycle 1 addressed these critique findings:
  - C1 (pre-mortem) / A1 (assumption-hunt): SDK regen cascade → explicit "DO NOT run script/generate.ts"; devil-vscode CI stays clean by keeping SDK types stale
  - C2 (pre-mortem): server response shape contradiction → explicitly QuickstartTemplate[] wrapper documented; route handler signature matches
  - A2 (assumption-hunt): route path mismatch → /config/team/presets + /config/team/validate (both flipped)
  - N2 (pre-mortem) / A3/A4 (assumption-hunt): WorkflowStateManager unchanged; team persists via Config.update, not state.ts
  - N4 (pre-mortem): commit boundary allowed after Task 1 (consumer swap + server flip); Task 2 (deletions) commits separately
  - N5 (pre-mortem): canonical-config.test.ts migration block fully removed; coverage lives in migration.test.ts; plan acknowledges full removal
  - INFO: /team migrate CLI deferred; migration doc uses Node API snippet
  - INFO: TEAM_PRESETS test-file consumers enumerated in scope
  - INFO: Config.Info.team field flip explicit (config.ts:1499)
  - INFO: command-input.tsx:48 TeamConfig.safeParse → CanonicalTeamConfig.safeParse
files_modified:
  - packages/opencode/src/server/routes/config.ts
  - packages/opencode/src/config/config.ts
  - packages/opencode/src/devilcode/team/router.ts
  - packages/opencode/src/devilcode/team/agents.ts
  - packages/opencode/src/devilcode/team/index.ts
  - packages/opencode/src/devilcode/team/config.ts
  - packages/opencode/src/devilcode/workflow/build-runner.ts
  - packages/opencode/src/devilcode/workflow/escalation.ts
  - packages/opencode/src/devilcode/workflow/reviewer.ts
  - packages/opencode/src/devilcode/workflow-tui/context.tsx
  - packages/opencode/src/devilcode/workflow-tui/orchestrator.ts
  - packages/opencode/src/devilcode/workflow-tui/command-input.tsx
  - packages/opencode/src/devilcode/workflow-commands.tsx
  - packages/opencode/src/devilcode/workflow-tui/detail-panel.tsx
  - packages/opencode/src/devilcode/workflow-tui/status-bar.tsx
files_modified_conditionally:
  - packages/opencode/test/kilocode/team/router.test.ts
  - packages/opencode/test/kilocode/team/config.test.ts
  - packages/opencode/test/kilocode/team/workflow-integration.test.ts
files_deleted:
  - packages/opencode/src/devilcode/team/presets.ts
files_created:
  - packages/devil-docs/pages/collaborate/teams/migration-v1.md
files_read_not_modified:
  - packages/opencode/src/devilcode/team/migration.ts
  - packages/opencode/src/devilcode/team/quickstarts/index.ts
  - packages/opencode/src/devilcode/team/library.ts
  - packages/opencode/src/devilcode/team/capabilities.ts
  - packages/opencode/test/kilocode/team/migration.test.ts
  - packages/opencode/test/kilocode/team/quickstarts.test.ts
  - packages/opencode/test/kilocode/team/canonical-config.test.ts
  - .planning/phases/02-preset-migration/02-01-SUMMARY.md
  - .planning/phases/02-preset-migration/02-CONTEXT.md
---

<objective>
Execute the full clean-break: swap every CLI consumer of legacy
`TeamConfig`/`TeamRole` to canonical types, flip the server endpoints
(`GET /config/team/presets` + `POST /config/team/validate`) to canonical
DTO, flip `Config.Info.team` schema, delete `presets.ts` + legacy halves
of `config.ts`, rework `/team init` to write quickstarts into
`Config.update({team})`, publish migration doc.

**SDK is NOT regenerated in this phase.** `./script/generate.ts` is
explicitly out of scope. Devil-vscode continues to compile against the
stale `@devilcode/sdk` types — `bun turbo typecheck` stays monorepo-clean.
Devil-vscode webview preset list will break at RUNTIME only (Phase 9
resolves).

Commit boundary: after Task 1 (consumer swap + server flip, legacy dead
code), OPTIONAL intermediate commit. After Task 3 (deletions + docs),
final commit.

After this plan:
- Zero legacy `TeamConfig`/`TeamRole`/`TEAM_PRESETS` references remain
  in the CLI source tree
- `GET /config/team/presets` returns `QuickstartTemplate[]`
- `POST /config/team/validate` accepts + validates `CanonicalTeamConfig`
- `Config.Info.team` typed `CanonicalTeamConfig.optional()`
- `/team init <quickstart>` writes `Config.update({team: quickstart.team})`
- Migration doc published
- All CI checks clean including monorepo typecheck; team test suite clean
- Phase 3 can start assuming canonical-only schema
</objective>

<context>
Phase 2 context: `.planning/phases/02-preset-migration/02-CONTEXT.md`
Plan 02-01 handoff: `.planning/phases/02-preset-migration/02-01-SUMMARY.md`

Dependencies from Plan 02-01 (landed in Wave 1):
- `team/migration.ts` — owns `fromLegacyTeamConfig` + legacy parse schemas (test-only export for `LegacyParseTeamConfig`)
- `team/quickstarts/` — 5 bundled JSONs + static-import loader
- `team/config.ts` — still carries legacy `TeamRole`/`TeamConfig`/`TeamRouting` + a temporary `export { fromLegacyTeamConfig } from "./migration"` re-export
- `canonical-config.test.ts` — slimmed to canonical-only tests
- `migration.test.ts` + `quickstarts.test.ts` — new test files from Plan 02-01
- Plan 02-01 summary documents the full `TEAM_PRESETS` consumer audit

Actual route paths (from `server/routes/config.ts:68-117`):
- `/config/team/validate` — `POST`, currently calls `TeamConfig.safeParse(payload)` at line 92
- `/config/team/presets` — `GET`, currently returns `TEAM_PRESETS` at line 117

Config.Info.team schema field (from `config/config.ts:27, 1499`):
- Line 27: `import { TeamConfig } from "@/devilcode/team/config"`
- Line 1499: `team: TeamConfig.optional().describe("Multi-model team configuration for hierarchical agent dispatch")`

Team runtime source (critique A3/A4):
- `workflow-tui/command-input.tsx:48`: `const result = TeamConfig.safeParse((sync.data.config as { team?: unknown }).team)` — team originates from `Config.get().team` (user's `kilo.json`), NOT from `WorkflowState`. Therefore `/team init <quickstart>` must write into `Config`, not WorkflowState.

Test consumers of `TEAM_PRESETS` (from Plan 02-01 Task 1 audit):
- `test/kilocode/team/canonical-config.test.ts` — uses `TEAM_PRESETS` in migration tests (block relocated to `migration.test.ts` by Plan 02-01 Task 3 Part A; but some references may still exist — audit)
- `test/kilocode/team/migration.test.ts` — imports `TEAM_PRESETS` to construct legacy fixtures (intentional; Plan 02-02 converts to inlined raw-object fixtures)
- `test/kilocode/team/router.test.ts`, `config.test.ts`, `workflow-integration.test.ts` — per Plan 02-01 Task 1 pre-flight audit; listed in this plan's `files_modified_conditionally`

Consumer swap catalog (exact paths + symbols):

| File | Legacy import | New import | Additional edits |
|---|---|---|---|
| `server/routes/config.ts:13-14` | `TeamConfig`, `TEAM_PRESETS`, `TeamPreset` | `CanonicalTeamConfig`, `loadQuickstartTemplates`, `QuickstartTemplate` | Line 92: `CanonicalTeamConfig.safeParse(payload)`. Line 117: `return c.json(Object.values(loadQuickstartTemplates()))`. Update OpenAPI response schema citations. |
| `config/config.ts:27, 1499` | `TeamConfig` | `CanonicalTeamConfig` | Line 1499 field annotation |
| `devilcode/team/router.ts` | `TeamConfig`, `TeamRole` | `CanonicalTeamConfig`, `CanonicalTeamRole` | Any `.capabilities.includes("coding")` → `"implementation"` etc. |
| `devilcode/team/agents.ts` | `TeamConfig`, `TeamRole` | canonical | Tier mapping unchanged |
| `devilcode/team/index.ts` | re-exports legacy | re-exports canonical + migration + quickstart | Barrel rewrite |
| `devilcode/team/config.ts` | exports legacy | canonical only | Task 2 deletion |
| `devilcode/workflow/build-runner.ts` | `TeamConfig` | `CanonicalTeamConfig` | |
| `devilcode/workflow/escalation.ts` | `TeamConfig`; `findParentRole` uses `.tier` | canonical | Parent-role tiebreak behavior covered by Plan 02-01 test |
| `devilcode/workflow/reviewer.ts` | `TeamConfig` | canonical | |
| `devilcode/workflow-tui/context.tsx` | `TeamConfig` | canonical | |
| `devilcode/workflow-tui/orchestrator.ts` | `TeamConfig` | canonical | |
| `devilcode/workflow-tui/command-input.tsx:48` | `TeamConfig.safeParse` | `CanonicalTeamConfig.safeParse` | |
| `devilcode/workflow-commands.tsx` | none today | `loadQuickstartTemplates`, `Config.update` | `/team init <id>` sub-commands |
| `devilcode/workflow-tui/detail-panel.tsx:17` | string copy only | string copy only | NO LAYOUT EDIT |
| `devilcode/workflow-tui/status-bar.tsx:24` | string copy only | string copy only | |

IPC break awareness (re-confirmed post-critique):
- Devil-vscode webview-ui reads `@devilcode/sdk` types (auto-generated). The SDK is NOT regenerated in Phase 2 → types remain legacy. Webview compiles.
- Server returns canonical JSON that does NOT match the stale SDK types → Zod/JSON-parse error at runtime in webview. Phase 9 resolves.
- `bun turbo typecheck` remains clean across `packages/devil-vscode` because TS types did not change.
- **OpenAPI runtime spec drift**: `hono-openapi` generates `/openapi.json` at runtime from the live server's Zod schemas. After Task 1 flips the response schemas, `/openapi.json` serves a canonical-shape spec while the committed `packages/sdk/js/src/v2/gen/` tree still reflects legacy shape. Any tool that dev-fetches `/openapi.json` (not the committed SDK) will see the divergence. Devil-vscode uses committed SDK types, so this is dev-tooling only; documented, not blocking.
</context>

<tasks>

<task id="02-02-T1">
  <title>Consumer sweep: switch all CLI files to canonical; flip `/config/team/presets` + `/config/team/validate`; flip `Config.Info.team`</title>
  <action>
    **Pre-flight audit**:
    1. `grep -rn "TeamConfig\|TeamRole\|TEAM_PRESETS\|TeamPreset" packages/opencode/src/ | grep -v migration.ts | grep -v "team/library" | grep -v "team/capabilities"` — authoritative consumer list.
    2. `grep -rn "\.capabilities\.includes\|\.capabilities\.some\|\.capabilities\.filter" packages/opencode/src/` — every capability string check. Each converts to a canonical enum value.
    3. `grep -rn "TEAM_PRESETS" packages/opencode/test/` — test-file audit (per Plan 02-01 Task 1 pre-flight record). Confirm it matches the audit in 02-01-SUMMARY.md.

    **Execution order** (iterative typecheck after each file swap; do NOT commit between steps within this task — the commit boundary is AT THE END of this task):

    a. **`team/index.ts` (barrel)** first:
       - Remove re-exports of legacy `TeamConfig`, `TeamRole`, `TeamRouting`, `TEAM_PRESETS`, `TeamPreset`
       - Add re-exports from `./migration` (`fromLegacyTeamConfig`, `migrateLegacyTeamConfig`, `migrateLegacyTeamConfigFile`, `LegacyMigrationIssue`, `LegacyMigrationResult`) — **NOT** `LegacyParseTeamConfig` (that remains internal/@internal)
       - Add re-exports from `./quickstarts` (`loadQuickstartTemplates`, `getQuickstart`, `QUICKSTART_IDS`, `QuickstartTemplate`, `QuickstartId`)
       - Keep canonical exports (`CanonicalTeamConfig`, `CanonicalTeamRole`, `CanonicalTeamRouting`, `CanonicalPosition`, `CanonicalCapability`, `POSITION_LIBRARY`, `STAGE_CAPABILITY_REQUIREMENTS`, `ReactionRule`, `EffortLevel`)

    b. **`server/routes/config.ts`**:
       - Line 13-14: remove legacy imports; add `import { CanonicalTeamConfig } from "../../devilcode/team/config"` + `import { loadQuickstartTemplates, type QuickstartTemplate } from "../../devilcode/team/quickstarts"`
       - Line 68 (`/config/team/validate`): route path unchanged; handler swaps `TeamConfig.safeParse(payload)` → `CanonicalTeamConfig.safeParse(payload)`. Error response shape unchanged.
       - Line 100 (`/config/team/presets`): route path unchanged; handler body: `return c.json(Object.values(loadQuickstartTemplates()))`. Response is `QuickstartTemplate[]`.
       - If OpenAPI response schema is declared inline (Hono OpenAPI helper), update the citation from `TeamPreset` → `QuickstartTemplate`. This changes the `packages/opencode/openapi.json` generated at build time — but since we are NOT running `script/generate.ts`, the `packages/sdk/js/src/v2/gen/` tree does not update. Document: "server OpenAPI spec drifts from SDK types until Phase 9 regens."

    c. **`config/config.ts:27, 1499`**:
       - Line 27: `import { TeamConfig } from "@/devilcode/team/config"` → `import { CanonicalTeamConfig } from "@/devilcode/team/config"`
       - Line 1499: `team: TeamConfig.optional()` → `team: CanonicalTeamConfig.optional()`
       - Run `bun run typecheck` immediately — expect remaining downstream failures; continue through step (d)-(m).

    d. **`devilcode/team/router.ts`** — canonical import swap. Convert capability string matches.

    e. **`devilcode/team/agents.ts`** — canonical import swap. `createWorkflowAgents` tier mapping unchanged. Run `bun test test/kilocode/team/workflow-integration.test.ts` to verify runtime post-swap.

    f. **`devilcode/workflow/build-runner.ts`**, **`escalation.ts`**, **`reviewer.ts`** — canonical import swap. Audit every `role.capabilities.includes(...)`:
       - `"coding"` → `"implementation"`
       - `"code-review"`, `"risk-analysis"` → `"review"`
       - `"ci"` → `"release"`
       - `"tests"` → `"testing"`
       - `"lookup"`, `"search"`, `"synthesis"`, `"analysis"`, `"long-form"`, `"triage"` → `"research"`
       - `"design"`, `"coordination"` → `"design"`
       - `"planning"` unchanged
       Supplementary capability checks: if consumer specifically needs to match a LEGACY string like `"summaries"` or `"accessibility"` that has no canonical mapping, check `role.supplementaryCapabilities.includes(...)` instead.

    g. **`devilcode/workflow-tui/context.tsx`**, **`orchestrator.ts`** — canonical import swap. No behavior change.

    h. **`devilcode/workflow-tui/command-input.tsx:48`** — `TeamConfig.safeParse` → `CanonicalTeamConfig.safeParse`.

    i. **`devilcode/team/capabilities.ts`** — verify no legacy imports leak. Should be clean from Phase 1.

    j. **Test-file consumer fixup** — for each test file in Plan 02-01's audit that imports `TEAM_PRESETS`:
       - If it uses `TEAM_PRESETS[i].team` as a `TeamConfig` parse fixture: inline the raw object literal (copy from the current `presets.ts` contents). Update the import from `@/devilcode/team/presets` to nothing (remove).
       - If it uses `TEAM_PRESETS[i].team` as input to `fromLegacyTeamConfig`: the fixture is a legacy shape; parse via `LegacyParseTeamConfig.parse(rawObject)` imported from `@/devilcode/team/migration`. Update import.
       - If the test is about preset correctness (legacy-shape-only): delete the test. Phase 1 legacy regression tests are already gone (Plan 02-01 Task 3 Part A); this catches stragglers.
       Specific test files (per Plan 02-01 audit):
       - `test/kilocode/team/migration.test.ts` — ALREADY uses inlined fixtures (Plan 02-01 Task 3 Part A step 3 did the inlining). No TEAM_PRESETS ref remaining. No Plan 02-02 edit needed.
       - `test/kilocode/team/canonical-config.test.ts` — slimmed by Plan 02-01; surviving tests are canonical-only. Audit for any remaining `TEAM_PRESETS` refs (should be zero); inline or delete any strays.
       - `test/kilocode/team/router.test.ts` — canonical conversion; inline any `TEAM_PRESETS` fixtures as canonical quickstart calls or as raw canonical objects
       - `test/kilocode/team/config.test.ts` — canonical conversion OR delete if it's legacy-shape-specific
       - `test/kilocode/team/workflow-integration.test.ts` — canonical conversion

    k. **Iterative typecheck**: after each file swap, `cd packages/opencode && bun run typecheck`. Errors localize to unfixed consumers; task complete for this step when typecheck is CLEAN monorepo-wide.

    l. **Full team test suite** run: `cd packages/opencode && bun test test/kilocode/team/` — all tests pass.

    m. **COMMIT BOUNDARY ALLOWED** (critique N4): if the reviewer wishes to checkpoint here, commit the consumer swap with message `feat(cli): switch team consumers + server routes to canonical types (legacy types still alive)`. This is an optional checkpoint — the working tree is consistent and the entire CLI runs on canonical types, but `presets.ts` and legacy `TeamRole`/`TeamConfig`/`TeamRouting` still exist as dead code. Task 2 deletes them. If no checkpoint desired, proceed straight to Task 2 without commit.

    Formatting: Prettier 120/no-semi on every edited file.
  </action>
  <verify>
    - `bun turbo typecheck` clean across monorepo INCLUDING `packages/devil-vscode` (stale SDK types still compile)
    - `cd packages/opencode && bun run typecheck` clean
    - `grep -rn "import.*\bTeamConfig[^a-zA-Z]\|import.*\bTeamRole[^a-zA-Z]\|TEAM_PRESETS" packages/opencode/src/ | grep -v migration.ts | grep -v config.ts | grep -v "team/index.ts"` — returns nothing
    - `bun test test/kilocode/team/` — clean
    - `bun test test/kilocode/team/workflow-integration.test.ts` — clean (tier-mapping runtime behavior preserved)
    - `grep -rn "\.capabilities\.includes" packages/opencode/src/devilcode/` — every surviving call uses a canonical capability value (enum values only)
    - Server route `GET /config/team/presets` handler returns `QuickstartTemplate[]` (verify by reading handler)
    - Server route `POST /config/team/validate` calls `CanonicalTeamConfig.safeParse`
    - `config/config.ts:1499` uses `CanonicalTeamConfig.optional()`
    - `command-input.tsx:48` uses `CanonicalTeamConfig.safeParse`
    - No changes in `packages/sdk/js/src/v2/gen/` — `git diff packages/sdk/` is empty
    - No changes in `packages/devil-vscode/` — `git diff packages/devil-vscode/` is empty
    - `cd packages/devil-vscode && bun run knip && bun run format:check && bun run check-kilocode-change` — all clean
  </verify>
  <done>Every CLI consumer + server endpoint switched to canonical; typecheck + team tests clean; legacy types still live in `config.ts` + `presets.ts` as dead code (Task 2 deletes); OPTIONAL commit checkpoint available.</done>
</task>

<task id="02-02-T2">
  <title>Delete legacy `presets.ts`, legacy `TeamRole`/`TeamConfig`/`TeamRouting` from `config.ts`, and temporary `fromLegacyTeamConfig` re-export from `config.ts`</title>
  <action>
    **Pre-flight**:
    1. Confirm Task 1 left the working tree typecheck-clean. Run `bun turbo typecheck` — must be clean.
    2. Re-run Task 1 (a) grep. Any surviving consumer beyond `migration.ts` + `config.ts` + `team/index.ts` (barrel) is a blocker; return to Task 1 (j) (test files) and fix.

    **Execution**:

    a. **Delete** `packages/opencode/src/devilcode/team/presets.ts`:
       ```
       git rm packages/opencode/src/devilcode/team/presets.ts
       ```

    b. **Edit** `packages/opencode/src/devilcode/team/config.ts`:
       - Delete legacy `TeamRole` Zod schema + type export
       - Delete legacy `TeamConfig` Zod schema + type export + 4 legacy refinements (Phase 1 pre-existing)
       - Delete legacy `TeamRouting` Zod schema + type export
       - Delete the temporary `export { fromLegacyTeamConfig, type LegacyMigrationIssue, type LegacyMigrationResult } from "./migration"` re-export (added in Plan 02-01 Task 1 (i))
       - Keep: `ReactionRule` (canonical config references it), `EffortLevel` re-import from `./effort`, all canonical types (`CanonicalTeamRole`, `CanonicalTeamRouting`, `CanonicalTeamConfig`)
       - Expected `config.ts` shape after this edit: canonical-only exports + `ReactionRule`. Target LOC: ~150-200.

    c. **Edit** `packages/opencode/src/devilcode/team/index.ts` final pass:
       - Verify no `TeamConfig`/`TeamRole`/`TeamRouting`/`TEAM_PRESETS`/`TeamPreset` re-exports remain
       - Barrel exports ONLY: canonical types + migration API + quickstart API + library + capabilities + `ReactionRule` + `EffortLevel` + router + agents

    d. **CI hygiene**:
       - `cd packages/devil-vscode && bun run knip` — confirm no dead exports surfaced. If knip flags unused exports elsewhere (e.g., webview-ui `TeamRoleConfig` still referenced by existing webview code — expected still-in-use), address any genuine new dead exports; ignore expected webview legacy references.
       - `cd packages/devil-vscode && bun run format:check` — clean
       - `cd packages/devil-vscode && bun run check-kilocode-change` — clean

    e. Format + diff:
       ```
       git diff --stat
       ```
       Confirmed: `presets.ts` deletion + `config.ts` significant LOC reduction. No other unexpected file changes.

    f. Full test pass:
       ```
       cd packages/opencode && bun test
       ```
       Only pre-existing `remote-ws.test.ts` + `remote-sender.test.ts` failures remain. No new failures.

    g. Monorepo typecheck:
       ```
       bun turbo typecheck
       ```
       Clean — INCLUDING `packages/devil-vscode` (stale SDK types still resolve).
  </action>
  <verify>
    - `packages/opencode/src/devilcode/team/presets.ts` DOES NOT EXIST
    - `team/config.ts` no longer exports `TeamRole`, `TeamConfig`, `TeamRouting`, nor contains `fromLegacyTeamConfig` re-export:
      - `grep -n "^export const TeamRole\|^export const TeamConfig\|^export const TeamRouting" packages/opencode/src/devilcode/team/config.ts` — empty
      - `grep -n "fromLegacyTeamConfig" packages/opencode/src/devilcode/team/config.ts` — empty
    - Canonical exports intact: `grep -n "CanonicalTeamConfig\|CanonicalTeamRole\|CanonicalTeamRouting\|ReactionRule" packages/opencode/src/devilcode/team/config.ts` — all four present
    - `bun turbo typecheck` clean across monorepo
    - `cd packages/opencode && bun test test/kilocode/team/` — clean
    - `cd packages/opencode && bun test` — only 2 pre-existing kilo-sessions failures
    - Knip + format:check + check-kilocode-change clean
    - `git diff packages/sdk/` empty (SDK NOT regenerated — critical)
    - `git diff packages/devil-vscode/` empty
  </verify>
  <done>Legacy code fully deleted; canonical-only CLI source tree; full test + typecheck sweep green; barrel clean; SDK untouched; devil-vscode compiles against stale SDK types.</done>
</task>

<task id="02-02-T3">
  <title>Rework `/team init` as quickstart picker via `Config.update`; publish migration doc; final verification</title>
  <action>
    **Part A — `/team init` rework**

    Edit `packages/opencode/src/devilcode/workflow-commands.tsx`:

    a. Imports:
       ```
       import { Config } from "@/config/config"
       import { loadQuickstartTemplates, QUICKSTART_IDS } from "./team"
       ```
       Remove any legacy `TeamConfig`/`TEAM_PRESETS` imports (should already be gone from Task 1).

    b. Keep `workflow.open` command unchanged (`/team` alias).

    c. Rework `workflow.init` onSelect + add per-quickstart sub-commands:

       ```
       const templates = loadQuickstartTemplates()

       command.register(() => [
         ...QUICKSTART_IDS.map((id) => {
           const template = templates[id]
           return {
             value: `workflow.init.${id}`,
             title: `Initialize Workflow — ${template.name}`,
             description: template.description,
             category: "Workflow Init",
             slash: { name: `team init ${id}` },
             onSelect: async () => {
               try {
                 const directory = sdk.directory!
                 const manager = new WorkflowStateManager(directory)
                 if (await manager.hasWorkflow()) {
                   toast.show({
                     message: "Workflow already initialized. Use /team to open.",
                     variant: "info",
                     duration: 3000,
                   })
                   route.navigate({ type: "workflow" })
                   return
                 }
                 const projectName = directory.split(/[/\\]/).pop() ?? "project"
                 // Write quickstart team to user Config BEFORE initializing workflow —
                 // command-input.tsx:48 reads team from Config.get().team.
                 // Config.update(config: Info) requires a full Info object (deep-merged
                 // at runtime via mergeConfig). Read-then-merge pattern keeps tsgo happy.
                 const current = await Config.get()
                 await Config.update({ ...current, team: template.team })
                 // Config.update calls Instance.dispose() at the end (config.ts:1681).
                 // The next Config.get() / manager.initialize() reads fresh Instance state.
                 await manager.initialize(projectName)
                 toast.show({
                   message: `Initialized with ${template.name}. Opening dashboard...`,
                   variant: "success",
                   duration: 3000,
                 })
                 route.navigate({ type: "workflow" })
               } catch (e: any) {
                 toast.show({
                   message: `Failed to initialize: ${e.message ?? String(e)}`,
                   variant: "error",
                   duration: 5000,
                 })
               }
             },
           }
         }),
         {
           value: "workflow.init",
           title: "Initialize Team Workflow",
           description: "Pick a quickstart: solo-enhanced, code-review-pair, full-stack-team, ci-cd-pipeline, research-team",
           category: "Workflow",
           slash: { name: "team init" },
           onSelect: async () => {
             toast.show({
               message: `Pick a quickstart: ${QUICKSTART_IDS.join(" | ")}. Run /team init <id>.`,
               variant: "info",
               duration: 5000,
             })
           },
         },
       ])
       ```

       Hoist `const templates = loadQuickstartTemplates()` outside the `.map` (critique N1: avoid hot-path re-loads).

    d. Verified `Config.update` signature (from `packages/opencode/src/config/config.ts:1677`):
       - Actual signature: `export async function update(config: Info)` — takes FULL `Info`, NOT a partial.
       - Runtime merges via `mergeConfig(existing, config)` at line 1680 — so passing a fully-populated Info works; passing just `{team: ...}` would typecheck-fail even though runtime merge would accept it.
       - Correct invocation: `const current = await Config.get(); await Config.update({ ...current, team: template.team })` (read-then-merge pattern shown in the onSelect snippet above).
       - `Config.update` also calls `await Instance.dispose()` at line 1681 — disposing the AsyncLocalStorage state. Subsequent `Config.get()` re-reads fresh state. `WorkflowStateManager.initialize(projectName)` initializes its own state separately. The smoke test in Part D verifies the team is reachable after init.
       - If a future refactor exposes `Config.updatePartial(patch: Partial<Info>)`, prefer that. For Phase 2: keep the read-then-merge pattern to avoid a shared-code refactor.

    **Part B — String copy updates** (NO layout edits):

    - `workflow-tui/detail-panel.tsx:17` — change body string to:
      `"Run /team init <quickstart> to start. Available: solo-enhanced, code-review-pair, full-stack-team, ci-cd-pipeline, research-team."`
      Do NOT touch the surrounding layout or `<text wrapMode="word" width="100%">` at lines 113-115. That rendering bug is Phase 5's concern.
    - `workflow-tui/status-bar.tsx:24` — change message to:
      `"No workflow initialized. Run /team init <quickstart> to start."`
    - `workflow-tui/command-input.tsx:91` — change error message to:
      `"No workflow initialized. Run /team init <quickstart> first."`

    **Part C — Migration documentation**

    Create `packages/devil-docs/pages/collaborate/teams/migration-v1.md`:

    ```markdown
    # Team Orchestrator — Migration Guide (v1)

    Devil Code v1 introduces the Team Orchestrator. The previous
    `/team init` flow and the 5 hardcoded team presets have been
    replaced by the canonical 11-position library and 5 bundled
    JSON quickstart templates.

    ## Quick migration paths

    ### No custom team config → use a quickstart

    Run `/team init <quickstart>` in the TUI. The 5 quickstarts map
    to the old presets:

    | Legacy preset | v1 quickstart |
    |---|---|
    | `Solo Enhanced` | `solo-enhanced` |
    | `Code Review Pair` | `code-review-pair` |
    | `Full Stack Team` | `full-stack-team` |
    | `CI/CD Pipeline` | `ci-cd-pipeline` |
    | `Research Team` | `research-team` |

    ### Custom `TeamConfig` in `kilo.json` → manual migration

    v1 removes the legacy `TeamConfig` shape from `kilo.json`. Open your
    `kilo.json` and apply the migration tool via a one-off Node script:

    ```ts
    // migrate.ts
    import { migrateLegacyTeamConfigFile } from "@devilcode/opencode/devilcode/team/migration"
    import { readFileSync, writeFileSync } from "node:fs"

    const src = process.argv[2]  // e.g., ./legacy-kilo.json
    const dst = process.argv[3]  // e.g., ./kilo.json

    const legacyFull = JSON.parse(readFileSync(src, "utf8"))
    const legacyTeam = legacyFull.team
    writeFileSync("/tmp/team-legacy.json", JSON.stringify(legacyTeam))

    const result = migrateLegacyTeamConfigFile("/tmp/team-legacy.json")
    if (!result.ok) {
      console.error("Migration failed:", result.errors)
      process.exit(1)
    }
    legacyFull.team = result.value
    writeFileSync(dst, JSON.stringify(legacyFull, null, 2))
    console.log("Migrated with warnings:", result.warnings)
    ```

    Run: `bun run migrate.ts ./legacy-kilo.json ./kilo.json`

    A CLI subcommand (`kilo team migrate`) is planned for a future
    release — see the Team Orchestrator roadmap.

    ## Role-key → canonical-position mapping

    | Legacy key | Canonical position |
    |---|---|
    | `lead` | `senior-dev` |
    | `coder` | `developer` |
    | `implementer` | `developer` |
    | `frontend-dev` | `frontend-specialist` |
    | `backend-dev` | `backend-specialist` |
    | `release`, `ci-fixer` | `release-engineer` |
    | `orchestrator` | `coordinator` |
    | `deep-researcher`, `fast-scanner`, `research` | `researcher` |
    | `reviewer` | `reviewer` |
    | `architect` | `architect` |

    ## Capability-string → canonical mapping

    | Legacy | Canonical |
    |---|---|
    | `coding` | `implementation` |
    | `code-review`, `risk-analysis` | `review` |
    | `ci`, `release` | `release` |
    | `tests` | `testing` |
    | `lookup`, `search`, `synthesis`, `analysis`, `long-form`, `triage` | `research` |
    | `design`, `coordination` | `design` |
    | `planning` | `planning` (unchanged) |

    Unknown capabilities land in `supplementaryCapabilities` with a
    migration warning.

    ## Tier semantics changed

    Canonical roles have library-fixed tiers:
    - Tier 1: architect, coordinator, senior-dev
    - Tier 2: developer, frontend-specialist, backend-specialist,
              reviewer, qa-tester, release-engineer, spec-writer
    - Tier 3: researcher

    A legacy role at `tier: 1` that maps to `developer` becomes tier 2
    after migration — the agent pool reassigns it from primary to
    subagent. If you want tier-1 behavior, manually set the
    `positionId` to `senior-dev` instead of accepting the default
    `developer` mapping.

    ## 7-stage coverage requirement

    A canonical team must cover every workflow stage's required
    capability. If `/team init` rejects your migrated team, add roles
    or capabilities until every stage is covered. The error message
    enumerates every missing `stage(capability)` pair.

    ## VS Code extension

    The extension's preset list will appear empty or log a Zod parse
    error in v1.0 because the extension still consumes the legacy
    preset shape. Extension parity ships in v1.9 (Phase 9). Use the
    CLI TUI in the interim.
    ```

    Check `packages/devil-docs/pages/collaborate/teams/*.md` for sibling files and match the navigation index (usually `_meta.json` or an index page). If the docs tree uses an index, add an entry.

    **Part D — Final verification sweep**

    ```
    bun turbo typecheck                                                      # clean monorepo
    cd packages/opencode && bun run typecheck                                # clean
    cd packages/opencode && bun test test/kilocode/team/                     # clean
    cd packages/opencode && bun test                                         # only 2 pre-existing kilo-sessions failures
    cd packages/devil-vscode && bun run format:check                         # clean
    cd packages/devil-vscode && bun run knip                                 # clean
    cd packages/devil-vscode && bun run check-kilocode-change                # clean
    bun run script/extract-source-links.ts                                   # no diff to source-links.md
    ```

    Manual smoke test:
    ```
    cd packages/opencode && bun run dev .
    # In TUI: /team init solo-enhanced
    # Assert: toast "Initialized with Solo Enhanced. Opening dashboard..."
    # Assert: /team opens dashboard without console error
    # Assert: no ExtensionBoundaryError or Zod parse throw from command-input.tsx:48
    ```

    Server-side smoke test:
    ```
    curl -s "http://localhost:<port>/config/team/presets" -H "x-password: $KILO_SERVER_PASSWORD" | jq 'length'
    # Expect: 5
    curl -s "http://localhost:<port>/config/team/presets" -H "x-password: $KILO_SERVER_PASSWORD" | jq '.[0].team.roles | keys'
    # Expect: canonical position ids (e.g., "senior-dev", "coordinator", ...)
    ```

    **Part E — Summary file**

    Write `.planning/phases/02-preset-migration/02-02-SUMMARY.md`:
    - Consumer swap list with before/after symbol per file (15 files + 3 test files)
    - `config.ts` size delta (LOC before Plan 02-01 +288 → LOC after this plan)
    - `presets.ts` deletion confirmation
    - Server route flips: request/response JSON sample for `/config/team/presets` + `/config/team/validate`
    - `Config.Info.team` schema flip confirmation (config/config.ts:1499)
    - `/team init` sub-commands registered + behavior
    - Copy updates at 3 sites (line numbers quoted; NO layout changes)
    - Migration doc publication URL
    - Removed from Phase 1: 10 migration tests + 2 legacy regression tests = 12 tests
    - Full test counts after Phase 2: total pass/fail, new failures (expected 0)
    - CI matrix: 8 checks with exit status
    - SDK NOT regenerated — `git diff packages/sdk/` empty
    - Devil-vscode — `git diff packages/devil-vscode/` empty; typecheck clean; webview runtime break expected
    - Extension IPC break confirmation: curl `/config/team/presets` returns canonical JSON → webview Zod parse error
    - Phase 3 handoff: canonical-only source tree; TUI primitives can assume canonical shape
  </action>
  <verify>
    - `/team init <quickstart>` initializes the workflow; toast confirms; `Config.get().team` reflects quickstart team
    - `/team init` (no arg) surfaces quickstart options in toast
    - Three onboarding copy sites updated (detail-panel line 17 ONLY, not 113-115)
    - Migration doc file exists; `cd packages/devil-docs && bun run build` (if local docs build supported) succeeds
    - Full CI sweep clean: 8 checks all pass
    - Manual smoke: `bun run dev .` + `/team init solo-enhanced` works without runtime error
    - `git diff packages/sdk/` empty; `git diff packages/devil-vscode/` empty
    - Summary file written with full handoff
    - `git diff --stat` shows expected file list: 15+ modified, 1 deleted (`presets.ts`), 1 created (`migration-v1.md`)
  </verify>
  <done>`/team init` quickstart picker via `Config.update` live; migration doc published; final verification sweep clean; Phase 2 complete; SDK untouched; Phase 3 can start.</done>
</task>

</tasks>

<verification>
Full sweep (all must exit 0 unless noted):

```
cd packages/opencode && bun run typecheck
cd packages/opencode && bun test test/kilocode/team/
cd packages/opencode && bun test                            # only 2 pre-existing kilo-sessions failures
bun turbo typecheck                                          # monorepo-wide INCLUDING devil-vscode
cd packages/devil-vscode && bun run knip
cd packages/devil-vscode && bun run format:check
cd packages/devil-vscode && bun run check-kilocode-change
bun run script/extract-source-links.ts                       # no diff
```

Diff checks:
- `git diff --stat --diff-filter=D` — lists only `packages/opencode/src/devilcode/team/presets.ts`
- `git diff --stat --diff-filter=A` — lists `packages/devil-docs/pages/collaborate/teams/migration-v1.md`
- `grep -rn "TeamConfig[^a-zA-Z]\|TeamRole[^a-zA-Z]\|TEAM_PRESETS\|TeamPreset" packages/opencode/src/ | grep -v "migration.ts" | grep -v "CanonicalTeam" | grep -v "LegacyParseTeamConfig\|LegacyMigrationIssue\|LegacyMigrationResult"` — empty
- `grep -rn "fromLegacyTeamConfig" packages/opencode/src/ | grep -v "migration.ts" | grep -v "team/index.ts"` — empty
- `git diff packages/sdk/` — empty (SDK not regenerated)
- `git diff packages/devil-vscode/` — empty (no extension edits)

Manual smoke:
- `bun run dev .` → TUI → `/team init solo-enhanced` → workflow initialized → `/team` → dashboard renders
- `curl http://localhost:<port>/config/team/presets -H "x-password: <...>"` returns `QuickstartTemplate[]` with 5 entries; each `team` field is a full `CanonicalTeamConfig`

Expected extension-break confirmation:
- VS Code extension loaded against this CLI build; webview Agent Manager preset list logs Zod parse error in DevTools console. Documented as expected Phase 9 fix.
</verification>

<success_criteria>
- `presets.ts` deleted
- `config.ts` shrunk to canonical-only + `ReactionRule`; legacy `TeamRole`, `TeamConfig`, `TeamRouting`, `fromLegacyTeamConfig` re-export all gone
- `team/index.ts` barrel exports only canonical + migration + quickstart + library + capabilities + `ReactionRule` + `EffortLevel`
- Every CLI consumer file (12 source files) imports only canonical types
- Test files (3 in `files_modified_conditionally`) converted to inlined legacy fixtures via `LegacyParseTeamConfig` or deleted
- `server/routes/config.ts` `GET /config/team/presets` returns `QuickstartTemplate[]`; `POST /config/team/validate` accepts canonical
- `config/config.ts:1499` uses `CanonicalTeamConfig.optional()`
- `command-input.tsx:48` uses `CanonicalTeamConfig.safeParse`
- `/team init <quickstart>` writes `Config.update({team: quickstart.team})` and initializes workflow
- `/team init` alone lists quickstart options in toast
- 3 onboarding copy sites updated (line numbers preserved; NO layout edits)
- Migration guide published at `packages/devil-docs/pages/collaborate/teams/migration-v1.md`
- Full CI sweep clean: typecheck (monorepo), team tests, knip, format:check, check-kilocode-change, source-links
- SDK NOT regenerated: `git diff packages/sdk/` empty
- Devil-vscode NOT edited: `git diff packages/devil-vscode/` empty; devil-vscode typecheck clean
- Manual smoke: `/team init solo-enhanced` + dashboard works
- Extension IPC break confirmed (expected runtime outcome)
- `.planning/phases/02-preset-migration/02-02-SUMMARY.md` written
</success_criteria>

<output>
Write plan summary to `.planning/phases/02-preset-migration/02-02-SUMMARY.md`:
- Consumer swap audit: 15 source files + 3 test files × before/after symbol table
- `config.ts` LOC before/after
- `presets.ts` deletion confirmation
- Server preset endpoint flip with sample request/response JSON (both routes: presets + validate)
- `Config.Info.team` field flip (config/config.ts:1499)
- `/team init` command list + sub-commands registered
- Copy updates at 3 sites (line numbers)
- Migration doc publication path
- Test count deltas: 12 tests removed from `canonical-config.test.ts`; total suite size after Phase 2
- CI matrix: 8 checks with exit status
- SDK untouched (intentional — Phase 9 owns regen)
- Devil-vscode untouched (intentional — Phase 9 owns webview)
- Extension IPC runtime break confirmation
- Critique AUTO_REFINE cycle 1 findings resolved: C1, C2, N2, N4, N5, INFO (pre-mortem); A1, A2, A3, A4 (assumption-hunt) — each with brief resolution note
- Phase 3 handoff: canonical-only source tree; TUI primitives ready to assume canonical
</output>

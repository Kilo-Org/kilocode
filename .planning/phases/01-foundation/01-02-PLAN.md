---
phase: 1
plan: 2
wave: 2
slug: position-library-and-canonical-team-types
depends_on: [01-01]
must_haves:
  - PH1-R1
  - PH1-R3
  - PH1-R6
agents:
  primary: senior-developer
  reviewer: qa-verification-specialist
estimated_complexity: medium
refine_cycle: 1
files_modified:
  - packages/opencode/src/devilcode/team/library.ts
  - packages/opencode/test/kilocode/team/library.test.ts
  - packages/opencode/src/devilcode/team/config.ts
  - packages/opencode/test/kilocode/team/canonical-config.test.ts
files_read_not_modified:
  - packages/opencode/src/devilcode/team/agents.ts
  - packages/opencode/src/devilcode/team/router.ts
  - packages/opencode/src/devilcode/team/presets.ts
  - packages/opencode/src/server/routes/config.ts
  - packages/devil-vscode/webview-ui/src/types/messages.ts
  - .planning/phases/01-foundation/01-reconciliation.md
  - .planning/phases/01-foundation/01-01-SUMMARY.md
---

<objective>
Ship the canonical 11-position library and introduce `CanonicalTeamRole` +
`CanonicalTeamConfig` as NEW exports in `team/config.ts` alongside the
existing legacy `TeamRole` / `TeamConfig` (which remain untouched).
Provide a `fromLegacyTeamConfig(input)` helper that maps legacy configs
to canonical form with a structured migration report. After this plan:

- Canonical types are available for new code (Phase 3+ TUI) to use
- Legacy types still power presets, server endpoints, and the VS Code
  extension IPC — nothing breaks
- Phase 2 owns the actual migration switch (presets, HTTP boundary,
  legacy type removal) using the helper this plan provides
</objective>

<context>
Phase 1 context: `.planning/phases/01-foundation/01-CONTEXT.md`
Spec: `.planning/specs/01-foundation-spec.md` (Deliverables updated for additive approach)
Prior plan outputs:
- `.planning/phases/01-foundation/01-reconciliation.md` — **READ FIRST; if `## REQUIRES USER DECISION` is non-empty, /legion:build must surface to user before this plan starts**
- `.planning/phases/01-foundation/01-01-SUMMARY.md` — headlines user-decision items

Dependencies from Plan 01-01 (already landed):
- `team/capabilities.ts` — `CanonicalCapability` (8-value enum) + `STAGE_CAPABILITY_REQUIREMENTS` + `requiredCapabilitiesFor`

Integration boundaries (why this plan is additive):
- `packages/devil-vscode/webview-ui/src/types/messages.ts:399-408` — parallel `TeamRoleConfig` interface with stringly-typed `capabilities: string[]`. Changing CLI's `TeamRole` mid-flight breaks 6 webview files until Phase 9 rewrites them.
- `packages/opencode/src/devilcode/team/presets.ts` — 5 presets declare stringly-typed `capabilities: ["..."]`. Schema change would fail their validation.
- `packages/opencode/src/server/routes/config.ts` — serves `TEAM_PRESETS` + `TeamConfig` state to extension.
- Phase 2's mandate: "Preset Migration & Clean-Break Schema Cleanup" — owns the migration switch.

Additive approach: Phase 1 adds new canonical types. Phase 2 flips the switch.

Canonical 11 positions (from exploration decision + spec):

| id | displayName | primaryCapability | tier | canonicalCapabilities | defaultCanDelegate |
|---|---|---|---|---|---|
| architect | Architect | planning | 1 | [planning, design] | [senior-dev, spec-writer, reviewer] |
| coordinator | Coordinator | planning | 1 | [planning, retrospective] | (all other 10) |
| spec-writer | Spec Writer | design | 2 | [design] | [] |
| senior-dev | Senior Developer | implementation | 1 | [implementation, design] | [developer, frontend-specialist, backend-specialist, reviewer] |
| developer | Developer | implementation | 2 | [implementation] | [reviewer] |
| frontend-specialist | Frontend Specialist | implementation | 2 | [implementation] | [reviewer] |
| backend-specialist | Backend Specialist | implementation | 2 | [implementation] | [reviewer] |
| reviewer | Reviewer | review | 2 | [review] | [] |
| qa-tester | QA Tester | testing | 2 | [review, testing] | [] |
| release-engineer | Release Engineer | release | 2 | [release] | [] |
| researcher | Researcher | research | 3 | [research] | [] |

Stage coverage expected from a minimal canonical team:
- plan/challenge → planning → {architect, coordinator}
- contract → design → {architect, spec-writer, senior-dev}
- build → implementation → {senior-dev, developer, frontend-specialist, backend-specialist}
- review → review → {reviewer, qa-tester}
- ship → release → release-engineer
- retro → retrospective → coordinator (only position with retrospective by default)

Coverage definition for PH1-R6 ("100% unit test coverage"): each new Zod
validator (enum, record, refine) has both positive and negative test
cases. No coverage-tool threshold enforced in CI; test coverage is
branch-by-case per the test list below.
</context>

<tasks>

<task id="01-02-T1">
  <title>Pre-flight + implement `team/library.ts`</title>
  <action>
    **Pre-flight (5 min)**:
    1. Read `.planning/phases/01-foundation/01-reconciliation.md` in full.
       If `## REQUIRES USER DECISION` is non-empty: STOP. Write
       `.planning/phases/01-foundation/ESCALATION.md` listing the items
       and the proposed resolutions. Do NOT proceed with code until
       /legion:build routes this to the user and returns with decisions
       recorded back into the reconciliation doc (section marked RESOLVED).
    2. Quick grep: `grep -n "role\.capabilities" packages/opencode/src/devilcode/team/` — expect only `agents.ts` and `presets.ts` references. If any other file surfaces, add it to this task's verification sweep.
    3. Quick grep: `grep -rn "TeamConfig\|TeamRole" packages/opencode/src/server/` — note every server-route consumer. Expected: `server/routes/config.ts`. If others: note in plan summary for Phase 2 pickup.

    **Implementation**:

    Create `packages/opencode/src/devilcode/team/library.ts`:

    a. Imports:
       ```
       import z from "zod"
       import { CanonicalCapability } from "./capabilities"
       ```

    b. `CanonicalPosition` — Zod enum (11 kebab-case values):
       `"architect","coordinator","spec-writer","senior-dev","developer","frontend-specialist","backend-specialist","reviewer","qa-tester","release-engineer","researcher"`

       Export the enum + `z.infer` type.

    c. `PositionLibraryEntry` Zod schema:
       ```
       z.object({
         id: CanonicalPosition,
         displayName: z.string().min(1),
         tier: z.number().int().positive(),
         primaryCapability: CanonicalCapability,
         canonicalCapabilities: z.array(CanonicalCapability).nonempty(),
         defaultCanDelegate: z.array(CanonicalPosition).default([]),
         description: z.string().min(1),
       }).refine(
         (e) => e.canonicalCapabilities.includes(e.primaryCapability),
         { message: "primaryCapability must be in canonicalCapabilities", path: ["primaryCapability"] }
       )
       ```

    d. `POSITION_LIBRARY: Record<z.infer<typeof CanonicalPosition>, z.infer<typeof PositionLibraryEntry>>`
       — const object with all 11 entries per the table in `<context>`.
       Each `description` is one user-facing sentence.

    e. Compile-time exhaustiveness:
       ```
       const _libExhaustive: Record<z.infer<typeof CanonicalPosition>, z.infer<typeof PositionLibraryEntry>> = POSITION_LIBRARY
       void _libExhaustive
       ```

    f. `POSITION_CAPABILITY_MAP: Readonly<Record<CanonicalPosition, CanonicalCapability>>`
       — derived at module load by extracting `primaryCapability` from
       each `POSITION_LIBRARY` entry. Wrap with `Object.freeze`.

    g. `getDefaultCanDelegate(id: CanonicalPosition): CanonicalPosition[]` —
       if `id === "coordinator"`, return all other 10 positions
       (`CanonicalPosition.options.filter(p => p !== "coordinator")`);
       otherwise return `POSITION_LIBRARY[id].defaultCanDelegate`.

    h. `validatePositionLibrary()` — zero-arg function that runs
       `PositionLibraryEntry.parse(entry)` for every entry in
       `POSITION_LIBRARY`. Intended to be called at the top of
       `library.test.ts` (and can be called from boot to fail loudly
       if the library is ever broken).

    i. JSDoc: single source of truth for canonical positions; additive
       to legacy `TeamRole`; Phase 2 consumers override.

    Formatting: Prettier 120/no-semi, match neighbors.

    Do NOT modify `config.ts` in this task.
  </action>
  <verify>
    - `bun turbo typecheck` clean
    - Pre-flight: if ESCALATION.md was written, document in summary (halt state, awaiting user)
    - Exports: `CanonicalPosition`, `PositionLibraryEntry`, `POSITION_LIBRARY`, `POSITION_CAPABILITY_MAP`, `getDefaultCanDelegate`, `validatePositionLibrary`
    - Removing one `POSITION_LIBRARY` entry locally produces TS error (revert after verifying)
    - `validatePositionLibrary()` run at module-load parses every entry without throwing
    - ~120-150 LOC
  </verify>
  <done>`library.ts` landed; 11 entries parse; typecheck clean; no consumer files modified.</done>
</task>

<task id="01-02-T2">
  <title>Add `CanonicalTeamRole` + `CanonicalTeamConfig` + `fromLegacyTeamConfig` to `team/config.ts` (additive)</title>
  <action>
    Modify `packages/opencode/src/devilcode/team/config.ts`. **All edits
    are ADDITIVE. Do NOT change existing `TeamRole`, `TeamConfig`, or
    their refinements.** Existing consumers (`agents.ts`, `router.ts`,
    `presets.ts`, `server/routes/config.ts`, devil-vscode IPC) must
    continue to compile and run.

    1. Add imports at top (keep existing imports):
       ```
       import { CanonicalCapability, STAGE_CAPABILITY_REQUIREMENTS } from "./capabilities"
       import { CanonicalPosition, POSITION_LIBRARY } from "./library"
       ```

    2. Add new exports AFTER the existing `TeamConfig` export:

       ```
       export const CanonicalTeamRole = z.object({
         displayName: z.string().min(1),
         positionId: CanonicalPosition,
         provider: z.string(),
         model: z.string(),
         effort: EffortLevel,
         tier: z.number().int().positive(),
         canDelegate: z.array(CanonicalPosition).default([]),
         maxConcurrent: z.number().int().positive().default(3),
         capabilities: z.array(CanonicalCapability).nonempty(),
         supplementaryCapabilities: z.array(z.string()).default([]),
       })
       export type CanonicalTeamRole = z.infer<typeof CanonicalTeamRole>

       export const CanonicalTeamRouting = z.object({
         strategy: z.enum(["hierarchical", "flat"]).default("hierarchical"),
         defaultRole: CanonicalPosition,
         escalationEnabled: z.boolean().default(true),
         parentRole: CanonicalPosition.optional(),
         reviewEscalationRole: CanonicalPosition.optional(),
       })
       export type CanonicalTeamRouting = z.infer<typeof CanonicalTeamRouting>

       export const CanonicalTeamConfig = z.object({
         enabled: z.boolean().default(false),
         roles: z.record(CanonicalPosition, CanonicalTeamRole),
         routing: CanonicalTeamRouting,
         reactions: z.array(ReactionRule).default([]).optional(),
       })
         .refine(
           (cfg) => !cfg.enabled || cfg.routing.defaultRole in cfg.roles,
           { message: "routing.defaultRole must reference an existing role", path: ["routing", "defaultRole"] }
         )
         .refine(
           (cfg) => !cfg.enabled || Object.values(cfg.roles).every((r) =>
             r.canDelegate.every((d) => d in cfg.roles)
           ),
           { message: "canDelegate entries must reference existing roles", path: ["roles"] }
         )
         .refine(
           (cfg) => !cfg.enabled || !cfg.routing.parentRole || cfg.routing.parentRole in cfg.roles,
           { message: "routing.parentRole must reference an existing role", path: ["routing", "parentRole"] }
         )
         .refine(
           (cfg) =>
             !cfg.enabled ||
             !cfg.routing.reviewEscalationRole ||
             cfg.routing.reviewEscalationRole in cfg.roles,
           { message: "routing.reviewEscalationRole must reference an existing role", path: ["routing", "reviewEscalationRole"] }
         )
         .superRefine((cfg, ctx) => {
           if (!cfg.enabled) return
           const missing: string[] = []
           for (const [stage, cap] of Object.entries(STAGE_CAPABILITY_REQUIREMENTS)) {
             const covered = Object.values(cfg.roles).some((r) => r.capabilities.includes(cap))
             if (!covered) missing.push(`${stage}(${cap})`)
           }
           if (missing.length > 0) {
             ctx.addIssue({
               code: z.ZodIssueCode.custom,
               message: `Team missing canonical capability coverage for stages: ${missing.join(", ")}`,
               path: ["roles"],
             })
           }
         })
       export type CanonicalTeamConfig = z.infer<typeof CanonicalTeamConfig>
       ```

       Use `.superRefine` for the coverage check so the `missing` list is
       computed once and emitted via `ctx.addIssue`. Single-pass,
       deterministic error ordering.

    3. Add migration helper:

       ```
       export type LegacyMigrationIssue =
         | { kind: "unknown-capability"; roleId: string; value: string }
         | { kind: "missing-position-id"; roleId: string; inferredFrom?: string }
         | { kind: "ambiguous-capability-mapping"; roleId: string; value: string; candidates: CanonicalCapability[] }

       export type LegacyMigrationResult =
         | { ok: true; value: CanonicalTeamConfig; warnings: LegacyMigrationIssue[] }
         | { ok: false; errors: LegacyMigrationIssue[]; warnings: LegacyMigrationIssue[] }

       export function fromLegacyTeamConfig(input: TeamConfig): LegacyMigrationResult
       ```

       Implementation outline:
       - For each legacy role:
         - Infer `positionId`:
           - If role key matches a `CanonicalPosition` value directly → use it
           - Else: use a hardcoded synonym table (see below)
           - Else: emit `missing-position-id` error with role ID
         - Map legacy `capabilities: string[]` → `{ canonicalCapabilities: CanonicalCapability[], supplementaryCapabilities: string[] }`:
           - Canonical-identity matches (e.g., "planning", "review", "research") → canonical
           - Synonym table: `coding`→`implementation`, `code-review`→`review`, `ci`→`release`, `tests`→`testing`, `lookup`/`search`/`synthesis`/`analysis`/`long-form`/`triage`→`research`, `design`/`coordination`→`design`, `ui`/`accessibility`/`api`/`db`→ (all map to `implementation` + original kept in supplementary), `risk-analysis`→`review`
           - Unknowns → emit `unknown-capability` warning, drop into `supplementaryCapabilities`
         - Map `canDelegate: string[]` → `CanonicalPosition[]` by filtering to known positions; unknown delegatees go to warnings
       - Result:
         - `ok: true` if every role yielded a `positionId` and at least one canonical capability, AND the produced `CanonicalTeamConfig` parses successfully
         - `ok: false` otherwise

       Synonym table for position-id inference (based on existing presets):
       ```
       lead            → senior-dev
       coder           → developer
       implementer     → developer
       ci-fixer        → release-engineer
       release         → release-engineer
       frontend-dev    → frontend-specialist
       backend-dev     → backend-specialist
       orchestrator    → coordinator
       deep-researcher → researcher
       fast-scanner    → researcher
       research        → researcher
       reviewer        → reviewer
       architect       → architect
       ```

       (Phase 2 test suite will assert every legacy preset migrates cleanly.)

    4. Formatting: Prettier 120/no-semi, match neighbors.

    5. Do NOT touch `presets.ts`, `agents.ts`, `router.ts`, `server/routes/config.ts`.
  </action>
  <verify>
    - `bun turbo typecheck` clean across monorepo
    - `cd packages/opencode && bun run typecheck` clean
    - Legacy `TeamRole` and `TeamConfig` exports unchanged (diff-check: neither removed nor rewritten); all 4 existing refinements on `TeamConfig` preserved byte-for-byte
    - New exports present: `CanonicalTeamRole`, `CanonicalTeamRouting`, `CanonicalTeamConfig`, `fromLegacyTeamConfig`, `LegacyMigrationIssue`, `LegacyMigrationResult`
    - Devil-vscode (`packages/devil-vscode/webview-ui/src/types/messages.ts`) untouched, still compiles
    - No presets moved/commented/deleted; `TEAM_PRESETS` still exports the original 5
  </verify>
  <done>Canonical types shipped additively; legacy types intact; migration helper
  available for Phase 2; typecheck clean monorepo-wide.</done>
</task>

<task id="01-02-T3">
  <title>Tests: `library.test.ts` + `canonical-config.test.ts` + full verification sweep</title>
  <action>
    **Part A — `packages/opencode/test/kilocode/team/library.test.ts`**

    Under `describe("team library")`:
    1. `POSITION_LIBRARY has exactly 11 entries` — `Object.keys(POSITION_LIBRARY).length === 11`; keys sorted-equal to `CanonicalPosition.options.slice().sort()`.
    2. `each entry's id equals its key`.
    3. `validatePositionLibrary() succeeds for all entries` (runs at top via `beforeAll`).
    4. `every canonicalCapabilities array is non-empty and canonical`.
    5. `every primaryCapability is in canonicalCapabilities`.
    6. `Coordinator carries planning AND retrospective` (regression guard for exploration decision).
    7. `only Coordinator carries retrospective by default` — exactly one library entry lists `retrospective`, and it's `coordinator`.
    8. `POSITION_CAPABILITY_MAP maps every position` + each value is a `CanonicalCapability`.
    9. `defaultCanDelegate entries (non-coordinator) all exist in library`.
    10. `getDefaultCanDelegate("coordinator") returns 10 other positions` (length 10, no self).
    11. `getDefaultCanDelegate("spec-writer") returns empty array`.

    **Part B — `packages/opencode/test/kilocode/team/canonical-config.test.ts`** (NEW FILE; does not touch legacy `config.test.ts`)

    Under `describe("CanonicalTeamConfig strict stage coverage")`:

    Helpers at top:
    ```
    function buildValidCanonicalTeam(): CanonicalTeamConfig  // 7 stages covered, roles keyed by positionId
    function buildCanonicalTeamMissing(cap: CanonicalCapability): unknown
    ```

    Tests:
    12. `valid canonical team parses successfully` (positive).
    13. `test.each` over `Object.entries(STAGE_CAPABILITY_REQUIREMENTS)`: removing the required capability causes parse failure AND error message contains stage name + capability name.
    14. `team missing both retrospective and release reports BOTH in one error` — assert message includes `"retro(retrospective)"` AND `"ship(release)"`.
    15. `canonical role with non-canonical capability value is rejected at role level` (not at coverage refinement).
    16. `supplementaryCapabilities round-trip` — parse + compare preserves the field.
    17. `positionId required on every canonical role`.
    18. `enabled:false canonical team skips coverage refinement` — missing all capabilities but `enabled:false` parses fine.

    Under `describe("fromLegacyTeamConfig migration helper")`:
    19. `migrates solo-enhanced preset` — pass the legacy `TEAM_PRESETS[0].team`; assert `ok: true`, resulting config parses, all roles have `positionId` + canonical capabilities, warnings enumerated.
    20. `migrates code-review-pair preset` — same pattern.
    21. `migrates full-stack-team preset` — 4 roles, assert architect/frontend-dev/backend-dev/reviewer all map correctly.
    22. `migrates ci-cd-pipeline preset` — 3 roles including release-engineer.
    23. `migrates research-team preset` — orchestrator→coordinator, deep-researcher→researcher, fast-scanner→researcher.
    24. `unknown role key emits missing-position-id error and ok:false`.
    25. `unknown capability value emits warning and lands in supplementaryCapabilities`.
    26. `migration output for each preset passes CanonicalTeamConfig.safeParse` (round-trip integrity).

    Under `describe("Legacy types unchanged after Phase 1")`:
    27. `TeamRole still accepts stringly-typed capabilities` — `TeamRole.safeParse({ ..., capabilities: ["coding", "ci"] }).success === true` (positive regression).
    28. `TeamConfig parse of existing TEAM_PRESETS[0].team succeeds` — asserts legacy path is intact for existing consumers.

    **Part C — Full verification sweep**

    Run from repo root + per-package:
    ```
    cd packages/opencode && bun test test/kilocode/team/      # new + existing team tests
    cd packages/opencode && bun test                            # full CLI suite (no regressions)
    bun turbo typecheck                                         # monorepo clean
    cd packages/devil-vscode && bun run format:check            # clean
    cd packages/devil-vscode && bun run knip                    # no dead exports
    cd packages/devil-vscode && bun run check-kilocode-change   # no stale markers
    ```

    All six must exit 0. No preset file was modified, so server route
    `GET /config/presets` continues to return the 5 legacy presets.

    Smoke test the server-facing path (manual, optional): the existing
    `test/kilocode/team/workflow-integration.test.ts` already covers
    `createWorkflowAgents` against legacy `TeamConfig`. Run it; confirm
    pass unchanged.
  </action>
  <verify>
    - All library tests pass (11 cases)
    - All canonical-config tests pass (17 cases: 7 coverage + 8 migration + 2 legacy-regression)
    - Existing team tests unchanged and still pass (concurrency, config, router, types, workflow-integration)
    - `bun turbo typecheck` clean
    - Full CLI `bun test` green
    - Knip + format:check + check-kilocode-change clean
    - Legacy `TeamConfig.parse(TEAM_PRESETS[0].team)` succeeds (legacy test 28)
  </verify>
  <done>Full canonical-type + migration-helper coverage landed; legacy path
  verified intact; /legion:build ready to proceed to plan critique
  cycle 2 (or PASS).</done>
</task>

</tasks>

<verification>
Full sweep (must all exit 0):

```
cd packages/opencode && bun run typecheck
cd packages/opencode && bun test
bun turbo typecheck
cd packages/devil-vscode && bun run knip
cd packages/devil-vscode && bun run format:check
cd packages/devil-vscode && bun run check-kilocode-change
```

Diff check: `git diff packages/opencode/src/devilcode/team/config.ts` —
confirm legacy `TeamRole`, `TeamRouting`, `ReactionRule`, `TeamConfig`
exports are UNCHANGED except for the additive block after them. If the
diff shows any edit to the existing blocks, revert and redo.

Diff check: `git diff packages/opencode/src/devilcode/team/presets.ts` —
must be empty.

Diff check: `git diff packages/devil-vscode/` — must be empty.
</verification>

<success_criteria>
- `packages/opencode/src/devilcode/team/library.ts` exports `CanonicalPosition` (11 values), `PositionLibraryEntry`, `POSITION_LIBRARY` (11 entries), `POSITION_CAPABILITY_MAP`, `getDefaultCanDelegate`, `validatePositionLibrary`.
- `packages/opencode/src/devilcode/team/config.ts` gains `CanonicalTeamRole`, `CanonicalTeamRouting`, `CanonicalTeamConfig`, `fromLegacyTeamConfig`, `LegacyMigrationIssue`, `LegacyMigrationResult` — all additive.
- Legacy `TeamRole`, `TeamRouting`, `ReactionRule`, `TeamConfig` unchanged.
- `CanonicalTeamConfig.superRefine` rejects any team missing a stage's required canonical capability; error message lists all missing `stage(capability)` pairs in deterministic order.
- `fromLegacyTeamConfig` successfully migrates all 5 existing presets (test assertions).
- No file outside `team/library.ts`, `team/config.ts`, `test/kilocode/team/library.test.ts`, `test/kilocode/team/canonical-config.test.ts` is modified.
- Full verification sweep (6 commands) all exit 0.
- Devil-vscode IPC path unaffected; server routes unaffected.
</success_criteria>

<output>
Write plan summary to `.planning/phases/01-foundation/01-02-SUMMARY.md`:
- Files created: `team/library.ts`, `test/kilocode/team/library.test.ts`, `test/kilocode/team/canonical-config.test.ts` — LOC counts
- Files modified: `team/config.ts` additive LOC delta (+N); diff-confirms no legacy edits
- Pre-flight results: reconciliation-doc REQUIRES USER DECISION count; server consumers identified beyond config.ts (if any)
- Tests: 11 library + 17 canonical-config = 28 new tests; pass/fail counts
- Legacy regression tests: 2 (TeamRole + TEAM_PRESETS[0]) both passing
- Migration helper: 5/5 presets migrate with `ok: true`; warning counts per preset
- Typecheck: monorepo clean
- Verification sweep: all 6 commands exit 0 / any failures
- Phase 2 handoff notes: (a) migration helper ready for preset flip, (b) extension IPC still speaks legacy shape — Phase 9 owns the extension-side schema update
</output>

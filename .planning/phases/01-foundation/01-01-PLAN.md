---
phase: 1
plan: 1
wave: 1
slug: capability-model-and-reconciliation
depends_on: []
must_haves:
  - PH1-R2
  - PH1-R4
  - PH1-R5
agents:
  primary: senior-developer
  reviewer: backend-architect
estimated_complexity: medium
refine_cycle: 1
files_modified:
  - packages/opencode/src/devilcode/team/capabilities.ts
  - packages/opencode/test/kilocode/team/capabilities.test.ts
  - .planning/phases/01-foundation/01-reconciliation.md
---

<objective>
Lock the canonical capability enum, the stage→capability mapping, and the
compile-time exhaustiveness checks that underwrite every later schema
refinement in this phase. Reconcile the prior 2026-04-06 workflow-tui design
spec against the current Clean-philosophy decision so Plan 01-02 ships on
verified foundations.

**Phase 1 is ADDITIVE** (per refine cycle 1): this plan does not remove or
modify existing legacy `TeamRole` / `TeamConfig` in `team/config.ts`. It
introduces a new canonical namespace that Plan 01-02 extends. Phase 2
performs the migration switch (presets + HTTP boundary) once the canonical
types are proven in isolation.
</objective>

<context>
Phase 1 context: `.planning/phases/01-foundation/01-CONTEXT.md`
Spec: `.planning/specs/01-foundation-spec.md` (Deliverables: capabilities.ts, reconciliation.md, capabilities.test.ts)
Approach: Clean + Additive (strong enums in NEW canonical namespace; legacy types unchanged until Phase 2)

Existing code reference:
- `packages/opencode/src/devilcode/workflow/types.ts:3` — `WorkflowStage` enum, 7 values: plan|challenge|contract|build|review|ship|retro
- `packages/opencode/src/devilcode/team/config.ts:6-15` — existing `TeamRole` (UNTOUCHED BY PHASE 1) with stringly-typed capabilities

Prior design artifacts to reconcile:
- `docs/superpowers/specs/2026-04-06-workflow-tui-design.md`
- `docs/superpowers/plans/2026-04-06-workflow-tui.md`

Integration boundaries discovered during refine cycle 1:
- `packages/devil-vscode/webview-ui/src/types/messages.ts:399-408` defines a parallel `TeamRoleConfig` interface (independent of CLI's Zod). 6 webview files consume it. Additive approach preserves runtime IPC with the extension until Phase 9 catches up.
- `packages/sdk/js/` does NOT import Team types. Safe.

Canonical decisions locked before this plan:
- 8 canonical capabilities: `planning, design, implementation, review, release, testing, research, retrospective`
- Stage→capability map: `plan→planning, challenge→planning, contract→design, build→implementation, review→review, ship→release, retro→retrospective`
- Coordinator owns `retrospective` (encoded in Plan 01-02 library)

Convention: Devil Code uses namespace modules, Zod schemas + `z.infer`
types, Prettier 120/no-semi, co-located/mirror tests in
`packages/opencode/test/kilocode/team/`. No `devilcode_change` markers
needed — `src/devilcode/team/` is entirely Kilo-specific.
</context>

<tasks>

<task id="01-01-T1">
  <title>Read and reconcile the prior 2026-04-06 workflow-tui design spec</title>
  <action>
    1. Read both prior docs in full:
       - `docs/superpowers/specs/2026-04-06-workflow-tui-design.md`
       - `docs/superpowers/plans/2026-04-06-workflow-tui.md`

    2. Extract every concrete claim the prior spec makes about any of:
       - workflow stages
       - team positions or roles
       - capabilities
       - TUI commands or keybindings
       - state machine invariants
       - file layout in `workflow-tui/` or `team/`

    3. Produce `.planning/phases/01-foundation/01-reconciliation.md` with sections:
       - `## Summary` (1 paragraph: prior spec purpose vs. current spec purpose)
       - `## Claims Table` — columns: Claim | Source (file:line) | Disposition
         (Preserved / Superseded / Rejected) | Rationale | New location
         (if Preserved)
       - `## Invariants Carried Forward` — bullets for any prior-spec
         invariant that this Phase 1 spec must honor but does not
         currently mention. For each, note the spec section that should
         be amended.
       - `## REQUIRES USER DECISION` (NEW, from refine cycle 1) — any
         prior-spec claim that directly conflicts with the current spec
         AND cannot be auto-resolved by the agent. For each, describe
         the conflict and propose two concrete resolutions (A or B).
         **Legion:build surfaces this section to the user before Plan
         01-02 starts.** If the section is empty, Plan 01-02 proceeds
         without gate.
       - `## Recommendations for Later Phases` — if the prior docs
         describe anything relevant to Phase 3+ (TUI scaffolding), list
         it here for the future phase's context file to pull in.

    4. If reconciliation surfaces a prior-spec claim about a position,
       capability, or stage that contradicts this Phase 1 spec:
       - If the resolution is trivial (e.g., naming preference): pick
         the current spec's choice and note under `Rejected` with
         rationale. Do NOT silently adopt the prior-spec choice.
       - If the resolution is material (e.g., an additional canonical
         capability the prior spec requires): add to `## REQUIRES USER
         DECISION`.

    5. Downstream wiring: the existence of `## REQUIRES USER DECISION`
       with non-empty content MUST be flagged in the Plan 01-01
       summary (`01-01-SUMMARY.md`). Plan 01-02's Task 1 then reads
       that flag before implementation starts.
  </action>
  <verify>
    - `.planning/phases/01-foundation/01-reconciliation.md` exists
    - All 5 sections present (Summary, Claims Table, Invariants, REQUIRES USER DECISION, Recommendations)
    - Claims Table has at least 1 row referencing a specific line in prior spec
    - If REQUIRES USER DECISION is non-empty: Summary (`01-01-SUMMARY.md`) explicitly lists the count + headlines
  </verify>
  <done>Reconciliation document published; user-decision items (if any) surfaced
  for /legion:build to route to user before Plan 01-02 runs.</done>
</task>

<task id="01-01-T2">
  <title>Implement `team/capabilities.ts`</title>
  <action>
    Create `packages/opencode/src/devilcode/team/capabilities.ts` with the
    following exports, in order:

    1. `import z from "zod"` and `import { WorkflowStage } from "../workflow/types"`.

    2. `CanonicalCapability` — Zod enum with exactly these 8 values:
       `"planning", "design", "implementation", "review", "release", "testing", "research", "retrospective"`

       Export the enum and its `z.infer` type.

    3. `STAGE_CAPABILITY_REQUIREMENTS` — a `const` object literal typed as
       `Record<z.infer<typeof WorkflowStage>, z.infer<typeof CanonicalCapability>>`.
       Entries:
       ```
       plan: "planning"
       challenge: "planning"
       contract: "design"
       build: "implementation"
       review: "review"
       ship: "release"
       retro: "retrospective"
       ```
       Use `as const` and type the const with the explicit `Record` type.

    4. Compile-time exhaustiveness assertion immediately below:
       ```
       // Compile-time: fails typecheck if a new WorkflowStage is added
       // without updating STAGE_CAPABILITY_REQUIREMENTS.
       const _exhaustive: Record<z.infer<typeof WorkflowStage>, z.infer<typeof CanonicalCapability>> =
         STAGE_CAPABILITY_REQUIREMENTS
       void _exhaustive
       ```

    5. Helper: `export function requiredCapabilitiesFor(stages: WorkflowStage[]): CanonicalCapability[]`
       returning the unique set of required capabilities for the given
       stages (Set dedup; preserve stage-order of first occurrence).

    6. JSDoc at top of file:
       - Purpose (single source of truth for stage requirements)
       - Phase 1 is additive: this module is independent of legacy TeamRole/TeamConfig
       - Link: `See .planning/specs/01-foundation-spec.md`

    Formatting: match neighbors in `team/` (Prettier 120/no-semi).

    Do NOT modify `team/config.ts` in this task.
    Do NOT modify `team/presets.ts` (Phase 2 owns it).
  </action>
  <verify>
    - `cd packages/opencode && bun run typecheck` passes
    - `bun turbo typecheck` from repo root passes
    - File exports: `CanonicalCapability`, `STAGE_CAPABILITY_REQUIREMENTS`, `requiredCapabilitiesFor`, inferred `CanonicalCapability` type
    - Manual check: remove `"retro"` entry from map locally → TS error → revert
    - File size roughly 50-70 LOC
  </verify>
  <done>`team/capabilities.ts` committed; exhaustiveness check compiles; no
  downstream file modified.</done>
</task>

<task id="01-01-T3">
  <title>Implement `test/kilocode/team/capabilities.test.ts`</title>
  <action>
    Create `packages/opencode/test/kilocode/team/capabilities.test.ts`
    using Bun's native test runner (`import { test, expect, describe } from "bun:test"`).

    Tests under `describe("capabilities")`:

    1. `CanonicalCapability enum has exactly 8 values` — assert length 8 and set equality
    2. `CanonicalCapability rejects unknown values` — `CanonicalCapability.safeParse("coordination").success === false`
    3. `STAGE_CAPABILITY_REQUIREMENTS covers all 7 WorkflowStage values`
    4. `STAGE_CAPABILITY_REQUIREMENTS has no extra stages`
    5. `each STAGE_CAPABILITY_REQUIREMENTS value is a CanonicalCapability`
    6. `retro stage requires retrospective capability` (regression guard)
    7. `challenge stage reuses planning capability` (regression guard)
    8. `requiredCapabilitiesFor returns unique capabilities preserving order` — input `["plan","challenge","contract"]` → `["planning","design"]`
    9. `requiredCapabilitiesFor(empty) returns empty array`

    Run:
    ```
    cd packages/opencode && bun test test/kilocode/team/capabilities.test.ts
    cd packages/opencode && bun test test/kilocode/team/
    ```
    Both green.
  </action>
  <verify>
    - All 9 new tests pass
    - No regressions in existing `test/kilocode/team/` tests (`concurrency`, `config`, `router`, `types`, `workflow-integration`)
    - `bun turbo typecheck` still clean
  </verify>
  <done>Test file committed, 9 new tests green, existing team tests green.</done>
</task>

</tasks>

<verification>
After all tasks:

```
cd packages/opencode && bun run typecheck
cd packages/opencode && bun test test/kilocode/team/
bun turbo typecheck
```

All three must exit 0.

Also:
```
cd packages/devil-vscode && bun run format:check
cd packages/devil-vscode && bun run knip
cd packages/devil-vscode && bun run check-kilocode-change
```

Must be green — no devil-vscode files touched, but CI enforces.

Manual: open `.planning/phases/01-foundation/01-reconciliation.md` and
verify `## REQUIRES USER DECISION` section exists. If non-empty: Plan
01-01 summary must headline the count and items; Plan 01-02 gates on
user resolution.
</verification>

<success_criteria>
- `packages/opencode/src/devilcode/team/capabilities.ts` exports `CanonicalCapability`, `STAGE_CAPABILITY_REQUIREMENTS`, `requiredCapabilitiesFor` with compile-time exhaustiveness check.
- `packages/opencode/test/kilocode/team/capabilities.test.ts` has 9 passing tests.
- `.planning/phases/01-foundation/01-reconciliation.md` published with 5 sections including `REQUIRES USER DECISION` (may be empty).
- Plan 01-01 summary (`01-01-SUMMARY.md`) headlines any user-decision items.
- `bun turbo typecheck`, `bun test` (opencode team tests), `knip`, `format:check`, `check-kilocode-change` all clean.
- No changes to `team/config.ts`, `team/presets.ts`, `team/agents.ts`, `team/router.ts`, or any devil-vscode file.
</success_criteria>

<output>
Write plan summary to `.planning/phases/01-foundation/01-01-SUMMARY.md`:
- Files created: with LOC counts
- Tests added: 9 cases, pass/fail
- Reconciliation: total claims processed; preserved/superseded/rejected counts; **REQUIRES USER DECISION count + headlines (required field — even if 0, state "0 items; Plan 01-02 proceeds without gate")**
- Downstream blockers for Plan 01-02: enumerated or "none"
- Typecheck status: clean / diagnostics count
</output>
